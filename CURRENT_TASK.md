# 현재 작업 상태
> 항상 자동 로드. 세션 종료마다 갱신. 상세 이력은 `PROGRESS.md`.
> 이 파일은 1~2k 토큰 이하를 유지한다 — "언젠가 할 일"이 아니라 "지금 유효한 작업"만.

## 현재 작업
없음 (상용화 점검에서 나온 법적/공유 공백 2건 처리 완료, 2026-07-28)

- **개인정보처리방침 화면 추가** (`js/screens/legal.js`, `/privacy`, 홈 하단 링크) — 카카오
  애드핏이 기기 식별자·쿠키를 처리하는데 처리방침이 사이트 어디에도 없었다. 이 사이트가 실제로
  하는 일(영속 데이터 없음·회원가입 없음)만 반영해 작성. 법률 자문 대체 아님을 페이지에 명시. → D-31
- **결과 공유 주소별 OG 메타** (`share/<test>/<slug>.html` 20개 + `_redirects` 자동 생성) — 빌드·서버가
  없어 `og:title`/`og:description`이 `index.html` 하나뿐이라, 어떤 결과를 공유해도 카카오톡 미리보기가
  전부 동일했다. `scripts/generate-share-shells.mjs`가 `data.js`에서 유형별 정적 셸을 만들고,
  `_redirects`가 원래 주소 그대로 그 셸을 보여준다(200 rewrite, URL 안 바뀜). `<body>`는 `index.html`과
  동일해 실사용자에게는 평소와 같은 SPA가 그대로 뜬다. `og:image`는 이번 범위에 안 넣었다(별도 이미지
  생성 파이프라인이 필요 — 알려진 이슈 참고). → D-32
- **드리프트 방지**: 생성 로직(`scripts/lib/share-shell.mjs`)을 생성 스크립트와 `npm test`
  (`test/share-shells.test.js`)가 같이 쓴다. 유형을 고치고 `npm run generate:share-shells`를 안 돌리면
  커밋된 셸이 낡는데, 그 어긋남을 바이트 비교로 잡는다. `serve.py`도 `_redirects`의 명시 규칙을 반영하도록
  확장(파일은 프로세스 시작 시 1회만 읽음 — 고치면 서버 재시작 필요).
- 채점 정확도 7건(DISC 궁합·게임 보너스·묵종 방어·경계선 안내 등, D-24~D-30)은 이전에 완료.
  `docs/DECISIONS.md` D-24~D-32 참고 — 이 반기 결정 기록이 300줄을 넘어 `docs/decisions/2026-h2.md`
  (D-1~D-23)와 `docs/decisions/2026-h2-part2.md`(D-24~D-32)로 나뉘었다.

## 다음 작업 우선순위
> "상용화해도 문제없나"에 대한 점검에서 나온 항목. 법적 공백 2건(개인정보처리방침·OG 메타)은 처리했고,
> 아래는 아직 남은 것.

1. **분석·에러 모니터링 전무** — 배포 후 뭔가 깨져도(과거 실제 사례: rAF 콜백이 결과 화면을 밀어버린
   E-4) 아무도 모른다. 최소 하나(Sentry 등 에러 모니터링)는 붙이는 걸 권장.
2. **광고 실렌더링 미확인** — 카카오 애드핏이 실기기에서 실제로 뜨는지 한 번도 못 봤다(샌드박스가
   외부 스크립트 차단). 수익 모델의 전부인데 검증 안 됨. 배포 후 확인 필요.
3. **`og:image` 없음** — 링크 미리보기에 텍스트는 이제 유형별로 다르지만(D-32) 이미지는 여전히 없다.
   결과별 이미지가 필요하면 `drawResultCard()`(캔버스)를 셸 생성 시점에 정적 PNG로 뽑는 파이프라인이
   새로 필요 — 이번 세션 범위 밖.
4. **미니게임 목록이 비어 있음** — `/game`이 "준비 중" 빈 화면인데, 딜레마 게임은 이미 동작한다.
   딜레마 게임을 `/game` 목록에 노출하거나 홈 문구를 조정 / 관련 파일: `js/screens/home.js` `renderGameList()`
   ※ 게임은 테스트 하위 경로에 속해 있다(`docs/DECISIONS.md` D-4). 목록에 넣더라도 경로는 바꾸지 않는다 — id·경로 변경 금지(D-16)
   ⚠️ **반응속도 게임은 여기 넣지 않는다** — 이제 ADHD 문항을 다 풀어야만 들어갈 수 있는 필수 단계라(D-19),
   미니게임 목록에서 단독으로 열도록 노출하면 guard가 사용자를 ADHD 인트로로 되돌려버려 혼란만 준다.

## 배포 후 확인 필요
> 샌드박스는 아웃바운드가 프록시로 막혀 아래를 **확인하지 못했다.** "확인됨"으로 간주하지 말 것.

- **결과 카드 폰트** — CDN 차단으로 결과 카드가 sans-serif 폴백으로만 그려졌다
- **`navigator.share` 공유시트** — 헤드리스엔 API가 없어 클립보드 폴백 경로만 확인됨. 실기기 확인 필요
- **`_redirects` 실동작** — 로컬은 `serve.py`로 흉내 낸 것. 배포본에서 `/test/disc/result/lion` 직접 접속이 200인지 확인
- **광고 슬롯** — AdFit 코드 연동 완료. 샌드박스에서 외부 스크립트(`t1.daumcdn.net`)가 막혀 실제 노출/렌더링은 배포 후 실기기로 확인 필요
- **공유 셸의 실제 링크 미리보기(D-32)** — 로컬 `serve.py` 기준 raw fetch로만 확인했다. Cloudflare Pages가
  실제로 "정적 파일 우선 → `_redirects`" 순서로 매칭하는지, 카카오톡·트위터 디버거에서 유형별 미리보기가
  실제로 다르게 뜨는지는 배포 후 확인 필요

## 알려진 이슈
| 이슈 | 영향 | 우선순위 |
|------|------|----------|
| `og:image` 없음 (텍스트는 D-32로 해결) | 낮음 — 미리보기에 제목·설명은 유형별로 다르지만 이미지는 아직 없다 | 중 |
| `THEME_CLASSES`의 `theme-adhd`에 대응하는 CSS 블록이 없음 | 낮음 — 아무 화면도 `theme: "adhd"`를 쓰지 않아 무해. 쓰려면 `styles.css`에 변수 블록부터 | 하 |
| 헤더 ☰ 버튼이 동작 없음 | 낮음 — 고장으로 보임 | 하 |
| 진행 중 새로고침하면 답변 소실 (`state`가 메모리 전용) | 낮음 | 하 |
| 이용약관 없음 | 낮음 — 계정·거래·업로드가 없는 사이트라 급하지 않음. 요청 시 개인정보처리방침(D-31)과 같은 방식으로 추가 | 하 |

## 보류 (차단됨)
- 없음

## 백로그
(3개월 이상 미처리 항목은 삭제하거나 `docs/IDEAS.md`로 이동)
- 기억력 미니게임 추가 (홈 문구가 이미 예고 중)
- 공유 시 결과 이미지 자동 첨부 (현재는 다운로드 후 수동)
