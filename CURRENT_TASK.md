# 현재 작업 상태
> 항상 자동 로드. 세션 종료마다 갱신. 상세 이력은 `PROGRESS.md`.
> 이 파일은 1~2k 토큰 이하를 유지한다 — "언젠가 할 일"이 아니라 "지금 유효한 작업"만.

## 현재 작업
2026-07-29(5): **오류 검토** — 부부 체크 전 구간을 훑고 실제 결함 1건 수정 + 분포 특성 측정.
① **뒤로가기가 직전 답을 지우던 문제**(D-44). 49문항짜리 검사인데 되돌아간 문항의 답이 사라져
직전에 뭘 골랐는지 볼 수가 없었고, 선택 표시(`picked-most`) 분기는 조건이 영원히 거짓인 죽은
코드였다. 원인은 결과 화면 guard가 진행 상태를 `answers` 개수로 세고 있어서, 답을 안 지우면
미완료 상태에서 `/result`가 열리는 것 — 진행 상태를 `state.couple.completed` 플래그로 분리했다.
검사 2개(브라우저: 선택 유지 / 단위: guard가 플래그를 본다)를 추가하고 **각각 결함을 되살려**
빨간불을 확인했다.
② **분포 측정**(코드 결함 아님, 아래 "알려진 이슈"): 무작위 응답 30,000건 · 개인 결과 720건 ·
부부 조합 4,800건을 돌려 유형 도달성·코덱 왕복·블록 누락을 확인했다. 16유형 전부 도달 가능,
누락 필드 없음. 대신 갈등 스타일이 CS5(절충형)로 69% 몰리고 애착이 AT4 32.6% / AT1 18.7%로
기울며 성향 확신도의 45.7%가 `edge`로 나온다 — 전부 기획서의 절단점에서 나오는 특성이라
파일럿 데이터 없이 임의로 손대지 않았다.
`npm test` 117/117, `scripts/verify.cjs` 116/116.

이전(2026-07-29(4)): **결과 화면이 무겁다는 피드백** 반영 — 산출 로직은 그대로 두고 표면만 정리(D-43).
갈등 스타일·역할 서사를 `<details>`로 접어 첫 화면 막대를 8개 → 6개로 줄였고(§6.1 필수인 4요인 +
애착 2축은 계속 펼침), 결합 결과의 성향 조합을 네 줄 설명 → 칩 한 줄 + 눈여겨볼 항목만 설명으로,
인지 격차는 "비슷함" 항목을 한 줄로 묶었다. 설명문 전반 축약, 이용 안내도 접기 도입.
개인 결과 3027 → 2701px, 결합 결과 2314 → 2119px, 안내 3430 → 2141px.
**문항 수는 줄이지 않았다** — 늘어난 문항은 전부 측정 오차용이라 건드리면 정확도가 실제로 내려간다.
`npm test` 116/116, `scripts/verify.cjs` 115/115.

이전(2026-07-29(3)): 기획서 **v3.3**(외부 검토 반영) 적용 + **이용 안내 화면 신설**.
v3.3에서 이 사이트에 해당하는 건 3건 — ① 앵커 배치: 35~49번 15칸에 6개를 2칸 이격으로 놓으면
16칸이 필요해 성립하지 않던 문제를 기획서가 이격 완화로 해소해서, D-36의 우회(구간 34번 시작)를
되돌리고 35번 기준으로 재배치(D-40). ② 역채점 정합성 임계 3 → 3.5(위양성 여지). ③ 동점
타이브레이크를 고정 순서에서 시드 기반 난수로(D-41 — 고정 순서는 동점을 전부 D로 몰아 집계를
왜곡한다). 규준이 필요한 항목(Z→경험적 백분위, 세그먼트)은 애초에 동작하지 않는 경로라 미적용.

**이용 안내 화면**(`/test/couple/guide`, D-42): 사용자가 "둘이 하면 더 정확해진다"고 오해하기
쉬운데 실제로는 개인 점수가 자기 응답만으로 계산된다. 합쳐서 얻는 건 정밀도가 아니라 혼자서는
존재할 수 없는 정보라는 점, 차이값에 두 사람 오차가 함께 실린다는 점, 결합 신뢰도가 평균이
아니라 약한 쪽을 따른다는 점을 쉬운 말로 풀었다. 인트로·초대·결합 결과에서 링크.
`npm test` 116/116, `scripts/verify.cjs` 113/113.

이전(2026-07-29(2)): 기획서가 **v3.2**로 개정되어 반영 완료. 세 갈래다 —
① 컴플라이언스: 애착 4유형·갈등 5스타일을 자사 코드(AT1~AT4 / CS1~CS5)로 전면 재작명하고
축 명칭도 임상 용어에서 행동 서술형으로 바꿨다(원 척도의 표준 번역어를 그대로 쓰고 있어서
상위 명칭만 바꾼 껍데기였다). 낙인으로 읽히는 유형명 5개도 중립 서술형으로 교체.
② 측정: 앵커를 개념당 2문항으로 증설(46→49문항)하고 후반부 **분산** 배치로 바꿨다. D3·QC2 교체,
역채점 정합성 검사 신설, 확신도 임계값 재조정, 갈등 절단점을 원점수 기준으로.
③ 매칭: **단일 궁합 점수·등급·Risk Matrix 폐기** → 요인별 3구간 + 애착 조합 10태그 + 앵커 개념별
격차 + 환경축 비교. 격차에서 **방향 표시도 제거**(크기와 개념명만). 배우자 코드 v2(앵커를 개념
점수로만 실어 문항값 복원 불가).
`npm test` 114/114, `scripts/verify.cjs` 108/108. 관련 결정 D-36~D-39.
기획서와 다르게 한 것: 앵커 구간 시작을 35 → 34번(35+minGap 2는 49문항에서 동시 만족 불가, D-36),
부모/연인 게이지 제거(v3.2에서 근거 절이 사라짐, D-37).

이전(2026-07-29 최초): 기획서(`AI 기반 부부 맞춤형 심리 진단 서비스 v3.0`) 검토 후 **부부 관계 성향 체크**
신설 — `js/tests/couple/`(data · assemble · score · match · screens · index), 화면 9개
(`/test/couple` · `/setup` · `/play` · `/ad` · `/result` · `/invite` · `/pair?p=` · `/together` ·
`/result/<슬러그>`), 새 테마 `theme-couple`. 46문항(가변 문항은 축별 3버전) · 16유형 · 연속
프로필 · 확신도 · 응답 품질 플래그 · 앵커 기반 인지 격차 · 부모/연인 게이지까지 기획서대로 넣었다.
배우자 매칭은 백엔드가 없어 **결과를 21자 코드로 주소에 실어 보내는 방식**으로 대체했다(D-32).
`npm test` 102/102, `scripts/verify.cjs` 105/105 통과.
기획서에서 **고쳐서 넣은 것**: 갈등 경계 폭 6.25 → 척도 파생 8.33(D-33), 비공개 고지 문구(D-34),
안내 전화번호 1577-1366·1393 → 1577-9337·1577-0199(D-35), §5.7의 R3/R2 코드 불일치는 R2로 통일.
**구현하지 않은 것**(백엔드·수집 데이터 부재): 백분위·세그먼트 비교·규준, 페어링 코드 만료·
재검사 쿨다운·데이터 파기, α 모니터링. 상세는 `docs/couple-architecture.md` §9.

이전(2026-07-28): 독립 미니게임 **NumPath: Stack & Clear**(숫자 경로 퍼즐) 추가 + main 머지 완료.
`js/games/numpath/`(engine·generate·solve·audio·play·screens·index) 신설, 라우터에
`registerGame`/`listGames` 신규 도입(`registerTest`/`listTests`와 대칭), `/game/numpath/*` 4화면.
`renderGameList()`가 이제 `listGames()`로 카드를 자동 생성해 "미니게임 목록이 비어 있음" 항목이
해소됨. 리뷰 중 `starsFor()`의 2성 등급이 모든 레벨에서 도달 불가능하던 버그를 찾아 같이 수정.
이어서 사용자 피드백("목표값이 안 보이고 설명 부족")으로 플레이 화면 HUD에 "현재값 → 목표값"
전용 배너를 추가하고 인트로에 칸 종류(시작/연산/배수/차단) 안내를 붙임 — 전부 main 머지 완료.
`npm test` 61/61, `scripts/verify.cjs` 64/64(로컬, 여러 차례 재현) 통과.
관련 결정: `docs/decisions/2027-h1.md` D-28(독립 게임 레지스트리)·D-29(공유 URL, D-7과 반대
방향)·D-30(타일 모델)·D-31(1차 범위 축소). 상세는 `docs/numpath-architecture.md`.
`docs/architecture.md`가 15KB를 넘겨 자동 분리 규칙에 따라 `docs/adhd-architecture.md` ·
`docs/disc-architecture.md` · `docs/numpath-architecture.md`로 나뉘었다 — 이제부터 ADHD/DISC
상세를 찾을 땐 architecture.md가 아니라 이 세 파일을 본다.

이전(2026-07-28 앞부분, 메인 머지 완료): 광고 수익화 3건·DISC 명칭 변경·딜레마 4택 재설계·
데이터팬트리 연동·홈 화면 기능 없는 헤더 ☰ 버튼 제거. 상세는 `PROGRESS.md` 2026-07-28 항목,
관련 결정은 `docs/DECISIONS.md` D-24~D-27.

## 다음 작업 우선순위
없음. (지난 세션에 올렸던 **QC2 위양성** 항목은 v3.2가 QC2를 I1의 근접 패러프레이즈로 교체하면서
해소됐다.)

## 배포 후 확인 필요
> 샌드박스는 아웃바운드가 프록시로 막혀 아래를 **확인하지 못했다.** "확인됨"으로 간주하지 말 것.

- **결과 카드 폰트** — CDN 차단으로 결과 카드가 sans-serif 폴백으로만 그려졌다
- **`navigator.share` 공유시트** — 헤드리스엔 API가 없어 클립보드 폴백 경로만 확인됨. 실기기 확인 필요
- **`_redirects` 실동작** — 로컬은 `serve.py`로 흉내 낸 것. 배포본에서 `/test/disc/result/lion` 직접 접속이 200인지 확인
- **광고 슬롯(배너·rect·interstitial)** — 코드 연동 + SPA 재스캔 버그 수정(`refreshAds()`) 완료. 로더 태그가
  화면 전환마다 실제로 갈아끼워지는지는 `scripts/verify.cjs`가 확인하지만, 샌드박스는 `t1.daumcdn.net`이 막혀
  **실제 광고가 그려지는지는 확인하지 못했다.** 배포 후 실기기 확인 필요 — 특히 홈→테스트 화면 등 여러 번
  이동하며 매번 광고가 뜨는지, 광고 게이트(`reaction-ad`/`dilemma-ad`)의 300×250도 포함해서.
  안 나오면 다음 순서로 본다: ① 로더 `<script>`가 화면 전환마다 새 엘리먼트로 갈아끼워지는가(DevTools Elements)
  ② 그 요청이 200인가(Network) ③ 둘 다 정상이면 코드가 아니라 **AdFit 쪽 노출 유예/미충족**이다(승인 직후 몇 시간~1일, 트래픽 부족 시 미충족)
- **OG 이미지 실제 미리보기** — 카카오톡/트위터/페이스북 디버거로 `https://fun.data-pantry.com/assets/og-image.png`가 실제로 불러와지는지, 카드가 `summary_large_image`로 정상 렌더되는지 배포 후 확인 필요 (샌드박스는 절대경로 이미지 요청을 외부에서 검증 못 함)
- **데이터팬트리 헤더 FUN 버튼** — `data-pantry-web-site` 저장소의 헤더에 새 링크를 추가했음. 두 사이트가 각각 배포된 뒤 실제로 FUN 버튼 → `fun.data-pantry.com` 이동이 되는지, 이 사이트 하단 `by 데이터팬트리` 링크 → `data-pantry.com` 이동이 되는지 상호 확인 필요
- **NumPath Web Audio 실기기 동작** — 헤드리스엔 오디오 출력이 없어 확인 못함. iOS Safari의 첫
  제스처 전 `AudioContext` suspended 상태에서 타일 클릭으로 정말 resume되는지, 무음 스위치일 때
  거슬리지 않는지 확인 필요
- **NumPath 광고 슬롯 실제 노출** — 기존 항목과 같은 이유(`t1.daumcdn.net` 차단)로 `/game/numpath/*`
  4화면·광고 게이트(`numpath-ad`)의 300×250 포함해서 실기기 확인 필요
- **NumPath 5×5 보드 터치 히트 영역** — 고레벨(레벨 4) 그리드의 타일이 실제 손가락으로 누르기에
  충분한 크기인지 실기기에서 확인 필요
- **부부 체크 49문항 이탈률** — 이 사이트의 다른 검사(12~16문항, 약 1분)보다 훨씬 길다(약 6분 30초).
  기획서가 정확도를 위해 의도적으로 늘린 것이라 그대로 넣었지만, 홈 문구는 여전히 "1분이면 끝나는"이다.
  실제 이탈 지점(축 선택 화면인지, 문항 중반인지)을 Cloudflare Web Analytics 경로별 필터로 확인 필요
- **부부 체크 배우자 초대 링크 실제 전달** — 카카오톡으로 `?p=<코드>` 링크를 보냈을 때 끝이 잘리거나
  미리보기 생성 과정에서 변형되지 않는지 실기기 확인 필요. 잘린 코드는 체크섬에 걸려 인트로로
  떨어지도록 해뒀지만(회귀 검사 있음), "잘리는지 여부" 자체는 샌드박스에서 확인 못 했다
- **부부 체크 결과·결합 결과 광고 슬롯** — 기존 항목과 같은 이유(`t1.daumcdn.net` 차단)로
  `/test/couple/*` 9화면과 광고 게이트(`couple-ad`)의 300×250 포함 실기기 확인 필요
- **`docs/architecture.md` 크기** — 부부 체크 화면 9개가 늘면서 16.6KB가 됐다(이전 14.7KB).
  다음에 테스트를 또 추가하면 화면 표를 테스트별 문서로 마저 쪼갤지 판단 필요
- **홈 화면 헤더 레이아웃** — 기능 없던 우측 상단 ☰ 아이콘 제거 후 로고만 남은 좌측 정렬이 실제 배포본에서도 깨지지 않는지 확인 필요

## 알려진 이슈
| 이슈 | 영향 | 우선순위 |
|------|------|----------|
| 페이지별(테스트별) OG 메타는 여전히 공통 | 낮음 — `og:image`는 2026-07-28에 채웠으나, 정적 SPA라 테스트별 미리보기 문구·썸네일 분기는 안 됨(결과 주소마다 정적 셸이 있어야 가능) | 하 |
| `THEME_CLASSES`의 `theme-adhd`에 대응하는 CSS 블록이 없음 | 낮음 — 아무 화면도 `theme: "adhd"`를 쓰지 않아 무해. 쓰려면 `styles.css`에 변수 블록부터 | 하 |
| 진행 중 새로고침하면 답변 소실 (`state`가 메모리 전용) | 부부 체크에서만 중간 — 49문항 약 6분 30초를 통째로 잃는다. 기획서 §9.1은 임시 저장을 요구하지만 이 사이트는 "영속 데이터 없음"(D-20)이고, 같은 기기를 부부가 번갈아 쓰면 남의 응답이 복원되는 프라이버시 문제가 생긴다. 도입하려면 두 가지를 함께 정해야 함 | 중 |
| 갈등 스타일이 CS5(절충형)에 69% 집중 | 낮음 — 기획서의 원점수 절단점(저 ≤7 / 고 ≥11) 특성. 3문항 척도라 중간대가 넓다. 기획서 §10도 실응답에서 집중이 나타날 것을 예고했고, 절단점 조정은 파일럿 데이터가 있어야 근거가 생긴다 | 하 |
| 애착 유형이 AT4 32.6% / AT1 18.7%로 기울고, 성향 확신도 45.7%가 `edge` | 낮음 — 기획서의 `원점수 ≥12 → 높음` 규칙에서 나오는 특성. 무작위 응답 기준이라 실응답 분포와 다를 수 있다. 사용자에게는 `edge` 플래그로 "경계에 가깝다"고 이미 고지된다 | 하 |

## 보류 (차단됨)
- 없음

## 백로그
(3개월 이상 미처리 항목은 삭제하거나 `docs/IDEAS.md`로 이동)
- 기억력 미니게임 추가 (홈 문구가 이미 예고 중)
- 공유 시 결과 이미지 자동 첨부 (현재는 다운로드 후 수동)
