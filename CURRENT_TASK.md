# 현재 작업 상태
> 항상 자동 로드. 세션 종료마다 갱신. 상세 이력은 `PROGRESS.md`.
> 이 파일은 1~2k 토큰 이하를 유지한다 — "언젠가 할 일"이 아니라 "지금 유효한 작업"만.

## 현재 작업
AI 협업용 컨텍스트 문서 체계 도입 — 완료 (2026-07-27)

## 다음 작업 우선순위
1. **미니게임 목록 비어 있음** — `/game`이 "준비 중" 빈 화면인데 홈에서 "반응속도·기억력"으로 유도한다.
   반응속도 게임을 `/game`에도 노출하거나 홈 문구를 조정 / 관련 파일: `app.js` `renderGameList()` · `renderHome()`
2. **홈 하단 네비게이션이 장식** — 인기·저장·내정보 탭이 클릭해도 아무 동작 없음. 동작 부여 또는 제거 / 관련 파일: `app.js` `renderHome()` · `styles.css` `.bottom-nav`
3. **광고 슬롯 자리표시자** — `.ad-slot`이 "카카오 AdFit · 320×50" 텍스트만 있는 상태. 실제 연동 또는 제거 / 관련 파일: `app.js` `renderHome()` · `styles.css` `.ad-slot`

## 배포 후 확인 필요
> 샌드박스는 아웃바운드가 프록시로 막혀 아래를 **확인하지 못했다.** "확인됨"으로 간주하지 말 것.

- **결과 카드 폰트** — CDN 차단으로 `drawResultCard()`가 sans-serif 폴백으로만 그려졌다
- **`navigator.share` 공유시트** — 헤드리스엔 API가 없어 클립보드 폴백 경로만 확인됨. 실기기 확인 필요
- **`_redirects` 실동작** — 로컬은 `serve.py`로 흉내 낸 것. 배포본에서 `/test/adhd/result/typhoon` 200 확인
- **광고 슬롯** — 자리표시자 상태라 노출 확인 불가

## 알려진 이슈
| 이슈 | 영향 | 우선순위 |
|------|------|----------|
| 페이지별 OG 메타·`og:image` 없음 | 중 — 공유 미리보기가 전부 동일, 썸네일 없음 | 중 |
| 헤더 ☰ 버튼·결과 화면 "상식 퀴즈" 카드가 동작 없음 | 낮음 — 고장으로 보임 | 하 |
| 진행 중 새로고침하면 답변 소실 (`state`가 메모리 전용) | 낮음 | 하 |

## 보류 (차단됨)
- 없음

## 백로그
(3개월 이상 미처리 항목은 삭제하거나 `docs/IDEAS.md`로 이동)
- 기억력 미니게임 추가 (홈 문구가 이미 예고 중)
- 공유 시 결과 이미지 자동 첨부 (현재는 다운로드 후 수동)
