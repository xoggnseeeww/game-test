# 현재 작업 상태

> 항상 자동 로드. 세션 종료마다 갱신.
> ⚠️ **이 파일은 1~2k 토큰 이하를 유지한다** — "언젠가 할 일"이 아니라 "지금 유효한 작업"만.
> 지난 항목은 쌓지 말고 그달의 `archive/PROGRESS_YYYY-MM.md`로 **옮긴다**(요약이 아니라 이동).
> 이 규칙을 안 지켜 30k 토큰까지 불어난 적이 있다(D-105) — 매 세션 자동 로드라 그대로 비용이다.

| 찾는 것 | 볼 곳 |
|---------|-------|
| 왜 이렇게 만들었나 (설계 근거·기각안) | `docs/DECISIONS.md` 인덱스 → D-번호 |
| 이 증상 전에 났었나 | `docs/ERRORS.md` |
| 그날 뭘 했나 (검증 로그 전문) | `PROGRESS.md` 인덱스 → `archive/PROGRESS_YYYY-MM.md` |
| 배포 후 확인 못 한 것 | `docs/VERIFY_PENDING.md` |

## 현재 작업
2026-08-30: **부부 체크 집중 작업을 main에 병합**(D-107~D-114). 심화 서술 신설 → 결합
기능 전면 삭제 → 피해야 할 대화법 → 애정 표현 5유형 → 문항 밀도 강화 → 결과 화면
다듬기 → 절충형(CS5) 쏠림 분해까지 일곱 라운드. 마지막 라운드는 **가상 응답자 5000명을
채점 파이프라인에 통과시켜 분화도를 재보고** 결정했다 — 16유형·400조합은 이미 고르게
갈리는데 갈등만 CS5에 54% 몰려 있어서, 문항을 늘리는 대신 그 안의 다섯 조합에 서술을
붙였다(갈등 서술 9개, 최대 11.6%). 병합하며 어휘 작업(D-98~D-106)과 D-번호가 겹쳐
부부 쪽을 D-107~D-114로 재번호했고, 개인정보처리방침에서 부부 매칭 코드(KV) 절을
지웠다 — 그 기능이 D-108로 사라졌는데 main 쪽 방침에는 남아 있었다. 상세 이력은
`archive/PROGRESS_2026-08.md`. **부부 체크는 아직 관리자 게이트(D-56) 뒤라 일반 방문자에게는
안 보인다** — 실사용 피드백을 받으려면 게이트를 여는 결정이 따로 필요하다.


2026-08-28(25): **어휘 2단계 신설 — DAY 5~8, 200단어 추가**(D-106). 1단계(DAY 1~4)가 거의
전부 동사라, 2단계는 "동사를 더"가 아니라 **품사를 넓히는 쪽**으로 열었다 — DAY 5 형용사 ①
· DAY 6 명사 ①(제도·사회·경제) · DAY 7 동사 ②(1단계에서 안 다룬 접두사 계열) · DAY 8
형용사 ②. 공무원 지문에서 막히는 건 대개 동사가 아니라 수식어·추상명사라서다. 어원 사전도
158 → 282항목으로 늘었고(형용사·명사는 어근이 흩어져 있어 재사용률이 낮다 — 예상된 비용),
철자가 겹치고 뜻이 다른 어근은 `in`/`in-neg` 방식대로 id를 갈랐다(`par-appear`/`par-equal`,
`nom`/`nom-name`, `dis`/`di-two`). 화면·라우팅·테스트는 한 줄도 안 고쳤다 — 설계대로
`words/day-NNN.js` 추가 + `manifest.js` 줄 하나가 전부였다. 작업하다 두 번 걸린 제약
(같은 DAY 안 `ko[0]` 중복 → 4지선다 불가, 예문에 표제어가 다른 단어 일부로 재등장 →
빈칸에 답이 남음)은 다음에 또 밟지 않게 `docs/vocab-architecture.md` §7에 적었다.
**검증**: `npm test` 222/222(데이터 검사가 200 → 400단어를 돈다) · `scripts/verify.cjs` 전체
통과 · 어휘 도구는 verify.cjs가 안 건드리므로 실제 브라우저에서 2단계 목차·DAY 5 동적
import·8개 DAY 개수 일치를 따로 확인했다.

## 다음 작업 우선순위

- **어휘 콘텐츠 확장(M5) 이어가기** — 2단계(DAY 5~8)까지 왔다. 다음은 3단계다. 절차는
  `words/day-NNN.js` 추가 + `manifest.js` 한 줄이 전부고(화면·라우팅·테스트 무변경),
  **쓰기 전에 `docs/vocab-architecture.md` §7의 제약 넷을 먼저 읽을 것**. 한 번에 몰아
  만들지 말 것 — D-73/74에서 "검증 없이 양만 늘리면 품질이 떨어진다"를 겪었다.
  지금 몇 단어인지는 `manifest.js`의 `TOTAL_WORDS`가 단일 소스다(여기 숫자를 박지 않는다).
- **어휘 통계·스트릭(M4)** — 마이페이지 `summary()` 훅은 D-101에서 붙었다. 일별 집계
  (`vocab_daily_stats`)와 스트릭은 아직.
- (선택) `docs/decisions/2027-h1.md`가 82KB로 분할 규칙 위반 상태 — 과거 아카이브라 급하진
  않지만, D-번호 하나 찾자고 82KB를 읽어야 하는 건 낭비다. 손댈 일이 생기면 그때 쪼갠다.

## 배포 후 확인 필요

→ **`docs/VERIFY_PENDING.md`** (분량이 커서 분리, D-105). 가장 급한 것 하나만 여기 남긴다:

- **[사용자 조치 필요] 일반 방문자 로그인이 실기기에서 안 됨** — Supabase 대시보드 →
  Authentication → URL Configuration → Redirect URLs에 `https://fun.data-pantry.com/` 추가
  필요(코드/MCP로는 못 고침). **이게 막혀 있으면 계정 저장 기능 전체(D-68·D-100·D-102·D-104)를
  실기기에서 확인할 수 없다** — 확인 대기 항목 상당수가 여기에 묶여 있다.

## 알려진 이슈

| 이슈 | 영향 | 우선순위 |
|------|------|----------|
| 결과 공유 슬러그(`/test/*/result/<slug>`, 20개) OG 메타가 공통 | 낮음 — 진입 화면 3곳은 D-47에서 분리됨. 슬러그별 미리보기는 유형마다 셸이 늘어 유지비가 커서 범위 밖 | 중 |
| 진행 중 새로고침하면 답변 소실(`state`가 메모리 전용) | 부부 체크에서만 중간(64문항 ≈6분 30초를 통째로 잃는다). 임시 저장은 "영속 데이터 없음"(D-20)과 충돌하고, 같은 기기를 부부가 번갈아 쓰면 남의 응답이 복원되는 문제가 생긴다 — 도입하려면 둘을 함께 정해야 함 | 중 |
| ~~갈등 CS5 쏠림~~ 해소(D-114) — 5000명 시뮬레이션에서 54.3%로 재측정하고 그 안을 다섯 서술로 나눴다(최대 11.6%). 애착 AT4 32.6%·확신도 45.7% `edge`는 그대로 | 낮음 — 기획서 절단점 특성이고 무작위 응답 기준이라 실응답과 다를 수 있다. 절단점 조정은 파일럿 데이터가 있어야 근거가 생긴다 | 하 |
| `THEME_CLASSES`의 `theme-adhd`에 대응하는 CSS 블록 없음 | 낮음 — 아무 화면도 안 써서 무해 | 하 |

## 보류 (차단됨)
- 없음

## 백로그
(3개월 이상 미처리 항목은 삭제한다. 아이디어로 남길 값이 있으면 그때 `docs/IDEAS.md`를 만들어 옮긴다 — 지금은 없는 파일이다)
- 기억력 미니게임 추가 (홈 문구가 이미 예고 중)
- 공유 시 결과 이미지 자동 첨부 (현재는 다운로드 후 수동)
