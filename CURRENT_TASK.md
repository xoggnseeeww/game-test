# 현재 작업 상태

> 항상 자동 로드. 세션 종료마다 갱신.
> ⚠️ **이 파일은 1~2k 토큰 이하를 유지한다** — "언젠가 할 일"이 아니라 "지금 유효한 작업"만.
> 지난 항목은 쌓지 말고 그달의 `archive/PROGRESS_YYYY-MM.md`로 **옮긴다**(요약이 아니라 이동).
> 이 규칙을 안 지켜 30k 토큰까지 불어난 적이 있다(D-105) — 매 세션 자동 로드라 그대로 비용이다.

| 찾는 것 | 볼 곳 |
|---------|-------|
| 왜 이렇게 만들었나 (설계 근거·기각안) | `docs/DECISIONS.md` 인덱스 → D-번호 |
| 이 증상 전에 났었나 | `docs/ERRORS.md` |
| 그날 뭘 했나 (검증 로그 전문) | `PROGRESS.md` 인덱스 → `archive/PROGRESS_YYYY-MM.md` |
| 배포 후 확인 못 한 것 | `docs/VERIFY_PENDING.md` |

## 현재 작업

2026-08-19(24): **MD 문서 정리 — 장기 관리용 최적화**(D-105). 사용자 요청("장기적으로
관리하기 위한 최적화, 분할·병합·요약, 토큰 최적화 고려"). **문제**: 매 세션 자동 로드되는
`CLAUDE.md` + `CURRENT_TASK.md`가 **81.6k 토큰**이었고 그중 `CURRENT_TASK.md`가 **61.8k**
(자기 규칙 1~2k의 30배 이상) — 8월 항목 50여 개가 이동 없이 계속 쌓였다. `PROGRESS.md`는
7월에서 멈춰 있었고(이관이 한 번도 안 일어남) 133KB, "배포 후 확인 필요" 80줄도 자동 로드에
얹혀 있었다. **정리**: ①8월 항목 전부 → `archive/PROGRESS_2026-08.md`(원문 그대로, 무손실)
②`PROGRESS.md` → 월별 인덱스만(전문은 `archive/PROGRESS_YYYY-MM.md`, DECISIONS 인덱스 패턴과
동일) ③"배포 후 확인 필요" → `docs/VERIFY_PENDING.md`로 분리 + 주제별로 묶음 ④`CLAUDE.md`
구조 개요에서 콘텐츠 인벤토리(문장 수·챕터 목록)를 걷어냄(이미 docs에 있던 중복) ⑤`ERRORS.md`도
자기 규칙대로 `errors-runtime.md`/`errors-ai.md`로 분리(인덱스에 빠져 있던 E-12~E-15도 채움)
⑥**분리 임계값에서 "15KB" 절 제거** — 한글은 1자=3바이트라 바이트 절이 항상 먼저 터져서
멀쩡한 문서를 쪼개게 만든다(A-4가 기록해둔 함정을 규칙 자체가 저지르고 있었다). 줄 수만 남겼다.
아카이브 단위도 반기 → **월**로 바꿨다(반기 파일이 200KB를 넘어 한 번에 못 읽는다).
**결과**: 자동 로드 **81.6k → 17.6k 토큰**(세션당 약 64k·78% 절감, A-4 공식 기준). 삭제한
정보는 없다 — 전부 이동이거나 이미 다른 문서에 있던 중복 제거다.

## 다음 작업 우선순위

- **어휘 콘텐츠 확장(M5)** — 200단어 → 800 → 2000 → 4000 → 8000. 구조는 다 갖춰졌고
  `words/day-NNN.js` 추가 + `manifest.js` 한 줄이면 된다(화면·라우팅·테스트 무변경).
  한 번에 몰아 만들지 말 것 — D-73/74에서 "검증 없이 양만 늘리면 품질이 떨어진다"를 겪었다.
- **어휘 통계·스트릭(M4)** — 마이페이지 `summary()` 훅은 D-101에서 붙었다. 일별 집계
  (`vocab_daily_stats`)와 스트릭은 아직.
- (선택) `docs/decisions/2027-h1.md`가 82KB로 분할 규칙 위반 상태 — 과거 아카이브라 급하진
  않지만, D-번호 하나 찾자고 82KB를 읽어야 하는 건 낭비다. 손댈 일이 생기면 그때 쪼갠다.

## 배포 후 확인 필요

→ **`docs/VERIFY_PENDING.md`** (분량이 커서 분리, D-105). 가장 급한 것 하나만 여기 남긴다:

- **[사용자 조치 필요] 일반 방문자 로그인이 실기기에서 안 됨** — Supabase 대시보드 →
  Authentication → URL Configuration → Redirect URLs에 `https://fun.data-pantry.com/` 추가
  필요(코드/MCP로는 못 고침). **이게 막혀 있으면 계정 저장 기능 전체(D-68·D-100·D-102·D-104)를
  실기기에서 확인할 수 없다** — 확인 대기 항목 상당수가 여기에 묶여 있다.

## 알려진 이슈

| 이슈 | 영향 | 우선순위 |
|------|------|----------|
| 결과 공유 슬러그(`/test/*/result/<slug>`, 20개) OG 메타가 공통 | 낮음 — 진입 화면 3곳은 D-47에서 분리됨. 슬러그별 미리보기는 유형마다 셸이 늘어 유지비가 커서 범위 밖 | 중 |
| 진행 중 새로고침하면 답변 소실(`state`가 메모리 전용) | 부부 체크에서만 중간(49문항 ≈6분 30초를 통째로 잃는다). 임시 저장은 "영속 데이터 없음"(D-20)과 충돌하고, 같은 기기를 부부가 번갈아 쓰면 남의 응답이 복원되는 문제가 생긴다 — 도입하려면 둘을 함께 정해야 함 | 중 |
| 갈등 스타일이 CS5(절충형)에 69% 집중 / 애착 AT4 32.6%·확신도 45.7% `edge` | 낮음 — 기획서 절단점 특성이고 무작위 응답 기준이라 실응답과 다를 수 있다. 절단점 조정은 파일럿 데이터가 있어야 근거가 생긴다 | 하 |
| `THEME_CLASSES`의 `theme-adhd`에 대응하는 CSS 블록 없음 | 낮음 — 아무 화면도 안 써서 무해 | 하 |

## 보류 (차단됨)
- 없음

## 백로그
(3개월 이상 미처리 항목은 삭제한다. 아이디어로 남길 값이 있으면 그때 `docs/IDEAS.md`를 만들어 옮긴다 — 지금은 없는 파일이다)
- 기억력 미니게임 추가 (홈 문구가 이미 예고 중)
- 공유 시 결과 이미지 자동 첨부 (현재는 다운로드 후 수동)
