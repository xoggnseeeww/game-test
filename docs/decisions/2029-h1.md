# DECISIONS 2029 상반기(6차 분리) — 전체 항목

> `docs/DECISIONS.md`의 자기 관리 규칙(300줄/15KB 초과 시 자동 분리)에 따라, 이전 파일
> `docs/decisions/2028-h2.md`가 D-83 추가로 한계를 넘어(15.4KB) 이 파일로 이어졌다
> (2026-08-06, D-84 추가 시점). 파일명은 실제 작성 시점의 반기가 아니라 순번이다 —
> `2027-h1.md`부터 이어진 것과 같은 이유.
> 새 항목은 이 파일 끝에 추가하고, 이 파일이 다시 300줄/15KB를 넘으면 `2029-h2.md`로 넘긴다.

### D-84. 초등 영어회화 3건 개선 — 이전 문장 이동·produce 질문 자동 재생·TTS 음성 품질
- **배경**: 사용자가 실제로 써보고 세 가지를 지적: ①챕터에 들어가면 다음 문장으로만
  갈 수 있고 이전 문장으로 돌아갈 수 없다. ②`type: "produce"`(질문·답변형) 문제에서
  질문 문장을 소리 내어 들려주는 기능이 없다(버튼을 눌러야만 들림 — 실제 대화라면
  질문이 먼저 들리는 게 자연스럽다). ③듣기 음성이 기계음처럼 부자연스럽다.
- **① 이전 문장 이동**: `basic-conversation/screens.js`의 `showCard()`와
  `elementary-conversation/screens.js`의 `showRepeatCard()`/`showProduceCard()` 모두
  현재 문장 인덱스가 0보다 클 때만 "◀ 이전 문장" 버튼(기존 `.retry-btn` 재사용, 새
  CSS 없음)을 보여준다. elementary 쪽은 `advance()`와 대칭인 `prev()` 헬퍼를 추가해
  `st.index -= 1` 후 `showCard()`를 다시 부른다 — 복습 모드(`reviewMode`)로 필터링된
  `sentences` 배열 안에서도 그대로 동작한다(인덱스는 항상 현재 활성 배열 기준). basic
  쪽은 기존 "다음 문장" 핸들러와 대칭으로 인라인 처리(그쪽 파일 자체가 아직 헬퍼로 안
  뽑혀 있어 굳이 새로 안 뽑았다).
- **② produce 질문 자동 재생**: `showProduceCard()` 끝에 `if (supportsSpeech())
  speak(card.text, 1)`를 추가해, 카드가 뜨면(처음 진입·이전/다음 이동 전부 포함)
  질문을 한 번 자동으로 읽어준다. "repeat" 카드는 그대로 자동 재생을 안 한다 — 그
  카드는 아이가 듣기 버튼을 눌러 스스로 페이스를 조절하는 게 원래 설계 의도라
  건드리지 않았다.
- **③ TTS 음성 품질**: `js/learning/speech.js`의 `speak()`에 `pickVoice(lang)`을
  추가 — `speechSynthesis.getVoices()`에서 해당 언어의 음성 중 `localService`가
  아닌(네트워크) 음성을 우선 골라 `utterance.voice`에 지정한다. 기기 내장 로컬 음성은
  대체로 기계음에 가깝고, Chrome의 "Google US English" 같은 네트워크 음성이 훨씬
  자연스럽다는 게 흔한 차이라 이 우선순위를 그대로 썼다. 특정 음성 이름을 하드코딩하지
  않은 이유는 기기·브라우저마다 음성 목록이 달라 이름 매칭이 깨지기 쉬워서다 —
  "로컬이냐 아니냐"만 보는 게 기기 간 이식성이 높다. 음성 목록이 아직 비동기 로드
  전이면(Chrome 특성) `matching`이 빈 배열이라 `voice`가 `null`로 남고, 그때는 그냥
  브라우저 기본 음성으로 재생된다 — 별도 대기 로직은 추가하지 않았다(다음 재생부터는
  목록이 채워져 있어 자연스럽게 좋은 음성으로 바뀐다). `speak()`가 두 도구
  (basic-conversation·elementary-conversation) 공용이라 이 개선이 자동으로 둘 다에
  적용된다.
- **검증**: `npm test` 147/147(로직 변경 없음, 순수 UI/음성 선택 추가). 헤드리스
  브라우저: (1) 두 도구 모두 1번째 문장엔 이전 버튼이 없고 2번째부터 생기는지, 클릭
  시 인덱스가 정확히 하나 줄어드는지 확인. (2) produce 카드에서만
  `speechSynthesis.speak()`가 자동 호출되고 repeat 카드에서는 호출되지 않는지 확인
  (중급 6문장 중 정확히 2번 — produce 문장 개수와 일치). (3) 콘솔 에러 없음. 음성
  품질 자체(③)는 실제 사람 귀로 들어야 판단 가능해 헤드리스로는 "voice 선택 로직이
  던지지 않고 동작하는지"까지만 확인했다 — 실제 자연스러움은 배포 후 실기기 확인이
  필요하다.
- **배포 후 확인 필요**: 실기기(특히 안드로이드 Chrome)에서 듣기 버튼을 눌러 음성이
  실제로 예전보다 자연스러워졌는지 확인. 기기에 따라 네트워크 음성이 아예 없을 수도
  있어(오프라인이거나 OS에 TTS 엔진이 로컬만 설치된 경우) 그때는 개선 체감이 없을 수
  있다 — 그 경우도 에러 없이 기존 동작(로컬 음성)으로 조용히 폴백된다.

### D-85. 문장 자연스러움 전수 검토 + produce 예시 답안 보강
- **배경**: 사용자가 외부 첨삭 도구 캡처를 공유 — "I shouldn't spend too much time on games."
  (좋아하는 것 챕터, 심화)가 문법적으로는 맞지만 원어민이라면 "playing games"처럼
  동명사를 붙여 쓴다는 지적. "이런 문장이 더 있으면 고치고, produce 문장엔 유사·대안
  표현도 같이 적어주면 도움이 되지 않겠냐"고 물어서, repeat 문장은 오류 교정만(채점이
  문장 하나 기준이라 대안 표현을 새 필드로 못 넣는다 — D-84 근처에서 이미 정리한
  제약), produce 문장은 이미 있는 `sample`(예시 답안) 배열을 보강하는 쪽으로 방향을
  잡고("그렇게 하자") 진행했다.
- **문장 교정**: `elementary-conversation/data.js` 300문장 + `basic-conversation/data.js`
  362문장을 전부 읽고 훑었다. 실제로 고친 건 3곳뿐(둘 다 elementary-conversation —
  basic-conversation은 애초에 어린이 방송 대사를 참고해 지은 문장이라 이미 구어체에
  가까웠다, D-65):
  1. `shouldnt-spend-too-much-time-on-games` → `shouldnt-spend-too-much-time-playing-games`
     로 id까지 같이 바꿈. "on games"를 "playing games"로.
  2. `why-do-you-like-school`(학교 가는 날 챕터, 심화): "Why do you like or not like
     going to school?" → "Why do you like or dislike going to school?" (hint도
     "I like/don't like" → "I like/dislike"로 맞춤). "or not like"보다 "or dislike"가
     원어민이 훨씬 자주 쓰는 짝이다.
  3. `why-do-you-like-pe`(체육 시간 챕터, 심화)도 같은 패턴 — "Why do you like or not
     like PE?" → "Why do you like or dislike PE?" (hint 동일하게 수정).
  전수 검토라 아이디도 문장도 안 바뀐 나머지 297개는 원어민 구어체 기준으로도 자연스럽다고
  판단해 그대로 뒀다.
- **produce 예시 답안 보강**: `type: "produce"`인 문장 60개(15챕터 × 챕터당 4개 —
  중급 2 + 심화 2) 전부 `sample` 배열에 세 번째 답안을 하나씩 추가했다. 기존 2개는
  대체로 "A and B" 식 완결문 두 개였는데, 세 번째는 실제 대화체에서 더 자주 쓰는 짧은
  형태("Just my backpack, really.", "Probably PE, because I love moving around.",
  "Honestly, it depends on my mood." 등 — Just/Probably/Honestly로 시작하는 축약형·
  헤지 표현)로 골라, 완결문만 보여주는 것보다 실제 대화 감각에 더 가깝게 했다.
  `test/learning.elementary.test.js`의 produce 검사가 "sample 최소 1개"만 요구해서
  기존 테스트 수정 없이 통과한다.
- **검증**: `npm test` 147/147. `node -e`로 produce 문장 60개 전부 `sample.length ===
  3` 확인, 챕터 내 id 중복 0건(재확인). 헤드리스 브라우저로 교정된 문장이 실제 카드에
  렌더되는지, 콘솔 에러 없는지 확인.

### D-86. produce 카드 마이크 버튼 레이아웃 깨짐 수정
- **배경**: 사용자가 실기기 스크린샷 공유 — produce 카드의 "🎤 내 생각 말하기" 버튼이
  아래 "✓ 다음 문장"/"◀ 이전 문장" 버튼과 겹쳐 보였다. `.btn-mic`가 84px 고정 원형
  (`width`/`height`/`border-radius: 50%`)이라 이모지 하나(`🎤`, basic-conversation과
  repeat 카드가 쓰는 형태)만 가정하고 있었는데, produce 카드만 긴 텍스트("🎤 내 생각
  말하기")를 넣으면서 원 밖으로 줄바꿈되어 아래 요소를 덮었다.
- **수정**: `.btn-mic`(원형, 아이콘 전용)는 그대로 두고, `.btn-mic-labeled` 변형
  클래스를 추가해 텍스트가 있는 경우만 `width: auto`·`border-radius: 999px`(알약
  모양)로 늘어나게 했다. 배경·그림자는 `.btn-mic`에서 그대로 물려받고 크기·모양만
  덮어쓴다. `showProduceCard()`의 마이크 버튼에만 `btn-mic btn-mic-labeled` 두 클래스를
  같이 붙였다 — repeat 카드(`🎤`만)와 basic-conversation은 그대로 원형 유지.
- **검증**: `npm test` 147/147(로직 무변경). 헤드리스 브라우저를 모바일 뷰포트(390×844)로
  띄워 produce 카드 진입 후 마이크 버튼과 "다음 문장" 버튼의 바운딩 박스가 겹치지
  않는지 확인, 스크린샷으로도 육안 확인.

### D-87. 각 학년 6번째 챕터 + 다중 턴 대화 도구(`dialogue`) 신설
- **배경**: 학년 3단계가 다 찬 뒤 남겨둔 두 갈래 — (1) 각 학년에 챕터 더 늘리기,
  (2) §3-7이 오래 미뤄둔 진짜 다중 턴 대화형 도구 — 를 사용자가 **둘 다** 진행하기로
  확정("1,2부터 진행").
- **(1) 각 학년 6번째 챕터**: 저학년 `급식 시간`(🍚)·중학년 `건강과 습관`(💪)·고학년
  `미디어와 기술`(📱)을 같은 틀(기본 10·중급 6·심화 4 = 20문장)로 추가 —
  3개 학년·18챕터·360문장이 됐다. 앞선 6번째 챕터들(좋아하는 것·우리 동네·여행과 경험)과
  달리 셋 다 **새 문법을 하나씩 도입**했다: G18(want to), G19(빈도부사
  always/usually/sometimes/never), G20(수동태 be + 과거분사). 순수 재사용으로 두지 않은
  이유는 세 소재의 핵심 표현이 기존 문법 목록에 아예 없었기 때문이다 — "~하고 싶다"는
  저학년이 제일 많이 쓰는데 G1~G6에 없었고, "얼마나 자주"는 습관 얘기의 핵심인데
  G7~G11·G16에 없었고, "~로 만들어졌다"는 기술·미디어 소재에서 자연스럽게 나오는데
  G12~G15·G17에 없었다. 번호는 D-83 규칙대로 학년과 무관하게 전체 최대값 다음으로 이어 붙임.
- **(2) `js/learning/dialogue/` 신설**: §3-7이 "기존 도구를 고치기보다 새 도구로 추가하는
  쪽이 맞아 보인다"고 미리 정해둔 그대로 따랐다 — basic/elementary는 "정해진 문장 하나를
  얼마나 똑같이 따라 했는가"(Levenshtein)를 재는 구조라, 주고받는 대화를 끼워 넣으면 두
  모델이 한 폴더에 섞인다. 모델:
  - 장면(`SCENES`) 하나 = 대화 하나. **학년·단계 층을 두지 않았다** — 대화는 상황 자체가
    난이도라, 문장 단위 도구처럼 3단계로 쪼개면 같은 대화를 세 번 써야 한다. 그래서
    elementary(학년 → 챕터 → 단계 → 문장)보다 두 단 얕다(장면 → 턴).
  - `turns` 배열에 `role: "partner"`(상대 대사 — 자동 재생 + "내 차례로 →")와
    `role: "you"`(내 차례 — 영어 정답 문장을 안 주고 한국어 지시문 + `hint`만 주고,
    STT 후 `sample`과 비교해 자가평가)가 번갈아 온다.
  - **유사도 채점 없음**: elementary의 produce 문장과 같은 이유(정답이 여럿이라 잴 대상이
    없다). 자가평가만 `weak`에 반영 — 마이페이지 집계(D-79)는 개별 도구를 import하지 않고
    `state.learning` 모양만 보므로 이 도구도 코드 수정 없이 자동으로 잡힌다.
  - **지나온 턴을 화면 위에 로그(`.dialogue-log`)로 쌓는다**: 대화는 앞 맥락이 있어야 내
    차례를 채울 수 있어서, 카드 하나만 띄우는 문장 단위 도구와 달리 이전 대사를 남긴다.
    내가 말한 것(STT 결과)도 `turn.said`에 담아 그 자리에 넣는다.
  - 장면 4개(친구와 약속 잡기·처음 만난 친구와 인사·가게에서 물건 사기·식당에서 주문하기),
    각 8턴(상대 4 + 내 차례 4).
- **분기(branching)를 일부러 안 넣었다**: 자유 발화를 알아듣고 갈래를 고르려면 STT 결과를
  의미로 해석해야 하는데, 브라우저 내장 STT 인식률로는 "조금 다르게 말했더니 대화가 산으로
  가는" 실패가 훨씬 잦다. 대신 한 줄기로 흐르되 내 차례에서 뭘 말하든 상대 대사가 자연스럽게
  이어지도록 썼다 — 분기형이라기보다 역할극(role-play)에 가깝다. 진짜 분기가 필요해지면
  그땐 STT가 아니라 선택지(고르기) 기반으로 다시 볼 것.
- **재사용한 것**: `speech.js`(speak/listen)·`.cover`/`.test-card`/`.retry-btn`/`.btn-mic
  .btn-mic-labeled`(D-86)·`learning` 테마·`saveLearningProgress()`를 그대로 썼다. 새로 추가한
  CSS는 `.dialogue-log`/`.dialogue-line`/`.cover.cover-you` 셋뿐이다.
- **검증**: `npm test` 147/147(`test/modules.test.js`의 화면 목록에 `dialogueScreens` 추가 —
  화면 id/경로 중복 검사가 새 도구까지 함께 본다). 모바일 뷰포트(390×844) 헤드리스 브라우저에
  가짜 `SpeechRecognition`을 주입해: 학습 목록에 카드 노출 → 장면 4개 → 1턴 상대 대사(이전
  버튼 없음) → 내 차례(로그·힌트 노출) → STT → 예시 답안 → 자가평가 → 다음 턴 로그에 내 발화가
  남는지 → 8턴 완주 → 완료 화면까지 전 흐름 확인, 콘솔 에러 없음. `node -e`로 elementary
  3학년×6챕터 전부 기본 10·중급 6·심화 4(총 360문장), 챕터 내 id 중복 0건, produce 문장 전부
  hint+sample 3개 확인.

### D-88. `docs/architecture.md` 분리 — "계약·절차"만 남기고 인벤토리 3개를 뗌
- **배경**: `CURRENT_TASK.md`의 "다음 작업 우선순위"에 **세 번 연속** 같은 메모가 쌓여 있었다
  (17.6KB → 23KB → 30.7KB, "다음엔 정말 미루지 말 것"). 자동 분리 기준(15KB)을 두 배 넘겼는데도
  미뤄진 이유는 기존 분리 규칙("테스트별 상세는 `docs/<test-id>-architecture.md`로 뗀다")이 이미
  다 적용된 상태였기 때문 — adhd/disc/couple/numpath/learning 전부 각자 문서가 있어서, 남은 건
  전부 **공통** 내용이라 그 규칙을 적용할 자리가 없었다.
- **새 분리 축 — 수명(lifetime)으로 가른다**: "테스트별로 뗀다"가 막힌 자리에서, 대신 **콘텐츠가
  늘 때 같이 커지는가**로 갈랐다.
  - **인벤토리**(무엇이 있는지 나열 — 콘텐츠를 추가할 때마다 행/줄이 늘어난다) → 별도 파일:
    `docs/module-map.md`(모듈맵, 9.0KB) · `docs/screen-map.md`(화면 표, 9.7KB) ·
    `docs/og-shells.md`(OG 셸, 4.1KB).
  - **계약과 절차**(어떻게 동작하고 어떻게 추가하는가 — 콘텐츠가 늘어도 안 커진다) → `architecture.md`에
    잔류: 라우터 계약 · 상태 모양 · 추가 절차 · 채점 파이프라인 · 공유 · 테스트 스위트(12.0KB).
  이 축이 중요한 건, 그동안 architecture.md가 계속 커진 원인이 정확히 인벤토리 쪽이었기 때문이다 —
  분리해두면 architecture.md는 이제 콘텐츠를 아무리 추가해도 잘 안 커진다(같은 메모가 네 번째로
  반복될 일이 줄어든다). 네 파일 전부 15KB 미만이 됐다.
- **같이 정리한 것**: 화면 표가 실제와 어긋나 있던 것 두 가지를 이번에 맞췄다 — (1) D-87에서 추가한
  `learning-dialogue` 화면이 표에 아예 없었고, (2) `learning-elementary`의 guard(학년 1개일 때
  건너뜀, D-79)가 비어 있었다. 그리고 표 위에 **"자동 생성 화면은 대표 1개만 싣는다"**는 규칙을
  명시했다 — 원래도 그렇게 쓰고 있었는데 규칙이 안 적혀 있어서, 콘텐츠를 추가하는 세션마다
  "표에 전부 넣어야 하나?"를 매번 다시 판단해야 했다(그래서 실제로 dialogue가 누락됐다).
- **참조 갱신**: `CLAUDE.md` 상세 문서 표(3행 추가)·동기화 매트릭스("구조 변경 →
  `docs/module-map.md`, 새 화면이면 `docs/screen-map.md`도")·`docs/adhd-architecture.md`·
  `docs/disc-architecture.md`·`docs/learning-architecture.md`의 헤더 안내 문장.
- **검증**: `npm test` 147/147(문서 변경이라 로직 영향 없음 — 다만 `test/og-shells.test.js`가
  `og-shells/` 폴더를 보는 것이지 새 `docs/og-shells.md`를 보는 게 아니라는 걸 확인했다.
  이름이 비슷해 헷갈릴 여지가 있어 새 문서 헤더에 그 사실을 적어뒀다). 네 파일 크기 전부 15KB 미만 확인.

### D-89. 출력 규칙을 `CLAUDE.md` 최상단으로 이동 — 내용이 아니라 위치가 문제였다
- **배경**: "도구 호출 사이에 과정을 나열하지 말라"는 지적이 **세 번 연속** 반복됐고
  (2026-08-01 최초 → 08-06에 두 번 더), 마지막엔 사용자가 "MD 설정 제대로 안돼있나?"라고
  물었다. 그 사이 규칙 문구는 이미 두 번 강화한 상태였다(모호한 "필요한 전환만" 제거 →
  이분법 규칙 + 금지 패턴 예시). 그런데도 또 위반했으니 **문구 문제가 아니었다.**
- **진단**: 규칙이 `## Claude Code 세션 (.claude/)` 절 **안**에 묻혀 있었다 — 그 절은
  플러그인 always-on 토큰 비용과 `MAX_THINKING_TOKENS` 설정을 다루는 곳이라, 세션 환경
  설정 얘기로 읽히지 "매 응답마다 지켜야 하는 출력 규칙"으로 안 읽힌다. 파일 안에 있긴
  했지만 실행 시점에 걸리는 자리가 아니었다.
- **훅으로 강제할 수 없다**: Claude Code 훅(PreToolUse/PostToolUse/SessionStart/Stop 등)은
  **도구 호출과 세션 경계**만 가로챈다 — 모델이 생성하는 산문 자체를 막거나 필터링하는
  훅은 없다. `.claude/settings.json`으로도 못 막는다. 즉 이 규칙은 **위치와 반복 노출로만**
  강제할 수 있다.
- **조치**: (1) 규칙 블록을 파일 **맨 위**(제목 바로 아래, READ ORDER보다도 앞)로 옮기고
  `## 🔇 출력 규칙 (모든 응답에 항상 적용 — 이 파일에서 제일 먼저 읽는다)`로 제목을 달았다.
  (2) 왜 맨 위에 있는지(= 세 번 위반해서 옮겼다)를 그 자리에 같이 적어, 다음 세션이 이걸
  "정리하다가" 다시 아래로 내리지 않게 했다. (3) `## ⚠️ ABSOLUTE RULES`의 **첫 줄**에도
  한 줄짜리 포인터를 넣어 두 곳에서 걸리게 했다.
- **왜 두 곳에 나눠 적었나**: 보통 이 저장소는 같은 내용을 두 곳에 풀어쓰는 걸 피한다
  (`CLAUDE.md`는 규칙, `DECISIONS.md`는 이유). 여기선 예외로 뒀다 — 본문은 맨 위 한 곳뿐이고
  ABSOLUTE RULES에 있는 건 한 줄 포인터라 중복 서술이 아니며, 세 번 반복된 실패라 걸리는
  지점을 하나 더 두는 비용이 더 싸다고 판단했다.

### D-90. 학습 도구 신뢰성 3건 — TTS/STT 충돌 · 재시도 버튼 · weak 유실
- **배경**: 사용자가 정리해온 우선순위 표의 1순위(각 1~5줄, A-3/A-4/A-5). 셋 다 미착수
  상태였고, 코드에서 원인 지점을 먼저 확인한 뒤 고쳤다.
- **A-3. TTS/STT 충돌**: `speech.js`의 `listen()`이 인식을 시작하기 전에 재생 중인 음성을
  안 끊고 있었다 — 스피커로 나가는 **앱 자기 목소리를 마이크가 주워 담아** 인식 결과가
  엉킨다. 원래도 있던 결함인데, D-84(produce 카드 자동 재생)와 D-87(대화 연습의 상대 대사
  자동 재생)로 "다 듣기 전에 마이크를 누르는" 경로가 흔해지면서 실제로 부딪히기 쉬워졌다.
  `listen()` 첫 줄에 `window.speechSynthesis?.cancel()` 한 줄 — 세 도구의 모든 호출부가 이
  함수를 지나므로 여기서 한 번에 끝난다(호출부마다 넣지 않았다).
- **A-4. 재시도 버튼**: 발음 점수가 낮게 나와도 결과 화면에 "다음 문장"밖에 없어서, 방금
  그 문장을 **그 자리에서 다시 말할 길이 없었다**(카드를 떠났다 돌아오거나 챕터를 다시 도는
  수밖에). 결과 블록에 "🎤 이 문장 다시 말하기"(기존 `.retry-btn` 재사용, 새 CSS 없음)를
  추가하고 같은 인덱스로 `showCard()`를 다시 부르게 했다 — 마이크·건너뛰기가 처음 상태로
  돌아온다. 점수를 매기는 repeat 카드에만 넣었다(produce·대화 연습은 이미 자가평가 두
  버튼으로 "다시 연습할래요"가 있다).
- **A-5. weak 유실 (둘 다 조용히 깨지는 종류)**:
  1. `cloud.js`의 `mergeProgress()`가 원격 진도가 앞서면 **엔트리를 통째로 갈아끼웠다** —
     진도가 조금 뒤진 기기에서 표시해둔 복습 목록(`weak`)이 그대로 사라진다. 진도는 더 나간
     쪽을 남기되 `weak`는 **합집합**으로 따로 남기도록 고쳤다.
  2. `weak` 필드가 생기기 전에 저장된 레코드가 클라우드에서 돌아오면 `weak`가 없는데,
     화면 쪽 `state.learning[key] ??= { index: 0, weak: {} }`는 **엔트리가 이미 있으면 통째로
     건너뛰므로** `weak`를 못 채운다 → `st.weak[id] = true`에서 TypeError. 두 화면 파일에
     `st.weak ??= {}` 한 줄씩, 그리고 `mergeProgress`가 항상 `weak`를 객체로 채우도록 해서
     양쪽에서 막았다.
- **검증**: `npm test` **152/152**(신규 `test/learning.cloud.test.js` 5개 추가 — `mergeProgress`만
  순수 함수라 node로 직접 검증 가능해서 `export`했다). **버그를 일부러 되살려 빨간불을 확인**했다
  (옛 병합 로직으로 되돌리니 2건 실패 → 복구 후 152/152) — `.claude/rules/js-modules.md`가
  요구하는 절차. 헤드리스 브라우저(390×844)로 A-3(마이크 클릭 시 `speechSynthesis.cancel()`
  호출됨)·A-4(재시도 버튼 노출, 클릭 후 같은 문장 유지·마이크 복귀) 확인, 콘솔 에러 없음.
- **남은 우선순위**(사용자 표 기준): 2순위 A-1(정규화 + 단어 단위 채점·오답 단어 표시,
  `score.js` 한 파일) → 3순위 B-3(간격 반복: `due` 필드 + "오늘 복습" 진입로) → 4순위
  B-1/B-2(영어 문장 가리기 / 뜻 접기) → 5순위 B-4/B-5(문법 형태 자동 확인 / kidLabel) →
  6순위 A-2(iOS 녹음 폴백)·C-2(듣기 이해 문항).

### D-91. 발음 채점을 글자 단위 → 단어 단위로 + 정규화 확장 + 오답 단어 표시
- **배경**: 사용자 우선순위 표의 2순위(A-1, "정규화 + 단어 단위 채점·오답 단어 표시,
  `score.js` 한 파일"). 기존 채점은 정답 문장과 인식 결과의 **글자 단위** Levenshtein
  하나뿐이었다.
- **문제 셋**:
  1. **맞게 말했는데 틀렸다고 나오는 경우가 있었다.** STT는 같은 말을 축약형으로도 풀어서도
     돌려주고("I'm" ↔ "I am"), 숫자는 대개 아라비아 숫자로 준다("at 9")는데 문장 데이터는
     영어 단어로 적혀 있다("at nine"). 정규화가 `[^a-z0-9\s]` 제거뿐이라 이 둘을 못 맞췄다.
  2. **어느 단어를 틀렸는지 알려주지 못했다.** 점수만 나오니 뭘 고쳐야 할지 모른 채 다음으로
     넘어간다.
  3. **길이 편향.** 긴 문장은 단어를 통째로 틀려도 글자 비율로는 몇 %만 깎이고, 짧은 문장은
     글자 한두 개만 어긋나도 급락했다.
- **바꾼 것**:
  - `normalizeToken()`이 축약형 사전(CONTRACTIONS, 40여 개)과 숫자 사전(NUMBER_WORDS, 0~20 +
    십 단위)을 적용한다. **무엇으로 펴는지는 중요하지 않다** — 양쪽에 똑같이 적용되므로
    일관성만 있으면 된다(그래서 "let's → let us"처럼 실제론 잘 안 쓰는 형태여도 무방).
  - 채점 단위를 **단어**로 올렸다. 단어 배열끼리 Levenshtein DP로 정렬하고, 거리/최대길이로
    점수를 낸다 — 기획서 4-4의 계산식 형태는 그대로고 분모만 글자 수에서 단어 수로 바뀌었다.
    삽입도 거리에 잡히므로 "정답 다 말하고 딴말 덧붙이기"는 여전히 깎인다.
  - **단어끼리 비교할 때는 글자 단위 유사도를 쓰되 완전 일치를 요구하지 않는다**(WORD_MATCH
    0.8) — "hello"를 "helo"로 인식한 것까지 오답 처리하면 점수가 실력이 아니라 인식기 성능을
    재게 된다.
  - `scoreSpeech(heard, answer)` 신설 — 점수·등급·**정답 문장의 단어별 정오**를 한 번에 준다.
    DP 역추적으로 "정답 쪽 각 단어가 매칭됐는지"를 뽑는다. tokens는 **원문 토큰 그대로**라
    (대소문자·문장부호 포함) 화면이 그대로 그리면 된다 — 축약형이 두 단어로 갈라지는 문제는
    "정규화 단어 → 원문 토큰" 역참조를 들고 다녀 해결했고, 토큰은 자기 단어가 **전부** 맞아야
    맞은 것으로 본다.
  - **틀린 단어가 있으면 등급을 한 단계 내린다**(perfect → good). 틀린 단어를 빨갛게 짚어주면서
    "완벽해요!"라고 하면 화면이 스스로 모순되기 때문이다. 점수 경계(85/60)는 기획서 그대로 뒀다.
  - 화면: 결과에 `.learning-words` 한 줄 추가 — 정답 문장을 단어별로 다시 보여주고 **틀린
    단어만** 강조한다(맞은 단어까지 색칠하면 오히려 안 읽힌다). 두 도구(basic·elementary)의
    repeat 카드에 적용. 대화 연습·produce 문장은 유사도 채점 자체를 안 하므로 해당 없음.
- **검증**: `npm test` **160/160**(`test/learning.score.test.js`에 8개 추가 — 축약형·숫자
  정규화, 길이 왜곡 감소, 사소한 오인식 관용, 단어별 정오, 등급 하향). **버그를 일부러
  되살려 빨간불 확인**: 정규화를 옛 상태로 되돌리니 2건 실패, 등급 하향 규칙만 빼니 1건 실패,
  복구 후 160/160. 헤드리스(390×844)로 실제 카드에서 축약형("It's" ↔ "it is")이 맞음 처리되고
  틀린 단어만 빨갛게 뜨는 것, 숫자 문장("at nine")을 "9"로 말해도 완벽으로 나오는 것 확인,
  콘솔 에러 없음. `scripts/verify.cjs` 전체 통과.

### D-92. 간격 반복(SRS) 신설 — `due` 필드 + 도구를 가로지르는 "오늘 복습"
- **배경**: 사용자 우선순위 표의 3순위(B-3). §3-3이 "나중에 붙일 때"로 남겨둔 항목이고,
  그때 적어둔 전제(불리언 `weak`로는 부족하고 문장별 시각/간격 필드가 필요하다 + 세션이
  끝나도 남아야 의미가 있다)를 그대로 따랐다 — 영속 저장은 D-68에서 이미 붙어 있었다.
- **저장 모양을 새로 만들지 않았다 — 값만 바꿨다**: `weak`의 **키는 그대로 두고 값만**
  `true` → `{ due, step }`으로 바꿨다. 그래서 기존에 `weak`를 읽던 코드가 **한 줄도 안 바뀐다**
  (마이페이지의 키 개수 집계, "헷갈렸던 문장만 복습하기"의 `weak[s.id]` 진리값 검사 — 객체는
  truthy이고 키 개수도 같다). 새 필드를 옆에 추가하는 대신 이 방법을 고른 이유는, 두 벌을
  두면 "어느 쪽이 진실인가"를 계속 맞춰줘야 하고 클라우드 병합(D-90 A-5)도 두 배로 복잡해져서다.
  옛 불리언이 클라우드에서 돌아오는 경우는 `normalizeEntry`가 흡수한다.
- **스케줄(`js/learning/srs.js`)**: Leitner box식 고정 간격 `[0,1,3,7,16]`일. 틀리면 **항상
  처음으로**(한 번 틀린 걸 "조금만" 미루는 건 이 나이대에 의미가 약하고 규칙이 단순해야
  예측 가능하다), 맞히면 한 칸 올라가고 마지막 칸을 넘기면 **지운다(졸업)** — 안 지우면
  복습 목록이 끝없이 길어진다. SM-2 같은 가변 계수는 **일부러 안 썼다**: 입력이 정답률뿐이고
  (문항당 응답 이력을 안 쌓는다), 어린이 대상이라 "며칠 뒤"가 정밀할수록 좋은 학습이 아니다.
  DOM·state를 모르는 순수 함수라 `node --test`로 직접 검증된다.
- **"오늘 복습"을 도구별이 아니라 하나로 모은 이유**: 복습은 "어느 챕터를 하고 있었는지"와
  무관하게 **오늘 해야 할 것**이 단위다. 도구마다 따로 두면 세 군데를 각각 들어가 봐야 오늘
  할 일을 알 수 있고, 그러면 D-79에서 지적된 "복습 진입로가 그 챕터 완료 화면에만 있어서
  놓치면 사라진다"가 형태만 바꿔 되풀이된다. `/learning/review` 화면 하나로 모았다.
- **그런데도 도구를 import하지 않는다**: 복습 화면이 세 도구를 직접 import하면 "홈·마이페이지는
  학습 도구를 import하지 않는다"(D-70)는 경계가 무너진다. 대신 도구 디스크립터에 선택 필드
  **`resolveReview(key, id)`**를 추가하고, 복습 화면은 `listLearning()`으로 훑어 각자에게
  "이 key/id가 네 것이냐, 문장이 뭐냐"를 묻는다 — 레지스트리 방식(D-13·D-63)의 연장이다.
  **새 학습 도구를 추가할 때 복습 화면은 안 고친다**(디스크립터에 `resolveReview`만 넣으면 합류).
  아무도 못 알아보는 key/id는 조용히 빠진다(콘텐츠에서 사라진 옛 문장).
- **`registerLearning`은 하지 않았다**: 학습 목록에 카드로 뜨면 "네 번째 도구"처럼 보이는데,
  실제로는 세 도구의 결과를 모으는 화면이다. `registerScreens`만 한다.
- **진입로 두 곳**: 학습 목록 맨 위 카드(**오늘 볼 게 있을 때만** 뜬다) + 마이페이지의
  "🔁 오늘 복습 시작하기". 마이페이지는 대기 총량과 오늘 볼 수를 나눠 보여준다("복습 목록에
  N개 — 그중 M개는 오늘 볼 차례").
- **한계(그대로 남김)**: 익명 사용자는 `state`가 메모리 전용이라 새로고침하면 복습 목록도
  날아간다 — 이 앱의 다른 진행률과 같은 성질이라 여기서만 예외를 만들지 않았다. 로그인하면
  `learning_progress`(D-68)에 그대로 실린다.
- **검증**: `npm test` **168/168**(`test/learning.srs.test.js` 8개 추가). **버그를 일부러 되살려
  빨간불 확인** — 졸업 처리를 빼니 1건 실패. 옛 형식 방어를 뺐을 땐 처음에 **안 잡혀서**
  검사를 고쳤다: JS가 불리언에 프로퍼티 접근을 허용해 `true.step`이 우연히 통과하고, 실제로
  터지는 건 `null`/`undefined`였다 — 검사를 그쪽으로 바꾸니 잡힌다(초록불만 보고 넘어갔으면
  빈 검사를 그대로 뒀을 사례). 헤드리스(390×844)로 전 흐름 확인: 오답 → 학습 목록에 진입로
  등장 → 복습 화면(출처 표시) → 정답 → 완료 → **간격이 밀려 오늘 목록에서 빠짐**, 그리고
  대화 연습의 produce 턴이 복습에 질문 카드로 뜨고 자가평가로 일정이 반영되는 것까지.
  콘솔 에러 없음. `scripts/verify.cjs` 전체 통과.

### D-93. 연습 카드에 "받침대 치우기" 토글 — 영어 문장 가리기 · 뜻 접기
- **배경**: 사용자 우선순위 표의 4순위(B-1 영어 문장 가리기 / B-2 뜻 접기). 지금 연습 카드는
  영어 문장과 한국어 뜻을 **둘 다 항상** 띄워놓고 있어서, "듣고 따라 말하기"가 실제로는
  **읽고 따라 읽기**가 된다 — 화면을 보고 읽으면 문장을 기억해서 만들어내는 연습이 아니라
  발음 흉내만 남는다. 한국어 뜻도 항상 보이면 영어를 먼저 이해해볼 기회가 사라진다.
- **강제로 가리지 않고 학습자가 켜고 끈다**: 이 도구의 다른 결정과 같은 판단이다(D-61 —
  듣기/말하기를 강제하지 않고 건너뛰기를 둔다). 처음 보는 문장은 보고 따라 읽는 게 맞고
  익숙해지면 가리는 게 맞는데, **그 시점은 학습자만 안다.** 기본값은 둘 다 꺼짐.
- **막히지 않게 하는 장치**: 가려진 영어는 **그 카드에서만** 눌러서 확인할 수 있고(설정은
  안 꺼진다), 접힌 뜻도 "뜻 보기"로 그 카드에서만 펼친다. 설정을 껐다 켰다 하지 않고도
  "정말 모르겠을 때 한 번 확인"이 되어야 학습이 멈추지 않는다.
- **어디에 붙였나**: repeat(따라 말하기) 카드 — basic-conversation · elementary-conversation ·
  복습 화면(D-92) 셋. **produce 카드와 대화 연습에는 안 붙였다** — 거기서는 그 텍스트가
  정답이 아니라 **질문/지시문**이라, 가리면 문제 자체가 성립하지 않는다.
- **중복을 만들지 않았다**: 마크업·바인딩을 `js/learning/prefs.js` 한 곳에 두고 세 화면이
  가져다 쓴다. 세 군데에 각각 쓰면 문구와 동작이 조금씩 갈라진다(D-78에서 score.js·speech.js를
  도구 폴더 밖으로 뺀 것과 같은 이유).
- **저장 위치**: 세션 한정 메모리. `state.learning`에 **넣지 않았다** — 그 객체는 클라우드로
  동기화되는 진행률이라(D-68), 화면 표시 설정이 섞이면 병합 규칙(D-90)이 다뤄야 할 것이 는다.
  localStorage에도 안 넣었다(이 저장소는 영속 데이터를 최소로 유지한다). 새로고침하면 꺼진
  상태로 돌아가는데, 이 앱의 다른 상태와 같은 성질이라 여기서만 예외를 두지 않았다.
- **작업 중 잡은 버그**: `bindReveal`에서 `innerHTML`을 갈아끼운 **뒤에** `koBtn.parentElement`를
  읽고 있었다 — 그 시점엔 버튼이 이미 DOM에서 빠져서 `parentElement`가 `null`이다. 부모를
  미리 잡아두는 것으로 수정(헤드리스 콘솔 에러로 잡혔다 — `npm test`는 DOM을 안 보므로
  이 종류는 브라우저 검증에서만 걸린다).
- **검증**: `npm test` 168/168(순수 UI라 신규 유닛 테스트는 없다 — 로직이랄 게 토글 뒤집기뿐).
  헤드리스(390×844)로: 두 토글 동작, 가린 채로도 채점·오답 단어 표시가 정상인지, 눌러서
  확인해도 설정이 유지되는지, 다음 문장·**다른 화면(복습)으로 넘어가도 설정이 유지**되는지,
  produce 카드에는 토글이 **안 뜨는지** 확인. 콘솔 에러 없음. `scripts/verify.cjs` 전체 통과.

### D-94. 문법 항목에 아이용 표현(kidLabel) + produce 문장의 문법 형태 자동 확인
- **배경**: 사용자 우선순위 표의 5순위(B-4 문법 형태 자동 확인 / B-5 kidLabel). 둘 다 같은
  `*_GRAMMAR_POINTS` 배열을 건드리는 일이라 함께 처리했다.
- **B-5 `kidLabel`**: D-79에서 "지금 뭘 배우는지 의식하게" 하려고 카드에 문법 라벨을 띄웠는데,
  그 라벨이 **문법 용어 그대로**였다 — "조동사 have to / must (의무)", "관계대명사 who/that",
  "수동태 (be + 과거분사)". 초등학생은 이 말을 모르니, 띄워봐야 **원래 목적이 오히려 무너진다**
  (알려주려던 게 또 하나의 모르는 말이 된다). 항목마다 아이 말로 다시 적은 `kidLabel`을
  추가하고(예: 수동태 → `"~되었어요"로 말하기`), 카드는 그걸 띄운다. `label`(문법 용어)은
  콘텐츠 저작·문서·테스트가 계속 쓴다 — 지우지 않고 **화면용 표현을 따로 뒀다**.
- **B-4 `check`**: produce 문장은 정답이 여러 개라 유사도 채점을 못 해서(D-78) 지금까지
  **자동 신호가 하나도 없었다**(예시 답안 + 자가평가뿐). 그런데 "목표 문법 형태를 넣어
  말했는가"는 잴 수 있다 — 문법 항목에 정규식 `check`를 달고, STT 결과에 걸리는지 보고
  한 줄로 알려준다(연습 화면과 복습 화면 둘 다).
  - **형태가 뚜렷하지 않은 항목은 일부러 비워뒀다**(G2 일반동사 현재형, G7·G12 현재형 복습).
    억지로 정규식을 만들면 **맞게 말했는데 "안 썼다"고 하는 오탐**이 생기는데, 그건 신호가
    없는 것보다 나쁘다. `check`가 없으면 그 줄이 아예 안 뜬다.
  - 못 넣었을 때도 **오답이 아니라 다음 시도 제안**으로 읽히게 문구·색을 잡았다
    ("💡 이번엔 ~에 도전해볼까요?") — 자가평가가 여전히 일정(SRS)의 유일한 입력이다.
- **문구를 한 번 고쳤다**: 처음엔 `✅ ${kidLabel}를 잘 넣어 말했어요!`였는데 kidLabel이
  "…말하기"로 끝나서 "말하기를 잘 넣어 말했어요"가 됐다. kidLabel 형태에 상관없이 읽히도록
  `✅ 오늘의 문법을 잘 넣어 말했어요! (${kidLabel})` / `💡 이번엔 ${kidLabel}에 도전해볼까요?`로 바꿈.
- **검증**: `npm test` **171/171**(검사 3개 추가). 세 검사가 각각 다른 걸 지킨다: ①모든 항목에
  kidLabel이 있고 **거기에 문법 용어가 안 남아 있다**(용어 목록으로 직접 검사 — 새 항목을
  추가하며 label을 복사해 넣는 실수를 막는다), ②`check`가 그 문법으로 태그된 **실제 문장에
  걸린다**(정규식 오타 방지), ③`check`가 **엉뚱한 문장을 안 잡는다**(오탐 방지 — "I use my
  phone"을 used to로, "I will become"을 조건문으로 잡으면 안 된다). **버그를 일부러 되살려
  빨간불 확인**: kidLabel에 "조동사"를 넣으니 1건, check 정규식에 오타를 내니 2건 실패 →
  복구 후 171/171. 헤드리스로 카드 라벨·형태 O/X 두 경우·check 없는 문법에선 줄이 안 뜨는 것·
  복습 화면에도 붙는 것 확인, 콘솔 에러 없음.

### D-95. iOS 녹음 폴백(A-2) + 듣고 뜻 맞히기(C-2)
- **배경**: 사용자 우선순위 표의 마지막 묶음(6순위, "대" 비용). 둘 다 붙였다.
- **A-2 — iOS Safari에서 말하기가 통째로 죽어 있었다**: iOS Safari는 `SpeechRecognition`을
  지원하지 않는다. `supportsRecognition()`이 false를 돌려주면 화면엔 "이 브라우저는 음성
  인식을 지원하지 않아요" 안내만 떴다 — 대상 사용자(초등학생·보호자 폰)의 아이폰 비중을
  생각하면 **사실상 절반이 말하기 연습을 아예 못 쓰는 상태**였다.
  - **폴백은 녹음-되듣기(shadowing)**: 내 목소리를 녹음해 다시 듣고 스스로 비교한다.
    `MediaRecorder`는 iOS 14.3+에서 지원돼 **의존성이 0**이다. 서버 STT는 검토만 하고
    버렸다 — 백엔드·비용·개인정보처리방침 개정이 전부 딸려오는데(이 저장소의 백엔드는
    부부 체크 KV 하나뿐이다) 얻는 건 점수 하나다. 자가평가는 이미 produce 문장·대화
    연습이 쓰는 방식이라 새로 만들 개념이 없다.
  - **6개 호출부를 각각 안 고쳤다**: `js/learning/record.js` 하나에 마크업+바인딩을 두고,
    각 화면은 기존 `if (!micBtn) return;` 자리에서 `bindRecordFallback`을 부르고 빠진다.
    자가평가 결과를 콜백으로 받아 **STT 경로와 똑같이** weak/SRS에 반영한다.
- **C-2 — 이해했는지는 한 번도 확인하지 않고 있었다**: 지금까지의 카드는 전부 "따라 말하기"라
  산출(production) 연습이고, 가리기 토글(D-93)도 마찬가지다. 문장을 **글자 없이 소리로만**
  들려주고 한국어 뜻 3개 중 고르게 하는 화면을 붙였다.
  - **콘텐츠를 새로 안 만들었다**: 오답 보기는 **같은 챕터의 다른 문장 뜻**을 쓴다. 소재가
    같아 "대충 찍어도 맞는" 보기가 안 나오고, 문장을 추가하면 문제도 저절로 늘어난다.
    produce 문장은 제외한다(text가 질문이라 "뜻 맞히기"가 성립 안 함).
  - 틀린 문장만 weak(SRS)에 넣는다 — 맞힌 걸 일정에 올릴 이유가 없다.
  - **진입로는 단계 완료 화면에만** 뒀다. 목차에 올리면 "챕터마다 두 개"처럼 보이는데,
    같은 문장을 다른 방식으로 묻는 화면이지 새 콘텐츠가 아니다.
  - **elementary-conversation에만 붙였다** — 학년·단계 층이 있어 문제 풀이 단위가 이미
    나뉘어 있다. basic-conversation(7세 이하)은 한글을 아직 못 읽는 대상이라 뜻 고르기가
    안 맞고, 대화 연습은 문장 단위가 아니다. 필요해지면 그때 넓힌다.
- **검증**: `npm test` 171/171(순수 UI·브라우저 API라 신규 유닛 테스트 없음 — `MediaRecorder`와
  `speechSynthesis`는 node에 없다). 헤드리스로 **`SpeechRecognition`을 지우고 iOS Safari를
  재현**해: 마이크 버튼 대신 녹음 버튼이 뜨는지, 녹음→정지→재생기·자가평가 버튼, 자가평가가
  weak에 반영돼 복습 진입로가 생기는지, 대화 연습에도 붙는지 확인. 듣기 화면은 영어가 안
  보이는지·보기 3개가 중복 없이 나오는지·자동 재생·정답 표시·완료 화면·단계 완료 화면의
  진입로까지 확인. 콘솔 에러 없음.
