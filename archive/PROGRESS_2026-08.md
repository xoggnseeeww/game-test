# PROGRESS 2026-08 — 작업 이력 (전체)

> 조건부 로드 문서(`.claudeignore` 대상). **이력 전용** — "지금 유효한 작업"은 `CURRENT_TASK.md`.
> 이 파일은 `CURRENT_TASK.md`의 "현재 작업" 섹션에 쌓여 있던 2026-08 항목 전체를 **원문 그대로**
> 옮긴 것이다(2026-08-19 정리). 옮긴 이유는 `docs/DECISIONS.md` D-105 참고 — 그 섹션이
> 자기 규칙(1~2k 토큰)의 15배를 넘겨 매 세션 자동 로드 비용이 되고 있었다.
>
> **찾는 법**: 대부분의 항목은 `docs/DECISIONS.md`에 D-번호로 같은 내용이 정리돼 있다.
> "왜 그렇게 정했나"를 찾는다면 D-번호 쪽이 짧고 정확하다. 이 파일은 "그날 무엇을 했고
> 무엇을 확인했는가"(검증 로그 포함)가 필요할 때만 본다.

---

2026-08-19(23): **로그아웃해도 로컬 진행률이 안 지워져 다른 계정에 섞이던 버그 수정**
(D-104). D-103 리뷰 중 발견해 "손대지 않은 것"으로 남겨뒀는데, 사용자가 "치명적인 오류인데
앱 전체의 기존 패턴이라도 수정해야겠다"고 판단해 바로 고쳤다. **증상**: 같은 브라우저 탭에서
계정 A로 공부하다 로그아웃 후 계정 B로 로그인하면, `js/learning/cloud.js`·
`js/learning/civil-vocab/cloud.js` 둘 다 로그아웃 시 `syncedForUser`만 초기화하고 **로컬
state는 그대로 둬서**, 로그인 동기화가 "로컬(=방금 나간 A의 값)"과 "B의 서버 값"을 병합해
A의 진행이 B 계정에 그대로 업서트될 수 있었다(공유 PC·가족 공유 기기에서 실제로 벌어질 수
있는 사고). **수정**: 로그아웃 감지 시 `state.learning = {}`(문장 도구) /
`state.vocab.cards = {}`·`days = {}`·`newPerDay = DEFAULT_NEW_PER_DAY`(어휘 도구)로 지운다.
**한 번도 로그인한 적 없는 세션은 지우지 않는다** — "로그인 전 진행을 로그인해서 이어 올리기"
(D-68·D-100)가 깨지지 않게, 로그아웃과 최초-비로그인을 구분한다.
**테스트 가능하게 리팩터링**: 두 모듈의 로그인 이벤트 처리를 `makeSyncHandler(cloud)`로
뽑아 cloud 클라이언트를 인자로 받게 했다 — 실제 CDN 클라이언트 대신 최소 가짜 cloud(로그인
상태를 흉내내는 `_login/_logout` + 체이닝만 하는 가짜 `supabase.from()`)를 넣어
`node --test`에서 로그인→로컬 축적→로그아웃→다른 계정 로그인까지 전체 시나리오를 검증한다
(예전엔 순수 함수 merge만 테스트되고 로그인/로그아웃 흐름 자체는 무테스트였다).
`npm test` **222/222**(5개 추가), **버그를 되살려 빨간불도 확인**. 이 샌드박스는 Supabase
CDN이 막혀 실제 로그인 왕복 헤드리스 검증은 못 했다 — 이 순수 로직 테스트가 지금 낼 수 있는
가장 강한 증거다.
**배포 후 확인 필요(꼭)**: 실기기에서 계정 A 로그인→학습→로그아웃→계정 B 로그인 순으로 해서
마이페이지·어휘 인트로가 B 자신의 진행만 보여주는지.

2026-08-19(22): **오류 검토 후속 — DAY 확인 문제·빈칸이 같은 단어를 중복 채점하던 버그
수정**(D-103). 사용자 요청으로 D-98~D-102 전체를 다시 훑었다. 가장 심각했던 것: DAY 화면의
카드(👍/😅)·확인 문제·빈칸 채우기 **셋 다 gradeWord()를 불러 같은 단어를 세 번 채점**하고
있었다 — 카드에서 reps 0→1(내일)까지는 맞는데, 확인 문제에서 1→2(3일 뒤), 빈칸에서 2→3
(8일 뒤)까지 몇 분 사이에 튀었다. "내일 다시 올라와요"라고 안내해 놓고 실제로는 DAY를 한 번
도는 것만으로 8일 뒤로 밀려나 있었던 것 — D-99·D-101·D-102가 사용자에게 약속한 것과
실제 동작이 어긋나 있었다. **수정**: DAY에서는 카드 자가평가만 SRS를 건드리게 하고, 확인
문제·빈칸의 gradeWord 호출을 제거(정답률·"틀린 단어만 다시 보기"는 그대로 유지). "오늘 학습"
(`/today`)은 원래 항목당 정확히 한 번만 채점되는 구조라 이 버그가 없어서 안 건드렸다.
**덤으로 고친 것**: ①로그인 동기화 시 설정(vocab_settings)·일정(vocab_progress) 응답 속도
차이로 화면 재렌더 알림이 한쪽에만 붙어 있던 경쟁 상태 → `Promise.allSettled`로 묶음
②D-101에서 "오늘 복습"→"오늘 학습"으로 개명했는데 탭 제목은 안 바뀌어 있던 것 ③오늘 큐의
빈칸 모드에 null 방어가 없어 콘텐츠 확장 시 화면이 죽을 수 있던 것(DAY는 이미 방어돼 있었음).
**검증**: 페이지 안에서 `state.js`를 직접 열어 `state.vocab.cards`의 실제 reps/ivl 값을
읽는 헤드리스 테스트를 새로 만들어 확인(문구만 보던 이전 테스트로는 이 버그가 안 잡혔다).
DAY 1을 카드→확인 문제→빈칸까지 다 마쳐도 reps=1·ivl=1(내일)이 유지되는 것 확인. **버그를
되살려 빨간불도 확인**(gradeWord 복구 시 확인 문제 단계에서 이미 값이 흔들림). `npm test`
217/217, 기존 헤드리스 스위트 + `scripts/verify.cjs` 재확인 통과.
**발견했지만 손대지 않은 것**: 같은 브라우저 탭에서 로그아웃 후 다른 계정으로 로그인하면
첫 계정의 로컬 세션 데이터가 초기화 안 되고 새 계정과 병합될 수 있는 경로 — 학습 진행률
(`js/learning/cloud.js`)·NumPath 마을 동기화에도 똑같이 있는 앱 전체의 기존 패턴이라, 이번
범위(어휘 도구)만 따로 고치면 세 모듈이 서로 달라진다. 별도로 논의 필요.

2026-08-19(21): **오늘 학습을 "다 외울 때까지"로 + 하루 목표 설정**(D-102). 사용자 요청 두 건.
①D-99에서 넣었던 **같은 세션 재노출 2회 상한을 제거** — 못 외운 단어를 남긴 채 끝나면 "오늘
학습"이 뭘 끝낸 건지 알 수 없다는 지적이 맞다. 이제 맞힐 때까지 다시 나오고 완료 문구도
"다 외웠어요"다. 대신 원래 걱정하던 실패 모드는 다른 방법으로 막았다: 틀린 항목은 **세 장 뒤**에
끼우고(바로 다시 물으면 인출 연습이 아니다), 카드마다 **"오늘은 여기까지"** 버튼을 뒀다.
②**하루 목표를 학습자가 고른다**(20/30/50/100, 최소 20 — `normalizeNewPerDay`가 하한을 강제하고
테스트가 지킨다). 설정은 `vocab_settings` 테이블(user_id PK)에 저장 — 단어별 행 테이블엔 사용자
단위 값을 넣을 자리가 없다. RLS + GRANT를 처음부터 함께 넣었다(E-16).
`npm test` **217/217**, 헤드리스 13항목 — 특히 **정답을 알고 푸는 종단 테스트**(페이지 안에서
단어 데이터를 동적 import)로 "일부러 틀린 단어가 4번씩 다시 나온 뒤 맞히면 종료"를 확인했다.
**배포 후 확인 필요**: 하루 목표를 바꾼 뒤 로그아웃→로그인 시 그 값이 유지되는지(계정 저장).

2026-08-19(20): **사용자 피드백 2건 반영 — 마이페이지 연동 · "오늘 학습"의 정체**(D-101).
①마이페이지가 `state.learning`만 훑어 어휘 도구가 안 보였다 → 도구 디스크립터에 `summary()`
훅을 두고 `listLearning()`으로 모은다(D-70 경계 유지). 로그인 동기화가 화면보다 늦게 끝나는
문제도 함께 고침(`onLearningSync` 공용 알림 → 마이페이지·도구 인트로 재렌더).
②**하루 신규 상한이 실제로는 안 걸리고 있었다** — 큐를 만들 때마다 상한을 새로 적용해서, 오늘
몫을 끝내고 다시 들어가면 새 단어가 또 20개 나왔다. 그래서 화면이 늘 처음 보는 단어로 채워졌고
"봤던 단어가 안 나온다"는 지적이 나왔다. 엔트리에 `first`(처음 만난 시각)를 넣고
`countNewToday()`로 오늘 시작한 수를 빼서 센다(서버에도 `first_seen` 열 추가).
③화면이 스스로 정체를 설명하게 고침: 이름 "오늘 학습", 상단 구성 줄, 항목 배지(처음/다시/헷갈림),
답한 뒤 다음 일정 안내, 완료 시 내일 예고, 빈 큐 사유 3분기, 인트로에 DAY와의 역할 차이.
`npm test` **214/214**, 헤드리스 13항목 통과.
**배포 후 확인 필요**: 로그인 상태에서 마이페이지에 어휘 진행이 뜨는지, 오늘 학습을 끝낸 뒤
다시 들어갔을 때 "오늘 새 단어 몫을 다 했어요"가 뜨는지(전에는 새 단어가 또 나왔다).

2026-08-19(19): **어휘 일정 계정 저장(M3) + 학습 진행률이 저장 안 되던 버그 발견·수정**(D-100).
`vocab_progress` 테이블 신설(`(user_id, word_id)` PK, RLS 본인 행만, `(user_id, due)` 인덱스) —
`learning_progress`처럼 통짜 JSONB로 안 간 이유는 8000단어에서 ①카드 한 장마다 수백 KB를 올리고
②두 기기가 서로의 진행을 통째로 덮어쓰기 때문이다. 병합은 **마지막 응답이 이긴다**(`srs.js`
엔트리에 `at` 추가, 서버 `updated_at`과 같은 값). 업서트는 10개/4초로 묶고 화면 이탈·탭 숨김에
flush, 로그인 시 서버 행을 페이지 넘겨 전부 받아 병합.
**부수 발견(중요)**: `learning_progress`(D-68)가 **배포 후 한 번도 저장된 적이 없었다** —
`authenticated` 역할에 테이블 GRANT가 없어 RLS 정책에 닿기도 전에 `permission denied`로
막히고 있었다(행 0, 화면엔 아무 표시 없음). 두 테이블 모두 GRANT 적용 → 학습 진행률 동기화도
이제 동작한다. 패턴은 `docs/ERRORS.md` **E-16**.
`/privacy`와 `CLAUDE.md` "영속 데이터" 항목 ④를 같은 커밋에 갱신했고, "서버에 저장하는데 방침에
없는" 상태를 테스트로 막았다. `npm test` **212/212**(5개 추가), 빨간불 확인 후 복구, 헤드리스로
비로그인 경로 재확인.
**배포 후 확인 필요(꼭)**: 실기기에서 **로그인 → 단어 몇 개 → 새로고침 → 진행 유지**.
샌드박스는 Supabase CDN이 막혀 로그인 왕복을 재현할 수 없다. 같은 김에 학습 진행률(문장 도구)도
이제 실제로 저장되는지 함께 확인할 것 — 그동안 안 되고 있었다.

2026-08-19(18): **어휘 학습 방식 개편 — 지연 노출 · 빈칸 채우기 · 간격 반복**(D-99).
효율성 검토에서 지적한 5가지 중 사용자가 고른 순서(①②먼저, 그다음 ③)대로 붙였다.
①**카드 앞면은 단어만** — 눌러야 뜻·어원이 열리고 거기서 자가평가(😅/👍). 토글로 끌 수
있다(D-61·D-93과 같은 판단). ②**빈칸 채우기** — 예문에서 표제어를 지운 산출 문제로,
**콘텐츠 추가 0**(D-98이 넣어둔 "예문에 표제어가 있다" 검사가 전제였다). ③**간격 반복** —
도구 안 `srs.js`(SM-2 변형, 1→3→8→20→50→125일, **졸업/삭제 없음**)와
`/learning/civil-vocab/today` 큐(복습+신규 섞기, 하루 신규 상한 20, 인출 강도 승급
고르기→빈칸). 틀린 항목의 같은 세션 재노출은 **두 번까지만**(전부 오답 시 59개 큐가
182→110 상호작용으로 줄었다). `npm test` **207/207**(11개 추가), 빨간불 확인 후 복구,
헤드리스 27항목 + 기존 `scripts/verify.cjs` 통과.
**다음(M3, 이게 없으면 SRS가 반쪽이다)**: 일정이 세션 메모리라 새로고침하면 날아간다 →
`vocab_progress` 테이블(user_id+word_id PK, RLS, 배치 업서트) + 로그인 병합. 그 커밋에서
`/privacy`와 `CLAUDE.md` "영속 데이터" 항목을 **반드시 함께** 갱신한다.
**배포 후 확인 필요**: 실기기에서 빈칸 입력(모바일 키보드·자동수정 끄기 속성이 먹는지),
카드 앞면 가림 상태가 실제로 읽히는지.

2026-08-19(17): **위 도구 오류 검토 6건 수정**(D-98 후속). 사용자 요청("오류 검토 및 공부의
효율성 검토")으로 코드·데이터를 다시 훑었다. **가장 나빴던 것은 어원 오류 3건** — 학습자에게
틀린 걸 가르치고 있었다: `attain`은 칩이 tain(잡다)인데 힌트는 "닿다"라고 말하고 있었고
(실제 뿌리는 tangere = 닿다, tenere와 다른 계통), `abandon`의 ab-는 라틴 "떨어져"가 아니라
banish와 같은 bann이며, `acknowledge`의 ac-는 라틴 ad-가 아니다(중세영어 a- + knowledge).
`diminish`의 di-는 dis-가 아니라 de-. 동작 오류 2건 — "틀린 단어만 다시 보기" 뒤 회차가
DAY 전체의 최고 정답률을 덮어썼고(3단어 100% → "최고 정답률 100%"), 카드를 다 본 뒤 목록
배지가 "51번째"로 떴다. 예문 1건(incline 자동사 용법) 교체. 검사 2개 추가(힌트가 자기 어근
표기를 담는지 · 어근 사전 필수 필드) → `npm test` **196/196**, 빨간불 확인 후 복구.
**효율성 검토는 별도 보고만 하고 코드는 안 고쳤다** — 무엇을 먼저 붙일지는 사용자 결정이다
(권장 순서: ①어원·뜻 지연 노출로 능동 인출 만들기 ②예문 빈칸 모드(콘텐츠 추가 0 — 예문에
표제어가 있다는 걸 테스트가 보장한다) ③SRS(M2) ④DAY를 10단어 블록으로 쪼개기).

2026-08-19(16): **9급 공무원 영단어 도구(civil-vocab) 신설 — M1**(D-98). 사용자 요청(어휘
8000개 목표 · 연상암기법 · 효율적 반복 · 계정별 진도)에 대해 **계획을 먼저 내고** 세 갈래를
확정받았다: ①복습은 "오늘 복습"과 **분리** ②연상은 **어원 우선** ③**200단어 먼저**.
기존 학습 도구 방식을 그대로 쓰면 8000단어에서 깨지는 세 곳을 M1에서 미리 갈랐다 —
①데이터는 `manifest.js`(메타)만 정적, `words/day-NNN.js`는 **동적 import**(부팅~인트로에선
단어 파일을 한 번도 안 받고 DAY 진입 시 그 DAY만 받는 걸 헤드리스로 확인) ②진행 상태는
`state.learning`이 아니라 **`state.vocab`**(통짜 업서트에 섞으면 카드 한 장마다 수백 KB)
③`srs.js` 재사용 안 함(졸업 시 삭제 규칙이 장기 보존과 반대) — 별도 스케줄러는 M2로,
껍데기는 **미리 안 만들었다**. 콘텐츠는 단어 200개(DAY 4개) + 어원 사전 156항목.
`npm test` **194/194**(`test/learning.vocab.test.js` 14개 신설). **버그 되살려 빨간불 확인**:
정적 import 추가 / 매니페스트 count 조작 / 죽은 어근 / 없는 어근 참조 → 4건 모두 실패,
복구 후 194/194. 헤드리스 30항목 통과(동적 로드 시점 · 카드/문제/결과 흐름 · 로딩 중 이탈
방어), 기존 `scripts/verify.cjs`도 통과.
**다음 단계(사용자 확인 필요)**: M2 SRS 스케줄러 + `/today` 큐 → M3 계정별 저장
(`vocab_progress` 테이블 · 이때 `/privacy`와 `CLAUDE.md` "영속 데이터" 항목 **필수 갱신**) →
M4 통계 → M5 콘텐츠 800→2000→4000→8000.
**배포 후 확인 필요**: 배포본에서 DAY 진입 한 번(동적 import는 다른 js 파일과 같은 정적
자산 경로를 타므로 새 위험은 없지만, 실제로 눌러본 적은 없다 — 경로가 틀리면 `_redirects`의
SPA 폴백 때문에 404가 아니라 index.html이 와서 문법 에러로 터진다). 어휘·예문의 자연스러움 전수 검토는 아직 안 했다(D-85가 문장 도구에 한 것과
같은 작업이 어휘에도 필요하다).

2026-08-06(15): **TTS 발음이 엉망이던 원인 두 가지**(D-97). 사용자 지적 "발음이 완전 이상해
엉망이야" — D-84에서 음성 품질을 손봤는데도 나온 지적이라 그때 고른 규칙 자체를 다시 봤고,
**원인 둘 다 D-84가 만든 것**이었다. ①**지역보다 로컬/네트워크를 먼저 봤다**: `matching`이
`en`으로 시작하는 음성 전부(en-US·en-GB·en-IN·en-AU)인데 `.find(v => !v.localService)`로
골라서, **기기에 en-US 로컬 음성이 있어도 en-IN 네트워크 음성이 뽑혔다.** 미국식을 배우는
아이에겐 "품질이 조금 떨어지는" 게 아니라 **틀린 발음**이다 → 점수를 매겨 지역 일치가 항상
우선하게 하고, 지역이 같은 것끼리만 네트워크를 선호(D-84 의도는 그 안에 살아있음). 음성 이름
하드코딩은 여전히 안 함. ②**음성 목록이 안 채워졌는데 그냥 재생했다**: Chrome은 `getVoices()`를
비동기로 채워 첫 호출에 빈 배열인데, D-84는 "브라우저 기본으로 재생되니 괜찮다"고 판단하고
대기를 안 넣었다 — **그 판단이 틀렸다.** 한국어 엔진이 기본인 기기는 영어를 한국어 발음으로
읽는다. 자동 재생(D-84·D-87)이 늘면서 "첫 재생"이 잦아져 더 자주 걸렸다 → `voiceschanged`를
한 번만 듣고 재시도. `npm test` **179/179**(`test/learning.speech.test.js` 7개 — `pickVoice`를
순수 함수로 떼어 처음으로 자동 검증 대상이 됐다, 원래 소리로만 드러나던 영역). **버그 되살려
빨간불 확인**: 옛 규칙으로 되돌리면 2건, 다른 언어 필터 빼면 2건 실패 → 복구 후 179/179.
헤드리스로 한국 기기 음성 목록(ko-KR·en-IN·en-GB·en-US로컬·en-US네트워크)을 흉내내 목록이
비었을 땐 **아무것도 재생 안 하고 기다렸다가** en-US로 읽는 것 확인.
**배포 후 확인 필요**: 실기기에서 실제로 발음이 정상인지. 기기에 영어 음성이 아예 없으면
코드로 못 고친다(그땐 pickVoice가 null을 주고 브라우저 기본에 맡긴다 — 다른 언어 음성을
억지로 고르지는 않는다).

2026-08-06(14b): **위 작업 점검 후속** — "더 개선할 건 없나?"에 코드를 다시 훑어 두 가지를
고쳤다. ①**복습 화면에는 문법 설명이 안 붙어 있었다** — 그러면 방금 받은 지적이 절반만
해결된다. 설명 블록을 `js/learning/grammar.js`로 빼서 연습·복습이 같은 걸 쓰게 했다(도구를
모르는 순수 view 함수라 D-70 경계는 그대로 — 문법 항목은 `resolveReview`가 넘겨준다).
②듣기 퀴즈에서 1인자 함수를 `markWeak(card.id, true)`로 부르고 있었다(동작은 같지만 같은
파일의 2인자 markWeak와 헷갈린다). 확인했지만 **안 고친 것**: 대화 연습의 `turn.said`가 SCENES
데이터 모듈에 쓰는 것(사용자 눈에 보이는 문제가 없어서 그대로 둠), 듣기 보기의 뜻 중복 위험
(현재 콘텐츠에 중복 0건이라 방어 코드 불필요). `npm test` 172/172, 회귀 스위트 통과.

2026-08-06(14): **문법 라벨에 설명 + "이 문장 어디가 그 문법인지" 표시**(D-96).
사용자 지적: "문항마다 하단에 문법을 적어놨던데 상세한 설명이 없어서 그 문법이 어떻게
적용이 된 건지 알 수가 없음." 맞는 지적 — D-79에서 라벨을 띄우고 D-94에서 아이 말로 바꿨지만
**둘 다 이름만** 알려줬다. ①항목마다 `explain`(형태+뜻을 아이 말로 한 줄) 추가, ②카드의 문법
줄을 펴면 설명과 함께 **문장에서 그 형태를 `<mark>`로 짚어준다** — 짚는 위치는 `check`(D-94)가
이미 아는 것이라 **표시용 데이터를 새로 안 만들었다**. check 없는 문법(G2·G7·G12)은 설명만 뜬다.
`<details>`로 접어둠(여는 JS 불필요, 매번 펼치면 카드가 덮인다). `npm test` **172/172**
(explain 존재 검사 추가 — 새 문법을 설명 없이 넣는 실수를 막는다, 이번 지적이 정확히 그것).
헤드리스로 check 있는 문법(G18 "want to"·G20 "was made")이 문장의 맞는 부분을 짚는지, check
없는 문법은 설명만 뜨는지, produce 카드에도 붙는지, 기본이 접힌 상태인지 확인. 콘솔 에러 없음.

2026-08-06(13): **iOS 녹음 폴백(A-2) + 듣고 뜻 맞히기(C-2)**(D-95). 우선순위 표 마지막 묶음.
**A-2**: iOS Safari는 `SpeechRecognition`을 지원하지 않아 **아이폰에서 말하기 연습이 통째로
죽어 있었다**(안내 문구만 뜸) — 대상 사용자의 아이폰 비중을 보면 사실상 절반이 못 쓰는 상태.
`MediaRecorder`(iOS 14.3+, 의존성 0)로 녹음-되듣기 폴백을 붙였다. 서버 STT는 버림(백엔드·비용·
방침 개정이 딸려오는데 얻는 건 점수 하나). 6개 호출부를 각각 안 고치고 `js/learning/record.js`
하나에 마크업+바인딩을 두고 기존 `if (!micBtn) return;` 자리에서 부르게 함 — 자가평가 결과는
STT 경로와 **똑같이** weak/SRS에 반영. **C-2**: 지금까지 카드는 전부 산출(따라 말하기) 연습이라
**이해했는지는 한 번도 확인 안 했다**(가리기 토글 D-93도 산출 연습). 글자 없이 소리만 듣고
한국어 뜻 3개 중 고르는 화면 추가 — 오답 보기는 **같은 챕터의 다른 문장 뜻**이라 콘텐츠를
새로 안 만들고(문장 늘면 문제도 저절로 늚), 소재가 같아 찍어서 맞기도 어렵다. produce 문장은
제외(text가 질문이라 성립 안 함). 틀린 것만 weak에 넣고, 진입로는 **단계 완료 화면에만**
(목차에 올리면 새 콘텐츠처럼 보이는데 같은 문장을 다르게 묻는 화면일 뿐). elementary만 —
basic은 한글 못 읽는 대상이라 뜻 고르기가 안 맞고 대화 연습은 문장 단위가 아니다.
`npm test` 171/171(순수 UI·브라우저 API라 신규 유닛 테스트 없음 — MediaRecorder/speechSynthesis는
node에 없다). 헤드리스로 **`SpeechRecognition`을 지워 iOS Safari를 재현**해 녹음 버튼·녹음→정지→
재생기·자가평가→복습 반영·대화 연습 연동 확인, 듣기 화면은 영어 안 보임·보기 3개 중복 없음·
자동 재생·정답 표시·완료 화면·진입로 확인. 콘솔 에러 없음.
**남은 우선순위 없음** — 사용자 표 1~6순위 전부 완료(D-90~D-95).
**배포 후 확인 필요**: 실제 아이폰에서 녹음 폴백(마이크 권한 프롬프트 → 녹음 → 재생)이 도는지.
헤드리스는 가짜 오디오 장치라 iOS의 권한 흐름·오디오 포맷(iOS는 mp4/aac로 떨어진다)을 재현 못 한다.

2026-08-06(12): **문법 항목에 아이용 표현(kidLabel) + produce 문장의 문법 형태 자동 확인**
(D-94). 우선순위 표 5순위(B-4/B-5) — 둘 다 같은 `*_GRAMMAR_POINTS` 배열을 건드려서 함께 처리.
**B-5**: D-79에서 "지금 뭘 배우는지 의식하게" 하려고 띄운 문법 라벨이 **문법 용어 그대로**였다
("조동사 have to", "관계대명사 who/that", "수동태") — 초등학생이 모르는 말이라 알려주려던 게
또 하나의 모르는 말이 돼서 **원래 목적이 무너졌다.** 항목마다 `kidLabel`(아이 말)을 추가해
카드는 그걸 띄우고, `label`(문법 용어)은 저작·문서·테스트용으로 남김. **B-4**: produce 문장은
정답이 여럿이라 유사도 채점을 못 해(D-78) 자동 신호가 하나도 없었는데, "목표 형태를 넣어
말했는가"는 잴 수 있어서 정규식 `check`를 달고 STT 결과에 걸리는지 한 줄로 알려준다(연습·복습
둘 다). **형태가 뚜렷하지 않은 항목(G2·G7·G12)은 일부러 비웠다** — 억지 정규식은 맞게 말했는데
"안 썼다"고 하는 오탐을 만들고 그게 신호 없는 것보다 나쁘다. 못 넣었을 때도 오답이 아니라
다음 시도 제안으로 보여준다(자가평가가 여전히 SRS의 유일한 입력). 문구를 한 번 고침 — kidLabel이
"…말하기"로 끝나 "말하기를 잘 넣어 말했어요"가 돼서, 형태에 무관하게 읽히는 문장으로 교체.
`npm test` **171/171**(검사 3개 — kidLabel에 문법 용어 잔존 금지 / check가 실제 문장에 걸리는지
/ check가 엉뚱한 문장을 안 잡는지). **버그 되살려 빨간불 확인**: kidLabel에 "조동사" 넣으니 1건,
정규식 오타 내니 2건 실패 → 복구 후 171/171. 헤드리스로 라벨·형태 O/X·check 없는 문법에선
줄 안 뜸·복습 화면 연동 확인, 콘솔 에러 없음.
**다음 우선순위**: 6순위 A-2(iOS 녹음 폴백) / C-2(듣기 이해 문항) — 표에서 "대" 비용으로
분류된 마지막 묶음.

2026-08-06(11): **연습 카드에 "받침대 치우기" 토글 — 영어 문장 가리기·뜻 접기**(D-93).
우선순위 표 4순위(B-1/B-2). 연습 카드가 영어와 뜻을 둘 다 항상 띄워서 "듣고 따라 말하기"가
실제로는 **읽고 따라 읽기**였다 — 보고 읽으면 기억해서 만들어내는 연습이 아니라 발음 흉내만
남는다. 토글 두 개를 추가하되 **강제로 가리지 않고 학습자가 켜고 끈다**(D-61과 같은 판단 —
처음 보는 문장은 보고 읽는 게 맞고 익숙해지면 가리는 게 맞는데 그 시점은 학습자만 안다,
기본값 둘 다 꺼짐). 막히지 않게: 가려도 **그 카드에서만** 눌러 확인 가능(설정은 안 꺼짐),
접힌 뜻도 "뜻 보기"로 그 카드에서만 펼침. 범위는 repeat 카드 셋(basic·elementary·복습) —
produce·대화 연습은 그 텍스트가 정답이 아니라 질문/지시문이라 가리면 문제가 성립 안 함.
마크업·바인딩은 `js/learning/prefs.js` 한 곳(세 군데에 쓰면 문구·동작이 갈라짐). 저장은
세션 한정 메모리 — `state.learning`은 클라우드 동기화되는 진행률이라 표시 설정을 안 섞었고
localStorage도 안 씀. **작업 중 잡은 버그**: `bindReveal`이 `innerHTML` 교체 **뒤에**
`parentElement`를 읽어 null이었다(버튼이 이미 DOM에서 빠짐) — 부모를 미리 잡도록 수정.
`npm test`는 DOM을 안 보므로 이 종류는 헤드리스 콘솔 에러로만 잡혔다. `npm test` 168/168
(순수 UI라 신규 유닛 테스트 없음 — 로직이 토글 뒤집기뿐). 헤드리스로 두 토글 동작·가린 채
채점 정상·확인 후 설정 유지·다음 문장과 복습 화면까지 유지·produce엔 안 뜸 확인, 콘솔 에러
없음. `scripts/verify.cjs` 전체 통과.
**다음 우선순위**: 5순위 B-4(문법 형태 자동 확인) / B-5(kidLabel) → 6순위 A-2(iOS 녹음 폴백)
/ C-2(듣기 이해 문항).

2026-08-06(10): **간격 반복(SRS) 신설 — `due` 필드 + 도구를 가로지르는 "오늘 복습"**(D-92).
우선순위 표 3순위(B-3). §3-3이 "나중에 붙일 때"로 남겨둔 걸 그때 적어둔 전제 그대로 붙였다.
**저장 모양을 새로 만들지 않고 `weak`의 값만** `true` → `{ due, step }`으로 바꿔서, 기존에
weak를 읽던 코드(마이페이지 집계·"헷갈렸던 문장만 복습하기")가 **한 줄도 안 바뀐다**(키가
같고 객체는 truthy). 옛 불리언이 클라우드에서 돌아오는 것도 `normalizeEntry`가 흡수.
스케줄(`js/learning/srs.js`, 순수 함수): Leitner식 고정 간격 [0,1,3,7,16]일 — 틀리면 항상
처음으로, 맞히면 한 칸, 마지막 칸 넘기면 졸업(삭제, 안 그러면 목록이 끝없이 길어짐).
SM-2 같은 가변 계수는 일부러 안 씀(입력이 정답률뿐이고 어린이 대상이라 정밀도가 목적이 아님).
**"오늘 복습"(`/learning/review`)은 도구별이 아니라 전 도구를 가로지른다** — 도구마다 따로
두면 D-79에서 지적된 "복습 진입로가 사라진다"가 형태만 바꿔 되풀이되기 때문. 그런데도 도구를
import하지 않는다: 디스크립터에 `resolveReview(key,id)` 선택 필드를 추가하고 `listLearning()`으로
물어본다(D-70 경계 유지, 레지스트리 방식의 연장) — **새 도구를 추가해도 복습 화면은 안 고친다.**
`registerLearning`은 안 함(도구가 아니라 도구들의 결과를 모으는 화면이라 학습 목록에 카드로
뜨면 안 됨). 진입로 둘: 학습 목록 맨 위 카드(오늘 볼 게 있을 때만) + 마이페이지 버튼.
`npm test` **168/168**(검사 8개 추가). **버그 되살려 빨간불 확인** — 졸업 처리 빼면 1건 실패.
옛 형식 방어를 뺐을 땐 **처음에 안 잡혀서 검사를 고쳤다**(JS가 불리언 프로퍼티 접근을 허용해
`true.step`이 우연히 통과, 실제로 터지는 건 null/undefined — 초록불만 봤으면 빈 검사를 그대로
뒀을 사례). 헤드리스로 오답→진입로 등장→복습→정답→간격 밀림→목록에서 빠짐 전 흐름과, 대화
연습 produce 턴이 복습에 질문 카드로 뜨는 것까지 확인. `scripts/verify.cjs` 전체 통과.
**한계(그대로 남김)**: 익명 사용자는 state가 메모리 전용이라 새로고침하면 복습 목록도 날아간다 —
이 앱의 다른 진행률과 같은 성질이라 여기서만 예외를 두지 않았다. 로그인하면 D-68에 실린다.
**다음 우선순위**: 4순위 B-1(영어 문장 가리기)/B-2(뜻 접기) → 5순위 B-4/B-5 → 6순위 A-2/C-2.

2026-08-06(9): **발음 채점을 글자 → 단어 단위로 + 정규화 확장 + 오답 단어 표시**(D-91).
우선순위 표 2순위(A-1). 기존 채점은 글자 단위 Levenshtein 하나뿐이라 세 가지가 걸렸다:
①**맞게 말했는데 틀렸다고 나왔다** — STT는 축약형/풀어쓴 형태를 섞어 돌려주고("I'm" ↔ "I am")
숫자는 아라비아 숫자로 주는데("at 9") 문장은 "at nine"이라, 정규화가 문장부호 제거뿐이라
못 맞췄다. ②어느 단어를 틀렸는지 못 알려줘 뭘 고칠지 모른 채 넘어갔다. ③길이 편향(긴 문장은
단어를 통째로 틀려도 몇 %만 깎이고 짧은 문장은 급락). 수정: 축약형 사전 40여 개 + 숫자
사전(0~20·십 단위)으로 정규화 확장, 채점 단위를 단어로 올려 단어 배열 Levenshtein DP로 정렬
(계산식 형태는 기획서 4-4 그대로, 분모만 글자 수 → 단어 수), 단어끼리는 여전히 글자 유사도로
비교하되 완전 일치는 요구 안 함(0.8 — "helo"를 오답으로 세면 실력이 아니라 인식기를 재게 됨),
`scoreSpeech()` 신설로 점수·등급·**단어별 정오**를 한 번에 반환(DP 역추적, 원문 토큰 그대로라
화면이 바로 그림), 틀린 단어가 있으면 등급을 perfect→good으로 내림(빨간 단어 짚으면서
"완벽해요!"는 모순). 화면엔 `.learning-words` 한 줄 추가 — 틀린 단어만 강조(다 색칠하면 안 읽힘).
두 도구 repeat 카드에 적용(대화 연습·produce는 유사도 채점 자체가 없어 해당 없음).
`npm test` **160/160**(검사 8개 추가). **버그 일부러 되살려 빨간불 확인**: 정규화 되돌리면 2건,
등급 하향만 빼면 1건 실패 → 복구 후 160/160. 헤드리스로 축약형·숫자 정규화가 실제 카드에서
동작하고 틀린 단어만 빨갛게 뜨는 것 확인, `scripts/verify.cjs` 전체 통과.
**다음 우선순위**: 3순위 B-3(간격 반복: `due` 필드 + "오늘 복습" 진입로) → 4순위 B-1/B-2 →
5순위 B-4/B-5 → 6순위 A-2/C-2.

2026-08-06(8): **학습 도구 신뢰성 3건 — TTS/STT 충돌·재시도 버튼·weak 유실**(D-90).
사용자가 정리해온 우선순위 표의 1순위(A-3/A-4/A-5). 셋 다 미착수였고 코드에서 원인을 먼저
확인한 뒤 고쳤다. **A-3**: `speech.js`의 `listen()`이 인식 시작 전에 재생 중인 음성을 안
끊어서 스피커로 나가는 앱 자기 목소리를 마이크가 주워 담았다 — D-84·D-87의 자동 재생으로
"다 듣기 전에 마이크 누르는" 경로가 흔해지며 실제로 부딪히기 쉬워졌다. `listen()` 첫 줄에
`speechSynthesis?.cancel()` 한 줄(모든 호출부가 이 함수를 지나므로 세 도구 전부 해결).
**A-4**: 점수가 낮아도 결과 화면에 "다음 문장"밖에 없어 그 자리에서 다시 말할 길이 없었다 —
"🎤 이 문장 다시 말하기"(기존 `.retry-btn` 재사용) 추가, 같은 인덱스로 `showCard()` 재호출.
점수를 매기는 repeat 카드에만(produce·대화 연습은 이미 자가평가 두 버튼이 있음). **A-5**(둘 다
조용히 깨지는 종류): ①`mergeProgress()`가 원격 진도가 앞서면 엔트리를 통째로 갈아끼워
뒤진 기기의 복습 목록이 사라졌다 → 진도는 앞선 쪽, `weak`는 **합집합**으로 분리. ②`weak`
필드 이전에 저장된 레코드는 `??=`가 엔트리째 건너뛰어 `weak`를 못 채워 `st.weak[id]`에서
TypeError → 두 화면에 `st.weak ??= {}` + `mergeProgress`가 항상 객체로 채우도록 양쪽 방어.
`npm test` **152/152**(신규 `test/learning.cloud.test.js` 5개 — `mergeProgress`만 순수 함수라
export해서 node로 직접 검증). **버그를 일부러 되살려 빨간불 확인**(옛 로직으로 되돌리니 2건
실패 → 복구 후 152/152). 헤드리스로 A-3(마이크 클릭 시 cancel 호출)·A-4(버튼 노출·클릭 후
같은 문장 유지) 확인, 콘솔 에러 없음. 문서: `docs/architecture.md` §8에 테스트 스위트 표
신설(파일별로 뭘 지키는지 — 원래 한 줄짜리 설명뿐이었다).
**다음 우선순위**(사용자 표): 2순위 A-1(정규화 + 단어 단위 채점·오답 단어 표시, `score.js`
한 파일) → 3순위 B-3(간격 반복: `due` 필드 + "오늘 복습" 진입로) → 4순위 B-1/B-2 →
5순위 B-4/B-5 → 6순위 A-2(iOS 녹음 폴백)·C-2(듣기 이해 문항).

2026-08-06(7): **출력 규칙을 `CLAUDE.md` 최상단으로 이동**(D-89). "도구 호출 사이에 과정을
나열하지 말라"는 지적이 세 번 연속 반복돼 사용자가 "MD 설정 제대로 안돼있나?"라고 물었다.
문구는 이미 두 번 강화한 상태였으니 **내용이 아니라 위치 문제**였다 — 규칙이
`## Claude Code 세션 (.claude/)` 절 안(플러그인 토큰 비용·MAX_THINKING_TOKENS를 다루는 곳)에
묻혀 있어서 "매 응답마다 지키는 규칙"으로 안 읽혔다. 훅으로는 강제 불가(훅은 도구 호출·세션
경계만 가로채고 모델이 쓰는 산문은 대상이 아니다) — 위치와 반복 노출이 유일한 수단이라
①규칙 블록을 파일 맨 위(READ ORDER보다도 앞)로 옮기고 `## 🔇 출력 규칙`으로 제목을 달았고
②왜 맨 위에 있는지를 그 자리에 같이 적어 다음 세션이 "정리하다가" 다시 내리지 않게 했으며
③`## ⚠️ ABSOLUTE RULES` 첫 줄에도 한 줄 포인터를 넣어 두 곳에서 걸리게 했다.

2026-08-06(6): **`docs/architecture.md` 분리 — 계약·절차만 남기고 인벤토리 3개를 뗌**(D-88).
"다음 작업 우선순위"에 세 번 연속 쌓여 있던(17.6KB→23KB→30.7KB, "다음엔 정말 미루지 말 것")
항목을 처리. 기존 분리 규칙("테스트별로 뗀다")이 이미 다 적용돼 남은 게 전부 공통 내용이라
막혀 있었는데, **수명(콘텐츠가 늘 때 같이 커지는가)**으로 축을 새로 잡아 갈랐다 — 인벤토리
(`docs/module-map.md` 9.0KB · `docs/screen-map.md` 9.7KB · `docs/og-shells.md` 4.1KB)는
떼고, 계약·절차(라우터 계약·상태 모양·추가 절차·채점·공유·테스트 스위트, 12.0KB)만
architecture.md에 남겼다. **네 파일 전부 15KB 미만.** architecture.md는 이제 콘텐츠를
추가해도 잘 안 커진다. 같이 정리: 화면 표에 D-87의 `learning-dialogue`가 누락돼 있던 것과
`learning-elementary` guard 설명 공란을 채우고, **"자동 생성 화면은 대표 1개만 싣는다"**는
규칙을 표 위에 명시(원래 그렇게 쓰고 있었는데 안 적혀 있어서 매번 다시 판단해야 했고, 실제로
dialogue가 누락된 원인이었다). 참조 갱신: `CLAUDE.md` 상세 문서 표·동기화 매트릭스,
`docs/adhd-architecture.md`·`docs/disc-architecture.md`·`docs/learning-architecture.md` 헤더.
`npm test` 147/147(문서 변경이라 로직 영향 없음 — `test/og-shells.test.js`는 `og-shells/`
폴더를 보는 것이지 새 `docs/og-shells.md`가 아니라는 점 확인, 이름이 비슷해 새 문서 헤더에
그 사실을 적어둠).

2026-08-06(5): **각 학년 6번째 챕터 + 다중 턴 대화 도구(dialogue) 신설**(D-87). 남겨둔 두
갈래를 사용자가 둘 다 진행하기로 확정("1,2부터 진행"). (1) 저학년 `급식 시간`(🍚)·중학년
`건강과 습관`(💪)·고학년 `미디어와 기술`(📱)을 같은 틀(기본 10·중급 6·심화 4)로 추가 →
3개 학년·18챕터·360문장. 셋 다 새 문법 도입 — G18(want to)·G19(빈도부사)·G20(수동태),
세 소재의 핵심 표현이 기존 문법에 아예 없어서 순수 재사용으로 두지 않았다. (2)
`js/learning/dialogue/` 신설 — §3-7이 오래 미뤄둔 다중 턴 대화를 별도 도구로. 장면
(SCENES) 하나 = 대화 하나이고 `role: "partner"`(상대 대사, 자동 재생)와 `role: "you"`
(내 차례, 힌트만 주고 예시 답안으로 자가평가)가 번갈아 온다. 유사도 채점 없음, **분기도
일부러 없음**(STT 오인식 때 대화가 산으로 감 — 한 줄기 역할극). 지나온 턴은
`.dialogue-log`로 화면 위에 쌓아 앞 맥락 제공. 장면 4개 × 8턴. `js/main.js`에
registerLearning+registerScreens 둘 다, `test/modules.test.js` 화면 목록에도 추가.
새 CSS는 `.dialogue-log`/`.dialogue-line`/`.cover.cover-you` 셋뿐(나머지는 기존 클래스
재사용). `npm test` 147/147. 모바일 뷰포트(390×844) 헤드리스로 대화 전 흐름(로그 누적·
내 발화 기록·완료 화면) 확인, 콘솔 에러 없음. **배포 후 확인 필요**: 실기기에서 대화
연습의 상대 대사 자동 재생과 내 차례 STT가 번갈아 도는 흐름이 실제 마이크로도 매끄러운지
(헤드리스는 가짜 STT라 인식 실패·지연을 재현 못 함). 문서 갱신: `CLAUDE.md`·
`docs/architecture.md`·`docs/learning-architecture.md` §3-7(만들어짐으로 갱신)·§3-8·
`docs/DECISIONS.md`(`docs/decisions/2029-h1.md`에 D-87 추가).

2026-08-06(4): **produce 카드 마이크 버튼 레이아웃 깨짐 수정**(D-86). 사용자가 실기기
스크린샷 공유 — "🎤 내 생각 말하기" 버튼이 84px 고정 원형(`.btn-mic`, 이모지 하나만
가정)이라 긴 텍스트가 원 밖으로 줄바꿈되어 아래 "다음 문장"/"이전 문장" 버튼과 겹쳤다.
`.btn-mic`는 그대로 두고 `.btn-mic-labeled` 변형(알약 모양, width auto)을 추가해
produce 카드 마이크 버튼에만 적용 — repeat 카드·basic-conversation은 원형 그대로.
`npm test` 147/147. 모바일 뷰포트(390×844) 헤드리스 브라우저로 겹침 없음 확인,
스크린샷 육안 확인. 문서 갱신: `docs/DECISIONS.md`(`docs/decisions/2029-h1.md`에
D-86 추가).

2026-08-06(3): **문장 자연스러움 전수 검토 + produce 예시 답안 보강**(D-85). 사용자가
외부 첨삭 도구로 "I shouldn't spend too much time on games."(좋아하는 것 챕터, 심화)를
확인해보니 문법은 맞지만 원어민은 "playing games"를 쓴다는 지적을 공유. "이런 문장이 더
있으면 고치고 produce 문장엔 대안 표현도 적어주면 어떨지" 물어서, repeat 문장은 오류
교정만(채점이 문장 하나 기준이라 대안 표현 필드를 새로 못 넣음), produce 문장은 기존
`sample` 배열 보강으로 방향 확정("그렇게 하자"). elementary-conversation 300문장 +
basic-conversation 362문장 전부 훑어서 실제 교정은 3곳뿐: (1) on games → playing
games(id도 같이 변경), (2)(3) "like or not like" → "like or dislike"(학교 가는 날·체육
시간 챕터, hint도 동일 수정). basic-conversation은 원래 아동 방송 대사 참고로 지어서
(D-65) 이미 구어체라 고칠 게 없었다. produce 문장 60개(15챕터×4개) 전부 `sample`에
Just/Probably/Honestly로 시작하는 짧은 구어체 답안을 세 번째로 추가. `npm test`
147/147(로직 무변경). `node -e`로 produce 60개 전부 sample 3개, 챕터 내 id 중복
0건 확인. 헤드리스 브라우저로 교정된 문장이 실제로 렌더되는지, 콘솔 에러 없는지 확인.
문서 갱신: `docs/learning-architecture.md` §3-8·`docs/DECISIONS.md`
(`docs/decisions/2029-h1.md`에 D-85 추가).

2026-08-06(2): **초등 영어회화 3건 개선 — 이전 문장 이동·produce 질문 자동 재생·TTS 음성
품질**(D-84). 실사용 피드백 세 가지: ①챕터 진입 후 다음으로만 갈 수 있고 이전 문장으로
못 돌아감. ②질문·답변형(produce) 문제에서 질문을 소리 내어 들려주는 기능이 없음. ③듣기
음성이 기계음 느낌. 수정: (1) `basic-conversation`·`elementary-conversation` 양쪽
`showCard`류 함수에 인덱스>0일 때만 뜨는 "◀ 이전 문장" 버튼(`.retry-btn` 재사용, 새
CSS 없음) 추가 — elementary 쪽은 `advance()`와 대칭인 `prev()` 헬퍼로 구현, 복습 모드
필터링된 배열 안에서도 동작. (2) `showProduceCard()`가 뜰 때마다 `speak(card.text, 1)`
자동 호출(repeat 카드는 그대로 버튼으로만 — 기존 페이스 조절 설계 유지). (3)
`js/learning/speech.js`의 `speak()`에 `pickVoice()` 추가 — `getVoices()`에서
`localService`가 아닌(네트워크) 음성을 우선 선택, 두 도구가 `speak()`를 공유해서 자동
적용됨. `npm test` 147/147(로직 무변경, 순수 UI/음성 선택 추가). 헤드리스 브라우저로
1번째 문장엔 이전 버튼 없음/2번째부터 있음·클릭 시 인덱스 정확히 감소, produce 카드
2개에서만 `speechSynthesis.speak()` 자동 호출(repeat 카드는 호출 안 됨) 확인, 콘솔
에러 없음. 음성 자연스러움 자체는 헤드리스로 못 재는 부분이라 **배포 후 확인 필요**:
실기기에서 듣기 버튼 음성이 실제로 자연스러워졌는지(네트워크 음성이 없는 기기는 기존과
동일할 수 있음, 그 경우도 에러 없이 폴백). 문서 갱신: `docs/learning-architecture.md`
§3-8·`docs/DECISIONS.md`(6차 분리 — `docs/decisions/2029-h1.md` 신설, D-84).

> **병렬 세션 D-번호 재충돌 메모(2026-08-05~06, 네 번째)**: 이 브랜치(학습 3단계 확장)와
> main(로그인 라우팅 버그 수정)이 같은 시간대에 D-75·D-76을 각자 썼고(main이 먼저 병합돼
> 이 브랜치를 D-77~D-82로 재번호), 그 직후 두 번째 병합 시도 때 main이 그새 D-77(OAuth
> 코드 교환 명시적 호출)을 또 추가해 이 브랜치를 D-78~D-83으로 한 번 더 밀었다(아래 항목·
> `docs/decisions/2028-h1.md`·`docs/decisions/2028-h2.md`·`docs/DECISIONS.md` 전부
> 반영). D-68, D-70/71, D-75/76에 이은 네 번째 충돌.

2026-08-06: **OAuth 코드 교환을 명시적으로 호출 + 실패를 로그인 버튼에 노출**(D-77). D-76(PKCE 전환)
배포 후에도 사용자가 실기기에서 재시도 — `get_logs(auth)`에 로그인 성공이 두 번(계정을 바꿔가며 9초
간격 재시도) 찍혀 있는데도 여전히 "또 로그인하라고 뜸". 결정적 단서: `/callback` 성공 직후 코드를
토큰으로 바꾸는 GoTrue `/token` 요청이 로그에 아예 없다 — 브라우저가 코드 교환을 **시도조차 안 함**.
`detectSessionInUrl`(기본값 true)의 자동 처리를 그냥 믿고 있었는데, 이 앱처럼 순수 `createClient`를
동적 import로 늦게 띄우는 구성에서 그게 실제로 매번 발동하는지 실기기로 확증할 방법이 없었다(D-55
한계). `detectSessionInUrl: false`로 끄고 `cloud-auth.js`가 부팅 시 `location.search`의 `code`를
직접 읽어 `exchangeCodeForSession()`을 명시적으로 호출하도록 바꿈 — 실패하면 `console.error` +
`getAuthError()`로 노출하고, `auth.js`의 `renderSignInButton()`이 그 값을 읽어 버튼 대신 실패 사유를
보여준다(지금까지 두 번의 수정이 전부 "조용히 실패"해서 원인을 좁힐 신호가 하나도 없었던 게 더 큰
문제였음). `npm test` 141/141, `scripts/verify.cjs` 전부 통과(콘솔 에러 없음). 상세는
`docs/decisions/2027-h2.md` D-77, 오류 패턴은 `docs/ERRORS.md` E-15. **실기기 재확인 완료** —
사용자가 로그인 정상 동작 확인함. E-12(리다이렉트 허용 목록)·E-13(라우터 해시 훼손)·E-14(implicit
플로우 해시 손실)·E-15(자동 코드 교환 미발동)까지 네 겹의 원인이 겹쳐 있던 사례였다.

2026-08-05(7): **각 학년에 챕터 하나씩 추가 — 체육 시간·학교 행사·협동과 리더십**
(D-83, 병합 전 D-80). D-82 직후 다음 방향을 두 갈래로 물었다: (1) 각 학년 안에 챕터를
더 늘리기 vs (2) 진짜 다중 턴 대화형 도구(§3-7)로 넘어가기. 사용자가 "1"로 확정. 학년마다
챕터 하나씩 추가해 4챕터 → 5챕터(총 15챕터·300문장)가 됐다. 저학년 `체육 시간`(🏃)은
좋아하는 것과 같은 이유로 새 문법 없이 G1~G6만 재사용. 중학년 `학교 행사`(🎉)는 새
문법 G16(과거진행형, was/were + -ing)을 도입해 현재형(G7)·미래형(G9)·현재진행형(G11)에
이어 "진행형" 계열을 완성했다. 고학년 `협동과 리더십`(🏆)은 새 문법 G17(used to, 과거
습관·상태 회상)을 도입 — "I used to be shy, but now I speak up." 같은 성장 서사가
리더십 얘기와 잘 맞는다. 새 문법 id 번호(G16·G17)는 그 학년 배열 안에서 다음 번호가
아니라 **지금까지 나온 것 중 가장 큰 번호 다음**을 전체에서 이어 붙였다 — 학년별로
번호 구간을 미리 나눠두면 어느 학년이 먼저 새 챕터를 받을지 모르는 상태에서 꼬인다.
세 챕터 다 이전 챕터의 문법을 최소 하나씩 재사용해 반복 규칙을 지켰다. `node -e`로
3개 학년·15챕터 전부 기본 10·중급 6·심화 4(총 300문장), 챕터 내 id 중복 0건 확인.
`npm test` 147/147(제네릭 테스트라 무수정 — grammar 유효성·반복 규칙 검사가 새 챕터
3개에 대해서도 전부 통과). 헤드리스 브라우저로 학년별 챕터 목차에 새 챕터 포함 확인,
새 챕터 3개 전부 심화 단계 건너뛰기로 완주해 완료 문구·문법 라벨(과거진행형/used to)
확인. 콘솔 에러 없음. 문서 갱신: `CLAUDE.md`·`docs/learning-architecture.md` §3-8·
`docs/DECISIONS.md`(`docs/decisions/2028-h2.md`에 D-83 추가). 다음 단계: 확정 아님 —
챕터를 계속 늘릴지(학년당 6번째), 다중 턴 대화형 도구로 넘어갈지는 다음 요청에서 정한다.

2026-08-05(6): **고학년(5~6학년) 챕터 4개 신설 — 의견 나누기·꿈과 진로·요즘 이슈·여행과
경험**(D-82, 병합 전 D-79). D-81로 중학년(4챕터·80문장)이 다 찬 뒤 "진행"으로 다음 단계
확정 — 이걸로 초등 영어회화 학년 3단계가 전부 채워짐. `HIGH_GRAMMAR_POINTS`(G12~G15 —
현재형 복습·최상급·조건문·관계대명사 who/that)를 이전 학년과 별개 배열로 두고 번호만
이어 붙였다. 챕터는 처음 제안한 목차 그대로(의견 나누기 💬·꿈과 진로 🌟·요즘 이슈 🌍·
여행과 경험 ✈️) 이전 학년과 같은 틀(기본 10·중급 6·심화 4, 총 20문장씩)로 만들어
고학년도 4챕터·80문장 — **초등 영어회화 전체가 3개 학년·12챕터·240문장으로 완성됐다.**
중학년(의무·미래·비교·현재진행형)에서 고학년(최상급·조건문·관계절)으로 넘어가며
"계획·의무" 문장에서 "의견·조건·관계절이 들어간 복문"으로 이어진다 — `꿈과 진로`가
조건문을 "If I study hard, I will become a scientist"처럼 진로 얘기와 엮어 도입.
`여행과 경험`은 앞선 학년의 마지막 챕터(좋아하는 것/우리 동네)와 같은 이유로 새 문법
없이 G12~G15만 재사용. `node -e`로 3개 학년·12챕터 전부 기본 10·중급 6·심화 4(총
240문장), 챕터 내 id 중복 0건 확인. `npm test` 147/147(제네릭 테스트라 무수정 — 반복
규칙 검사가 고학년 4챕터에 대해서도 통과). 헤드리스 브라우저로 학년 선택 카드 3개,
고학년 챕터 목차·문장 개수 확인, 4챕터 전부 심화 단계 건너뛰기로 완주해 완료 문구·문법
라벨 확인, produce 힌트 노출 확인. 콘솔 에러 없음. 문서 갱신: `CLAUDE.md`·
`docs/learning-architecture.md` §3-8·`docs/DECISIONS.md`(`docs/decisions/2028-h2.md`에
D-82 추가). 다음 단계: 학년 3단계가 다 찼으니 (1) 학년 안에 챕터를 더 늘리거나 (2) 진짜
다중 턴 대화형 도구(§3-7, 아직 미착수)로 넘어가는 두 방향 — 확정 아님, 다음 요청에서
방향을 정한다.

2026-08-05(5): **중학년(3~4학년) 챕터 4개 신설 — 수업과 과제·친구 관계·방과 후 생활·
우리 동네**(D-81, 병합 전 D-78). D-80으로 저학년(4챕터·80문장)이 다 찬 뒤 "진행"으로 다음
단계 확정. `MIDDLE_GRAMMAR_POINTS`(G7~G11 — 현재형 복습·have to/must·미래형·비교급·
현재진행형)를 저학년과 별개 배열로 두고 번호만 이어 붙였다 — G7이 저학년 G2(일반동사
현재형)와 개념은 겹치지만 학년 트랙 독립성(D-78 전제) 때문에 일부러 새 id로 다시 정의.
챕터는 처음 제안한 목차 그대로(수업과 과제 📚·친구 관계 🤝·방과 후 생활 🎨·우리 동네
🏘️) 저학년과 같은 틀(기본 10·중급 6·심화 4, 총 20문장씩)로 만들어 중학년도 4챕터·
80문장. `우리 동네`는 `좋아하는 것`(저학년)과 같은 이유로 새 문법 없이 G7~G11만
재사용. 저학년(간단한 사실 진술)→중학년(의무·미래·비교가 들어간 문장)으로 이어지는
방향은 처음 목차 설계 때 잡은 "중학년은 감정·의견 표현이 늘고 문법이 조금 복잡해짐"과
맞춤. `node -e`로 저학년+중학년 8챕터 전부 기본 10·중급 6·심화 4(총 160문장), 챕터 내
id 중복 0건 확인. `npm test` 147/147(제네릭 테스트라 무수정 — 반복 규칙 검사가 중학년
4챕터에 대해서도 별도로 통과). 헤드리스 브라우저로 `/learning/elementary`가 학년 2개를
다시 보여주는지(D-79의 1개일 때만 건너뛰는 guard가 저절로 꺼짐) 확인, 중학년 챕터
목차·문장 개수 확인, 4챕터 전부 심화 단계 건너뛰기로 완주해 완료 문구·문법 라벨 확인.
콘솔 에러 없음. 문서 갱신: `CLAUDE.md`·`docs/learning-architecture.md` §3-8·
`docs/DECISIONS.md`(`docs/decisions/2028-h2.md`에 D-81 추가). 다음 단계: 고학년(의견
나누기·꿈과 진로·요즘 이슈·여행과 경험, 새 HIGH_GRAMMAR_POINTS부터) — 채워지면 초등
영어회화 3개 학년이 전부 완성된다.

2026-08-05(4): **저학년 나머지 챕터 3개 완성 — 교실에서·쉬는 시간·좋아하는 것**(D-80,
병합 전 D-77). D-79에서 챕터 하나(학교 가는 날)로 메커니즘(문법 태그·반복·produce·
hint·문법 라벨·마이페이지 노출)이 전부 실제로 동작하는 걸 확인한 뒤, 사용자가 "이제 큰
그림 그렸으니까 내용 채워보자"고 요청. `교실에서`(🏫)·`쉬는 시간`(⏰)·`좋아하는 것`(⭐)
3챕터를 학교 가는 날과 같은 틀(기본 10·중급 6·심화 4, 총 20문장)로 추가해 저학년이
4챕터·80문장으로 다 찼다. 문법은 챕터마다 무조건 하나씩 새로 늘리지 않았다 —
`교실에서`가 G5(should)를, `쉬는 시간`이 G6(과거형)를 새로 도입하고 `좋아하는 것`은
새 문법 없이 G1~G6를 다른 소재로 재사용만 한다("반복이 매 챕터 새 문법 하나씩"이
아니라는 사례로 일부러 이렇게 짬). 챕터마다 이전 챕터 문법을 최소 1문장 재사용해서,
챕터가 2개 이상이 되며 `test/learning.elementary.test.js`의 반복 규칙 검사가 이번에
처음으로 실제 데이터에 대해 돌아갔다(전부 통과, 테스트 파일 무수정). produce 문장
6개(챕터당 2개×3)도 D-79 규칙대로 전부 hint를 달았다. `node -e`로 4챕터 전부 기본
10·중급 6·심화 4, 챕터 내 id 중복 0건 확인. `npm test` 147/147. 헤드리스 브라우저로
학습 목록 카드 4개("기본 10 · 중급 6 · 심화 4") 확인, 4챕터 전부 심화 단계 건너뛰기로
완주해 완료 문구 확인, 콘솔 에러 없음. 문서 갱신: `CLAUDE.md`·
`docs/learning-architecture.md` §3-8·`docs/DECISIONS.md`(`docs/decisions/2028-h2.md`에
D-80 추가). 다음 단계: 중학년(수업과 과제·친구 관계·방과 후 생활·우리 동네, 새
MIDDLE_GRAMMAR_POINTS부터), 그다음 고학년.

2026-08-05(3): **초등 영어회화 UX 마찰 4건 수정**(D-79, 병합 전 D-76). D-78 직후
사용자가 "구조적으로 공부가 정말 쉽게된다고 말할수있나?"라고 다시 물어서 정직하게
짚었다: ①학년이 저학년 하나뿐인데 "학년을 골라주세요" 화면을 거치게 해서 클릭만 늘고
얻는 게 없었다. ②produce 문장이 아무 참고 없이 질문만 던져서 백지에서 답해야 하는
아이가 얼어붙을 위험이 있었다. ③`grammar` 태그가 내부 장치일 뿐 학습자 화면엔 안
보여서 지금 뭘 배우는지 의식하지 못했다. ④"헷갈렸던 문장만 복습하기"가 완료 화면에서만
뜨고 다른 데서 모아볼 방법이 없어 그 순간을 놓치면 복습 진입로가 사라졌다. 사용자가
"넷 다 반영해서 고쳐줘"로 확정. 수정: (1) `learning-elementary` 화면에
`GRADES.length===1`이면 그 학년 챕터 목차로 곧장 보내는 guard 추가(학년이 늘면 저절로
꺼짐), 챕터 목차 뒤로가기도 학년이 하나일 땐 학습 목록으로 바로 가게 수정(루프 방지).
(2) produce 문장에 `hint`(문장 시작 조각) 추가, `test/learning.elementary.test.js`에
검사 추가. (3) 연습 카드(repeat·produce 둘 다)에 "🔤 오늘의 문법: ..." 한 줄 추가
(`grade.grammarPoints`에서 라벨 조회). (4) 마이페이지 학습 집계에 `weak` 총합 노출
(개별 도구 import 안 함, 기존 D-70 집계 방식 그대로 `ch.weak || {}` 키 개수만 더함) —
단, 복습 화면 딥링크는 없어서 문구를 "해당 단계를 한 번 더 끝까지 풀면 다시 떠요"로
정직하게 적음. `npm test` 147/147(hint 검사 1개 추가). 헤드리스 브라우저로 `/learning/
elementary` 자동 건너뛰기, 뒤로가기 루프 없음, 연습 카드 문법 라벨·produce 힌트 노출,
가짜 STT로 오답 만들고 마이페이지에서 "헷갈렸던 문장 1개" 문구 확인, 콘솔 에러 없음.
문서 갱신: `docs/learning-architecture.md` §3-8 확장, `docs/DECISIONS.md`(5차 분리 —
`docs/decisions/2028-h2.md` 신설, D-79부터 여기로).

2026-08-05(2): **초등 영어회화(elementary-conversation) 신설 — 문법 진행·반복·produce
문장으로 "실질적 회화" 보강**(D-78, 병합 전 D-75). "초등학생버전 만들어보자, 목차 및
계획부터 잡자"는 요청에 학년 3단계 × 상황별 챕터 4개 목차를 먼저 제안했더니, 사용자가
"실제로 저 카테고리대로 공부하면 언어 실력향상이 도움이 되게끔 짜여져있다고 생각해?"
라고 되물어서 정직하게 답했다: 아니다 — ①문법 진행 없음(챕터가 상황으로만 나뉨),
②반복(recycling) 없음(한 번 지나간 챕터가 다시 안 나옴), ③Levenshtein 유사도 채점이
발음·암기 훈련이지 새 문장을 만드는 능력은 안 봄. 사용자가 "7세 이하는 반복이면 되지만
초등학교부터는 실질적 회화 및 언어 향상이 동반돼야 한다"며 세 가지를 보강해서 만들자고
확정. `js/learning/elementary-conversation/` 신설 — 학년(GRADES) → 챕터 → 단계
(기본/중급/심화) → 문장 연습, 자동 생성 방식은 basic-conversation의 챕터×단계(D-73)에
학년 한 겹만 곱한 것. 문장마다 `grammar` 태그(그 학년 grammarPoints의 id) 추가 +
`test/learning.elementary.test.js`가 "두 번째 챕터부터 이전 챕터의 grammar를 최소
1문장 재사용"을 자동 검사. `type: "produce"` 문장은 정답을 안 읽어주고 질문만 던지고,
예시 답안(sample)과 자가평가("잘 말했어요"/"다시 연습할래요")로 넘어간다. 낮은 점수/
자가평가로 표시된 문장은 `state.learning[key].weak`에 남아 챕터 완료 화면의 "헷갈렸던
문장만 복습하기"가 그것만 다시 돈다(진짜 SRS는 아님, §3-3에 다음 단계 기록). 공용 로직
정리: `score.js`(Levenshtein)와 TTS/STT 헬퍼(speak/listen 등)를 basic-conversation
폴더 밖 `js/learning/score.js`·`js/learning/speech.js`로 옮겨 두 도구가 공유(복사
방지). 범위: 저학년 챕터 1개(학교 가는 날, 기본 10·중급 6·심화 4, 총 20문장)만 실제로
만듦 — 중학년·고학년, 저학년 나머지 챕터 3개는 다음 단계로 남김(아직 안 만든 학년은
화면에 미리 노출하지 않음). `npm test` 146/146(신규 테스트 파일 5개 + 기존 141개
score.js/speech.js 경로 변경 후에도 그대로 통과). 헤드리스 브라우저에 가짜
SpeechRecognition을 주입해 학년 → 챕터 → 단계 → 중급 연습(오답→weak 표시, 정답→완벽,
건너뛰기, produce 문장 예시 답안·자가평가) → 완료 화면 "헷갈렸던 문장만 복습하기" →
weak 2문장만 재연습까지 전체 흐름 확인, 콘솔 에러 없음. 문서 갱신: `CLAUDE.md`·
`docs/architecture.md`·`docs/learning-architecture.md`(§3-2/§3-3/§3-5 갱신 + 새
§3-8)·`docs/DECISIONS.md`(`docs/decisions/2028-h1.md`에 D-78 추가).

2026-08-05(3): **Supabase Auth를 implicit → PKCE 플로우로 전환**(D-76). D-75(해시 보존) 배포 후
사용자가 실기기에서 재시도 — 주소창은 정확히 `fun.data-pantry.com`으로 정상 복귀하고(E-12 배제)
Supabase `get_logs(auth)`에도 그 시각 실제 Google 로그인 성공이 찍혀 있는데도 브라우저는 로그인
안 된 상태 그대로. E-12·E-13을 둘 다 배제하고 나니 남는 건 "해시가 리다이렉트 체인을 끝까지
살아남는다"는 가정 자체 — 이 앱 코드가 만드는 해시 훼손은 D-75가 막았지만, 이 앱이 통제할 수 없는
구간(카카오톡 인앱 브라우저 등)에서 해시가 사라지는 경우까지는 원천적으로 못 막는다. `cloud-auth.js`의
`createClient()`에 `auth: { flowType: "pkce" }`를 명시해 토큰을 애초에 해시가 아니라 쿼리
코드(`?code=...`)로 받도록 전환 — `router.js`의 모든 `replaceState`/`pushState`는 D-75 이전부터도
`location.search`를 무조건 보존하고 있었어서, 이 앱 구조에 원래 있던 보존 경로를 그대로 탄다.
`npm test` 141/141, `scripts/verify.cjs` 무회귀(전부 통과, 콘솔 에러 없음 — 실제 OAuth 왕복은
이번에도 샌드박스에서 재현 불가, D-55·E-12·E-13과 같은 한계). 상세는 `docs/decisions/2027-h2.md` D-76, 오류 패턴은
`docs/ERRORS.md` E-14. **배포 후 확인 필요**: 실기기에서 Google 로그인 왕복 전체(버튼 클릭 → 계정
선택 → 로그인 상태 반영) 재확인 — 이번에도 안 되면 실제 리다이렉트 체인에서 무슨 일이 벌어지는지
(예: 인앱 브라우저 여부) 확인할 방법이 필요하다.

2026-08-05(2): **라우터 부팅 `replaceState`가 OAuth 콜백 해시를 지우던 버그 수정**(D-75, 병합 시
D-72→D-75로 재번호 — 이 브랜치가 main과 같은 시간대에 병렬로 진행된 학습 3단계 확장 세션이 이미
D-72~74를 먼저 병합해 둔 상태였다). 사용자가 D-71 요청대로 Supabase 대시보드 Redirect URLs를 고친
뒤 "콜백이 없어서 그런가?"라고 재질문 — 코드를 다시 추적해 E-12(리다이렉트 허용 목록)와는 별개인
두 번째 버그를 찾았다. `cloud-auth.js`가 `flowType` 미지정으로 supabase-js 기본값인 `implicit`
플로우를 쓰는데(토큰이 `#access_token=...` 해시로 옴), `js/main.js`가 이 클라이언트를 동적
import로 띄우는 동안 곧바로 동기로 `router.js`의 `start()`가 `history.replaceState`를 호출하면서
`location.hash`를 빼먹고 URL을 덮어써버렸다 — Supabase 클라이언트가 뜨기도 전에 토큰이 URL에서
사라지는 경쟁 상태. `router.js`의 `setScreen()` 안 `replaceState` 호출 두 곳에 `location.hash`를
이어 붙여 수정. `npm test` 141/141(라우팅은 순수 JS 테스트 범위 밖이라 예상대로 무수정 통과),
Playwright로 `#access_token=FAKE...` 주소 접속 후 부팅해도 해시가 안 지워지는 것 직접 확인(실제
Google OAuth 왕복은 이 샌드박스에서 여전히 재현 불가, D-55 한계). 상세는 `docs/decisions/2027-h2.md`
D-75, 오류 패턴은 `docs/ERRORS.md` E-13. **배포 후 확인 필요**: 실기기에서 Google 로그인 왕복
전체(버튼 클릭 → 계정 선택 → 로그인 상태 반영) 재확인.

> **병렬 세션 D-번호 재충돌 메모**: 이 브랜치와 main이 같은 시간대에 각자 D-70·D-71을
> 썼다(main: 로그인 배지/마이페이지·로그인 이름 표시, 이 브랜치: 학습 3단계 확장) — main이
> 먼저 병합돼 있어 이 브랜치의 D-70/D-71/D-72를 D-72/D-73/D-74로 재번호했다(D-68에 이어
> 두 번째 충돌, `docs/decisions/2027-h2.md` 끝의 메모·`docs/DECISIONS.md` 4차 분리 헤더
> 참고). 위 D-75(라우터 해시 버그 수정)도 같은 이유로 원래 D-72였던 것을 재번호했다.
> 아래 항목의 번호는 재번호 후 값이다.

2026-08-04(4): **기본/중급/심화를 챕터당 30개씩 완전히 채움**(D-74, 병합 전 D-72). D-73에서
"30/30/30"을 제안받고도 기본만 30개 안팎으로 채우고 중급 12개·심화 6개로 범위를 좁혀
올렸는데("30개까지 채우는 건 다음 요청으로 미룬다"고 알림), 사용자가 "덜채운것 마저채워보자"고
바로 이어서 요청. 중급을 챕터당 12개 → 30개로(+18×4=72), 심화를 챕터당 6개 → 30개로
(+24×4=96) 채워 총 168문장 추가(194 → 362, 기본 122 + 중급 120 + 심화 120). 중급은 D-73
톤(짧은 질문·요청)을 유지, 심화는 D-72의 의견·이유·비교·협상·되묻기 축에 화해·공감·조건부
추론까지 소재를 넓혔다. 화면·라우팅·상태 구조는 D-73 그대로 — `LEVEL_LABELS` 필터링
방식이라 문장 개수만 늘어도 코드는 안 고쳤다. `node -e`로 챕터별 개수(전부 30/30/30,
daily-routine만 기본 32)와 챕터 내 id 중복(0건) 확인. `npm test` 141/141(제네릭 테스트라
무수정). 헤드리스 브라우저로 목차 카드 설명("기본 30 · 중급 30 · 심화 30")·단계 선택
카드("문장 30개")·family-and-me 심화 30문장 완주 확인. 문서 갱신: `CLAUDE.md`·
`docs/architecture.md`·`docs/learning-architecture.md`·`docs/DECISIONS.md`(`docs/decisions/
2028-h1.md`에 추가).

2026-08-04(3): **챕터마다 기본/중급/심화 3단계 + 단계 선택 화면 추가**(D-73, 병합 전 D-71).
사용자가 "상황챕터 누르면 기본/심화 고를수있게 하자. 심화 내용이 갑자기 확 어려워지는건
아닌가 싶기도한데, 기본에 대략 30개, 중급에 대략 30개, 심화에 30개 이렇게 넣는건 어때"라고
요청. 챕터 클릭 시 이제 단계 선택 화면(`renderLevelSelect`)이 먼저 뜬다 —
`/learning/basic-conversation/<chapterId>`가 단계 선택, `/learning/basic-conversation/
<chapterId>/<level>`가 실제 연습. `data.js`에 `LEVEL_LABELS`(기본/중급/심화)를 단일
소스로 두고 `index.js`가 `CHAPTERS × LEVEL_LABELS`로 화면을 자동 생성, `renderChapter
(chapter, level)`이 문장을 레벨로 필터링하고 진행 상태 키도 `${chapter.id}-${level}`로
단계별 독립 진행되게 했다. 콘텐츠는 기본을 챕터당 30개 안팎으로 채우고(112 → 122) 중급을
챕터당 12개씩 새로 썼다(+48) — 심화는 이번엔 6개 그대로 뒀다(30/30/30 목표엔 미달, 품질
저하 우려로 범위를 좁혔다고 사용자에게 알림). 총 문장 136 → 194(기본 122 + 중급 48 +
심화 24). `npm test` 141/141(제네릭 테스트라 무수정). 헤드리스 브라우저로 챕터 클릭 시
기본/중급/심화 카드(문장 개수 표시)가 뜨는지, 중급 진입 시 `(1/12)`부터 그 단계 문장만
나오는지, 레벨 배지·목차 카드 설명("기본 N · 중급 M · 심화 K")이 맞는지, 뒤로가기가 단계
선택 화면으로 돌아가는지, 심화 6문장 완주 후 "단계 다시 고르기"가 챕터의 단계 선택
화면으로 정확히 돌아가는지 확인. 문서 갱신: `CLAUDE.md`·`docs/architecture.md`·
`docs/learning-architecture.md`·`docs/DECISIONS.md`(4차 분리 — `docs/decisions/
2028-h1.md` 신설).

2026-08-04(2): **챕터마다 기초 → 심화 2단계 추가 — 원어민 7세 수준 문장까지**(D-72, 병합
전 D-70). 사용자가 "7세쯤 되면 자신의 의견이나 대화의 질이 훨씬 늘어난다고 생각하는데
지금 만든건 입문용인거같고 단계를 나눠서 원어민 7살정도 되는 친구들이 할수있는 대화까지
만들어볼까"라고 제안 — 탐색형 질문이라 먼저 트레이드오프만 2~3문장으로 짚었다: 이 앱의
채점 방식(문장 하나 듣고 그대로 따라 말하기)은 진짜 다중 턴 대화(질문→대답→되묻기)를
담기엔 구조적으로 안 맞아서, (1) 지금 구조에서 챕터별 기초/심화 2단계로 늘리기 vs (2)
대화형 도구를 아예 새로 설계하기 중 골라달라고 물었다. 사용자가 "지금은 1번 구조로 가고
대화형 웹앱은 추후에"로 확정. `js/learning/basic-conversation/data.js`의 문장에 선택적
`level: "advanced"` 필드를 추가하고, 4챕터 전부에 심화 문장 6개씩(의견·이유·비교·협상·
되묻기 패턴) 끝에 이어서 112 → 136문장으로 늘렸다. 화면은 안 바꿨다 — 레벨 선택 UI 없이
기초를 다 풀면 자연스럽게 심화로 넘어가고, 목차 카드 설명만 "기초 N · 심화 M"으로 갈라
보여준다. 심화 문장 카드엔 기존 `.cover .tag`(다른 테스트 인트로가 쓰던 카테고리 라벨
클래스)를 재사용해 "심화" 배지를 얹었다 — 새 CSS 없이 됐다. 대화형 학습 도구는
`docs/learning-architecture.md` §3-7에 "나중에 어떻게 붙일지"로 기록해뒀다(새 도구로 분리,
지금 도구는 확장하지 않음). `npm test` 141/141(제네릭 테스트라 무수정). 헤드리스
브라우저로 목차 설명 변경 확인, greeting 챕터를 31번째까지 건너뛰었을 때 정확히 그
지점부터 심화 배지가 나타나는지 확인, 4챕터 전부(36/38/30/32문장) 완주 확인, 스크린샷
육안 확인.

이전(2026-08-05): **로그인 UI에 이름 표시 + 실기기 로그인 무반응 원인 진단**(D-71). 두 요청이
같은 턴에 왔다. (1) "로그인이 됐으면 구글로로그인버튼위치에 사용자 이름이 떠야지" →
`js/core/auth.js`에 `currentName`(Google `user_metadata.full_name`/`name`, `gt_user_name`
캐싱) 추가, 헤더 계정 박스·마이페이지가 `currentName || currentEmail`을 메인 줄로,
이름이 있으면 이메일을 보조 줄로 표시. (2) "구글로 로그인해도 로그인이 안된다. 회원가입
기능을 안넣어서 그런가?" → Supabase MCP `get_logs(auth)`로 실제 로그인 시각을 확인하니
서버 쪽 로그인은 매번 성공(에러 없음) — 회원가입 유무는 원인이 아니다(OAuth는 최초
로그인이 곧 계정 생성). `/callback` referer가 계속 `data-pantry.com`으로 찍히는 게
단서라, `redirectTo`(`fun.data-pantry.com/`)가 Supabase Redirect URLs 허용 목록에 없어
GoTrue가 에러 없이 Site URL(추정: data-pantry.com)로 조용히 되돌리고 있을 가능성이
크다고 진단했다(D-56 도입 때부터 "확인 필요"로 남아있던 항목). 대시보드 전용 설정이라
이 세션 도구로는 못 고쳐 사용자에게 Redirect URLs 추가를 요청함 — 진단 패턴은
`docs/ERRORS.md` E-12에 기록. `npm test` 141/141, Playwright로 이름/이메일 폴백 렌더링
직접 확인(실제 OAuth 왕복은 여전히 샌드박스에서 재현 불가).

이전(2026-08-04(3)): **로그인 상태 배지 + 마이페이지 신설**(D-70, 병합 시 D-69→D-70로 재번호 —
아래 어휘 점검 항목이 같은 시간대에 D-69로 먼저 병합돼 있었다). D-68 직후 사용자 피드백
"로그인이 됐는데 됐는지 안됐는지 확인이안되네 표기를 해주던지 마이페이지를 만들어야지
좋을거같네" → 둘 다 만듦(사용자가 "마이페이지도 같이 만들어줘"로 확정). 햄버거 버튼에
로그인 시 점 배지(`.hamburger-btn.logged-in::after`), `initHeader()` 끝에서 `renderPanel()`을
즉시 한 번 더 호출해 새로고침 직후에도 로컬 캐시로 바로 정확하게 뜨게 함. `/mypage`
신설(`js/screens/home.js`) — 이메일·관리자 여부·로그아웃(햄버거 계정 박스 재사용) +
`state.learning` 집계(도구·챕터 제목은 안 봄, `renderLearningList`와 같은 이유로 개별
학습 도구 import 안 함). 구현 중 `js/core/auth.js`의 `onAuthChange`가 구독자 하나만
담는 변수였던 버그를 발견해 `Set` 기반으로 고침(안 고쳤으면 마이페이지 구독이 header.js
구독을 덮어써서 배지가 깨졌을 것). `npm test` 141/141, `scripts/verify.cjs` 전체 재통과 +
Playwright로 배지·마이페이지 렌더 직접 확인. 상세는 `docs/decisions/2027-h2.md` D-70.

이전(main 머지 완료, 2026-08-04, 2차) — 아래 D-69 커밋이 main과 합쳐짐. 이 브랜치
(`claude/learning-category-webapp-plan-vpcwic`)와 main이 같은 세션 시간대에 각자
따로 진행되면서 둘 다 "D-68"을 썼다 — main 쪽 D-68(로그인 개방 + 학습 진행률 Supabase
동기화)이 먼저 병합돼 있었던 걸 이번 병합에서 발견해, 이 브랜치의 D-68을 **D-69로
재번호**했다(아래 항목·`docs/decisions/2027-h2.md`·`docs/DECISIONS.md` 전부 반영).
파일 충돌은 `CURRENT_TASK.md`·`docs/DECISIONS.md`·`docs/decisions/2027-h2.md` 세
문서뿐이었고(둘 다 같은 절 근처에 새 항목을 추가해서), 코드(`js/learning/basic-conversation/screens.js`)는
git이 자동 병합했다 — 한국어 듣기 버튼과 `saveLearningProgress()` 호출이 서로 다른
줄이라 겹치지 않았다. 병합 후 `npm test` 141/141 재확인.

2026-08-04: **어휘 점검 · 7세 이하로 대상 명시 · 문장 11개 추가 · 한국어 음성 지원**
(D-69, 병합 시 D-68→D-69로 재번호). 사용자 요청 4건: (1) 112문장 전체 어휘 점검 — 전부
원어민이 실제로 쓰는 고빈도 표현이라 교체 불필요(유일하게 "Cheers!"가 다소 어른스럽지만
오류는 아님). (2) 도구 카드를 "기초 영어회화 (7세 이하)"로 대상 명시. (3) 위생/화장실 4개
(daily-routine) · 색깔 3개(family-and-me) · 공원/카운트다운 4개(playground) 총 11문장
추가(101 → 112문장), 전부 기존 챕터에 자연스럽게 붙어 D-66 방침대로 새 챕터는 안 만듦.
(4) 한글을 아직 못 읽는 어린이를 위해 `speak(text, rate, lang)`에 `lang` 매개변수를
추가하고 한국어 해석 옆에 🔊 버튼을 붙여 `ko-KR`로 뜻을 읽어주게 함 — 기존 영어 듣기와
같은 메커니즘, 언어만 다름. 이미지(픽토그램) 대신 음성을 택한 이유: 마스코트 이미지는
D-64에서 이미 뺀 상태라, 기존 TTS 인프라를 그대로 재사용하는 게 제일 작은 확장이었다.
`npm test` 141/141. 헤드리스 브라우저로 `speechSynthesis.speak`를 가로채 한국어/영어
듣기 버튼이 각각 `{lang:"ko-KR"}`/`{lang:"en-US"}`으로 독립 호출되는지 확인, 4챕터
전부(30/32/24/26문장) 완주 확인, 스크린샷으로 🔊 버튼 위치 육안 확인.

이전(main 머지 완료, 2026-08-04, 1차) — 아래 D-68 커밋(`claude/supabase-env-keys-xbec6s`,
9e5bd4c → 31a50c9)이 충돌 없이 main에 fast-forward 병합됨. 병합 전 `npm test` 141/141
재확인 후 push. main push라 Cloudflare Pages 자동 배포 트리거됨 — 아래 "배포 후 확인
필요"의 로그인·학습 진행률 동기화 실기기 확인 항목이 이제부터 실제로 유효해진다.

이전(2026-08-04): **로그인을 관리자 전용 → 일반 방문자로 열고, 학습 진행률을 Supabase에
동기화**(D-68). 사용자 요청 "일반방문자도 로그인 가능하도록 만들어줘. 그래야 학습시스템도
사용자별로 저장할수있지. 관리자 이메일만 별도로 등록해서 관리되도록 코드 설정해두면
되잖아". `js/core/auth.js`의 `ADMIN_EMAIL` 필터를 없애 로그인한 사용자 누구든
`currentEmail`로 추적하고, `isAdmin()`만 부부 체크 게이트(D-56)용으로 별도 유지.
localStorage 키 `gt_admin_email` → `gt_user_email`. Supabase에 `learning_progress`
테이블(user_id PK, RLS 본인만) 신설 + `js/learning/cloud.js`(NumPath 마을 D-55와 같은
패턴, 코인/마을 없음)로 로그인 시 병합·진행 시 업서트. 개인정보처리방침(`/privacy`)에
로그인·학습 진행률 저장 섹션 반영. `npm test` 141/141, `scripts/verify.cjs` 전체 통과
(관리자 게이트 시뮬레이션은 새 키 이름으로 갱신). 상세는 `docs/decisions/2027-h2.md` D-68,
배포 후 실기기 확인 항목은 아래 "배포 후 확인 필요" 참고.

이전(main 머지 완료, 2026-08-03) — 아래 학습 카테고리 세션(D-60~D-67) 커밋 8개가 충돌 없이
main에 병합됨(`claude/learning-category-webapp-plan-vpcwic`, dc3494d → abcb043,
fast-forward). main이 그 사이 더 앞서가지 않아 순수 fast-forward — 별도 병합 커밋도
충돌 해소도 없었다. 병합 전 `npm test` 141/141 재확인 후 push. main push라 Cloudflare
Pages 자동 배포 트리거됨 — `docs/learning-architecture.md` §4·이 파일의 "배포 후 확인
필요"에 있는 실기기 STT/TTS 확인 항목이 이제부터 실제로 유효해진다.

2026-08-03(8): **문장 틀 반복(패턴 드릴) 추가**(D-67). 사용자 질문 "반복학습에 최적화
하려면 같은 형식의 문장이 단어만 바뀌어서 여러번 반복되는게 좋잖아. 그럼 문장을 더
늘려주는게 좋은거 아닌가?"에 동의 — 로드맵 1-2가 애초에 제안했던 "만능 문장 틀" 방식을
기존 4챕터에 13문장 추가로 반영(88 → 101문장). "I want ~"(milk/juice/water)+"Can I have
a cookie?"는 daily-routine에, "I like ~"(singing/soccer/ice cream)+"This is my
~"(sister/grandpa)는 family-and-me에, "Let's play ~"(tag/hide and seek/soccer)+"It's
cloudy today."는 playground에 — 전부 이미 있던 문장(I like drawing / This is my mom /
Can I have more? / Let's play!)의 변형이라 D-66 방침대로 새 챕터 없이 기존 챕터에
붙였다. 답하면서 캐치한 지점: 패턴 드릴(한 세션 안 반복)과 간격 반복 SRS(시간을 두고
다시 노출)는 서로 다른 걸 최적화한다 — 이 앱은 SRS가 아직 없어서(로그인 이후로 미룸,
`docs/learning-architecture.md` §3-3) 패턴 드릴을 SRS의 대체물이 아니라 먼저 넣을 수
있는 별개 기법으로 추가했다.
`npm test` 141/141(제네릭 테스트라 문장 추가에도 무수정). `node -e`로 챕터별 문장 수
(30/28/21/22, 합계 101) 확인, 헤드리스 브라우저로 4챕터 전부 건너뛰기 완주해 새 개수와
완료 문구 일치 확인.

이전(2026-08-03(7)): **챕터를 8개 → 4개로 병합 — 잘게 쪼개지 말고 시간순/소재로 묶는다**(D-66).
사용자 요청 "묶을수있는건 묶어서 한 챕터당 문항수를 늘리고 챕터가 무분별하게 늘어나서
정리안되는 상황을 방지하자". D-65에서 8~10문장씩 8챕터로 짰던 게 결국 목차 화면을 카드
8개짜리 스크롤 목록으로 만들어버려서, D-62("학습을 카탈로그가 아니라 하나의 공부
도구로")의 문제가 도구 안 챕터 레벨에서 재발한 상태였다. `morning-routine`+`meal-time`+
`bath-bedtime` → `daily-routine`(하루 일과, 24문장, 아침→식사→목욕/잠자리 시간순),
`self-intro`+`family` → `family-and-me`(가족·자기소개, 16문장), `playing-with-friends`+
`weather` → `playground`(놀이터에서, 18문장)로 합쳤다. `greeting`(30문장)은 그대로.
문장은 하나도 안 버렸다 — `node -e`로 병합 전후 총합을 88개로 직접 대조하다가 옛
meal-time의 "Cheers!"(건배!)가 옮기는 과정에서 누락된 걸 발견해 다시 넣었다.
앞으로의 기본값도 정했다: 새 상황이 생기면 새 챕터부터 만들지 않고, 시간순으로 이어지거나
소재가 겹치는 기존 챕터에 먼저 붙일 자리를 본다(`docs/learning-architecture.md` §2,
`docs/architecture.md` §5에 반영).
`npm test` 141/141(제네릭 데이터 검증 테스트라 챕터 병합에도 테스트 파일 무수정).
헤드리스 브라우저로 목차가 4카드로 줄고 문장 개수 표기(30/24/16/18)가 맞는지 확인,
4챕터 전부 건너뛰기로 완주해 완료 문구 일치 확인, 스크린샷으로 목차가 스크롤 없이
한 화면에 들어오는지 육안 확인.

이전(2026-08-03(6)): **챕터 목차를 일상대화 위주로 재구성 — 어린이 애니메이션 참고**(D-65).
사용자 요청 "일상대화 위주로 카테고리를 짜줘. 어린이 애니메이션 참고해서". 기존 4챕터 중
`animals-food`(동물/음식)·`school-playground`(학교/놀이터)는 로드맵 1-2의 단어 카테고리를
그대로 옮긴 거라 문장도 "This is a rabbit" 같은 사물 명명에 가까웠다 — 뽀로로·타요·
페파피그류가 어휘가 아니라 "아침에 일어나기·친구랑 놀기·밥 먹기·목욕하고 자기" 같은 하루
일과 하나를 에피소드로 삼고 그 안에서 실제로 오가는 말을 반복해서 들려주는 결을 참고해
6챕터로 다시 짰다: `morning-routine`(아침 일과)·`family`(가족 이야기)·
`playing-with-friends`(친구랑 놀기, 옛 school-playground의 놀이 문장 재사용+확장)·
`meal-time`(밥 먹을 때, 옛 animals-food의 "yummy"만 남기고 재작성)·`bath-bedtime`(목욕·
잠자리)·`weather`(날씨 이야기). `greeting`(30문장)·`self-intro`는 그대로 유지. 최종 8챕터
(30·8·8·10·8·8·8·8문장). `js/learning/basic-conversation/data.js`만 고쳤고 `js/main.js`·
라우터·목차 화면(`renderBasicConversationIntro`)·점수 로직은 전부 무수정 — D-63 구조가
챕터 교체에도 그대로 버틴다는 걸 다시 확인했다.
`npm test` 141/141(제네릭 데이터 검증 테스트라 챕터 내용이 바뀌어도 테스트 파일은
무수정). 헤드리스 브라우저로 목차의 8개 챕터·문장 개수 표기 확인 + 8개 챕터 전부
건너뛰기로 완주해 완료 문구가 실제 문장 개수와 일치하는지 확인, 스크린샷 육안 확인.

이전(2026-08-03(5)): **마스코트 제거 · 인사/기분 표현 30문장 확장 · 챕터 3개 추가 · 한글 해석
폰트 확대**(D-64). 사용자 피드백 4건을 한 번에 처리: (1) "위에 이모티콘이 별로인거같아서
빼고 나중에 이미지 추가하게 되면 따로 넣자" → `js/learning/mascot.js`·문장의 `mood` 필드·
`test/learning.mascot.test.js` 전부 삭제(D-61 되돌림), `styles.css`의 죽은 `.cover .mascot`
규칙도 같이 제거. (2) "인사 기분표현에 내용을 30개정도로 늘리고" →
`js/learning/basic-conversation/data.js`의 `greeting` 챕터를 5문장에서 인사·감정 표현·기본
대화 30문장으로 확장. (3) "추가 챕터리스트를 좀 뽑아보자" → 로드맵 1-2가 어린이 특화로
지목한 나머지 세 상황(동물/음식·학교/놀이터·자기소개)을 각 8문장씩 채워 `CHAPTERS`에
추가 — `js/main.js`·라우터·목차 화면 전부 무수정으로 반영됨을 확인(D-63 구조가 실전
검증됨). (4) "영어 밑에 한글 해석 폰트를 조금 더 키우자" → 카드의 한글 `<p>`에
`cover-ko` 클래스 추가, 13px → 17px. `.cover p`(0,1,1)가 단일 클래스 선택자(0,1,0)보다
상세도가 높아 그냥 두면 밀리는 걸 확인하고 `.cover p.cover-ko`(0,2,1)로 올려서 확실히
이기게 했다 — 다른 테스트 인트로의 `.cover p`(13px)는 안 건드림.
`npm test` 141/141(마스코트 검사 3개 감소). 헤드리스 브라우저로 목차 화면 4개 챕터·문장
개수 표기(30/8/8/8) 확인, `.cover-ko` 실제 렌더 폰트 크기 확인, 마스코트 svg 미노출 확인,
네 챕터 전체를 건너뛰기로 완주해 문장 개수·완료 문구 일치 확인, 스크린샷 육안 확인.

이전(2026-08-03(4)): **학습을 2단(도구 카탈로그 → 도구 내 목차)으로 재정정**(D-63). 직전 세션(D-62,
바로 아래)에서 학습 카테고리의 레지스트리·목록을 통째로 없앤 게 과잉이었다 — 사용자가
"학습에 하나의 도구로 만들어줘야지 목차별로 다 나열할 수가 없잖아. 하나의 공부 창이
되는거지. 왜냐하면 학습 카테고리 안에 다른 공부 도구들도 넣을거거든"이라고 정확한 목표
구조를 알려줬다: 홈 → 학습 목록(도구 카드, 심리테스트 목록과 대칭) → 도구 하나("기초
영어회화")의 목차(챕터 카드) → 챕터 하나(문장 연습). "상황을 홈 최상위 카드로 노출하지
않는다"는 D-62의 판단은 유효했고, 빠진 건 "도구 레벨엔 카탈로그가 있어야 한다"였다.
`js/core/router.js`에 `registerLearning`/`listLearning` 재추가(도구 등록용, `registerTest`와
대칭). `js/screens/home.js`에 `renderLearningList()` 재추가. `js/learning/greeting/` →
`js/learning/basic-conversation/`로 이동하고, `data.js`를 평평한 `SENTENCES`에서 챕터
배열 `CHAPTERS`(챕터마다 `{ id, title, emoji, sentences }`)로 재구성 — `index.js`가
`CHAPTERS.map()`으로 챕터별 화면을 자동 생성해 챕터가 늘어도 이 파일을 안 고친다.
경로: `/learning`(도구 목록) → `/learning/basic-conversation`(목차) →
`/learning/basic-conversation/greeting`(첫 챕터). `state.learning`도 `{ greeting: {...} }`
고정 키에서 `{}` + 챕터 화면 진입 시 `state.learning[chapter.id] ??= { index: 0 }` 지연
초기화로 바꿔, 도구·챕터가 늘어도 state.js를 안 고친다.
`test/learning.score.test.js`·`test/learning.mascot.test.js`를 `CHAPTERS` 구조에 맞게
갱신(챕터 id 중복·챕터별 문장 id 중복 검사 추가). `npm test` 144/144(챕터 검사 1개 순증).
헤드리스 브라우저로 홈 → 학습 목록 → 기초 영어회화 목차 → greeting 챕터 진입, 건너뛰기
5회 → 완료 → "목차로 돌아가기", 뒤로가기를 3단(챕터→목차→목록→홈) 전부 확인.

이전(2026-08-03(3)): **학습의 레지스트리·목록 화면 제거 — "카탈로그"에서 "하나의 공부 도구"로 정정**
(D-62). 사용자 피드백 — "학습에 하나의 도구로 만들어줘야지 목차별로 다 나열할 수가 없잖아.
하나의 공부 창이 되는거지." 직전 세션(D-60)에서 `registerGame`/`listGames`를 그대로 복사해
`registerLearning`/`listLearning` + 목록 화면(`learning-list`)을 만든 게 잘못된 모델이었다 —
심리테스트·미니게임은 서로 독립적이라 카탈로그가 맞지만, 학습은 여러 상황이 하나의
커리큘럼을 이루는 도구라 매번 홈까지 돌아와 카드를 고르게 하면 안 맞았다.
`js/core/router.js`에서 `registerLearning`/`listLearning` 삭제, `js/screens/home.js`에서
`renderLearningList`/`learning-list` 삭제하고 "학습" 카드가 `learning-greeting`으로 바로
가게 변경, `js/learning/greeting/index.js`의 카탈로그용 `card` 메타데이터 삭제. 경로도
`/learning/greeting` → `/learning`로 단순화(목록이 없어져 하위 경로일 이유가 없음) — 이
화면은 이번 스프린트에 새로 만든 것이라 아직 배포 이력이 없어 경로/id를 바꿔도 "이미 열린
탭의 뒤로가기가 깨진다"는 규칙에 걸리지 않는다.
다음 상황을 어떻게 이을지(홈에 카드를 또 만들지 않고 같은 화면 흐름 안에서 잇는 방향)는
아직 실전 검증 전이라 `docs/learning-architecture.md` §3-5에 방향만 적어뒀다.
`npm test` 143/143(개수 불변). 헤드리스 브라우저로 홈 → 학습 카드 클릭 → 목록 없이 바로
문장 카드 도착 → 뒤로가기 → 홈, `/learning` 직접 접속까지 확인.

이전(2026-08-03(2)): **학습 카테고리 문장 카드에 마스코트 SVG + 건너뛰기 버튼 추가**(D-61). 사용자가
"상황별 그림·캐릭터 이미지"와 "원하면 체크하고 다음으로 넘어가는 기능"을 요청. 이미지 생성
도구가 없어 외부 에셋 대신 `js/learning/mascot.js`(얼굴 하나 + 표정·소품 조합 인라인 SVG)로
해결 — mood 문자열(`wave`/`happy`/`sad`/`friendly`) 하나로 기존 조합을 재사용한다. 처음엔
"nice to meet you"에 손 모양 소품(handshake)을 썼는데, 헤드리스로 스크린샷을 직접 찍어보니
얼굴 옆에 작은 원 하나만 떠 있어 "악수"로 안 읽히길래 하트 소품(friendly)으로 교체.
`renderLearningGreeting()`에 `#learning-skip` 버튼("✓ 이미 할 수 있어요 · 다음 문장")을
추가해, 듣기/말하기 없이 바로 다음 문장으로 넘어갈 수 있게 했다 — 마이크 결과가 나온 뒤엔
같은 역할을 하는 "다음 문장" 버튼과 겹치지 않도록 건너뛰기 버튼을 숨긴다.
`test/learning.mascot.test.js` 신설(마스코트 svg 유효성 · 미등록 mood 폴백 · 문장 데이터
mood↔MASCOTS 대응 검사 — 오타로 조용히 기본값 폴백되는 걸 잡는다). `npm test` 143/143.
헤드리스 브라우저로 5개 카드 전부 스크린샷 확인(사람이 보기에 감정이 구별되는지는 이 세션이
스크린샷으로 직접 판단) + 건너뛰기만으로 마이크 없이 완료 화면까지 도달 확인.

이전(2026-08-03): **학습(외국어 연습) 카테고리 신설 — 어린이 모드 파일럿**(D-60). 사용자가 준비한
영어 학습 웹앱 로드맵(어르신/어린이용, Next.js+Supabase+PG결제 풀스택 기획)을 이 저장소의
세 번째 카테고리("학습")로 대폭 축소해 적용 — `AskUserQuestion`으로 (1) 이 저장소에 세 번째
카테고리로 얹을지 vs 완전 별도 앱으로 새로 지을지, (2) 어르신/어린이 중 v1을 누구부터 만들지
확인해 "이 저장소에, 어린이 모드부터"로 좁혔다.
`js/core/router.js`에 `registerGame`/`listGames`를 그대로 복사한 `registerLearning`/
`listLearning` 추가, `js/learning/greeting/`(인사/기분 표현, 문장 `SENTENCES.length`개) 신설 —
브라우저 내장 `speechSynthesis`(0.75배속 토글)·`SpeechRecognition`만 쓰고 서버 API·로그인·
영속 저장 없음. 발음 판정은 Levenshtein 유사도 기반 3단계 피드백(`js/learning/greeting/score.js`,
DOM을 모르는 순수 함수). 홈에 "학습" 카테고리 카드 + 목록 화면(`/learning`) 추가,
`theme-learning`(노랑-오렌지 계열) 신설.
로드맵에서 잘라낸 것(로그인·SRS·결제·어댑터 패턴·어르신 모드·광고 슬롯)은 "안 만든다"가 아니라
"나중에 이렇게 붙인다"로 `docs/learning-architecture.md` §3에 항목별로 기록 — 다시 원점에서
논의하지 않기 위해서다.
`npm test` 140/140(135 + 학습 채점 테스트 5개). 헤드리스 브라우저(playwright, 가짜 `SpeechRecognition` 주입)로
목록→상황→문장 5개 전체 진행→완료→재시작까지 실제 클릭 확인 — 이 과정에서 뒤로가기 버튼이
`bindNav()` 호출 누락으로 무반응이던 버그를 찾아 같이 고쳤다. 실제 마이크·STT 자체는 이
샌드박스에 마이크·네트워크가 없어 검증 못 함(`docs/learning-architecture.md` §4).
`docs/architecture.md`가 이번 추가로 23KB까지 커져 자동 분리 기준(15KB)을 더 넘었다 — 다만
남은 내용이 이미 "공통" 부분(모듈맵·라우터 계약·화면 표·상태 모양)이라 테스트별 문서로 뺄
잉여가 없어 보여, 무엇을 더 뺄지는 판단이 필요해 보류했다(아래 "다음 작업 우선순위" 참고).
`docs/decisions/2027-h1.md`도 이번에 787줄/82KB로 한계를 크게 넘겨, 안내된 대로
`docs/decisions/2027-h2.md`를 새로 만들어 D-60부터 그쪽에 썼다(과거 D-28~D-59는 그대로 둠).

이전(2026-07-30): **NumPath 런 타이머 + 난이도별 개인 최고 기록**(D-59). 요청("타이머·사용자
랭킹")에서 랭킹은 `AskUserQuestion`으로 범위를 확인 — 멀티유저 비교는 방금 되돌린 Supabase
백엔드(D-54/D-55)를 다시 켜야 해서, "내 기록만(로컬)"로 좁혔다. 플레이 HUD에 실시간
타이머(m:ss), 결과 화면에 소요 시간 + 신기록 배지/최고 기록 비교, 인트로 난이도 카드에도
🏆 표시. `run.startedAt`/`finishedAt`(Date.now())로 계산, 최고 기록은
`localStorage["gt_numpath_best_<난이도>"]`(`screens.js`의 `loadBestTime`/`saveBestTime` 두
곳에만 접근 격리). `data.js`에 순수 함수 `formatTime()` 추가 + 단위 테스트.
`docs/numpath-architecture.md`가 지난 되돌림(D-54/D-55) 이후 갱신이 안 돼 마을·클라우드
기준으로 남아 있던 것도 이번에 같이 정리(`docs/architecture.md`의 화면 표·state 모양 스테일
참조 포함).
`npm test` 135/135, `scripts/verify.cjs` 169/169(`serve.py`) 통과.

이전(2026-07-30): **NumPath 마을(코인·건설·클라우드 동기화, D-54/D-55) 되돌림.** 사용자 판단 —
"채울 공간이랄 게 없어서 메리트가 크지 않다, 나중에 더 큰 그림 그린 다음 다시." 코인 지급·
마을 화면(`/game/numpath/village`)·클라우드 로그인 코드를 전부 제거하고 게임(난이도·리플레이성)만
남겼다. 삭제: `village.js`·`cloud.js`(NumPath 전용판)·`cloud-loader.js`·`test/numpath.village.test.js`,
`DIFFICULTIES`의 `coinsPerStar` 필드, `screens.js`/`play.js`/`index.js`/`data.js`/`state.js`의
관련 코드, `styles.css`의 `.np-village-*`/`.np-cloud-*`/`.np-scene-*`/`.np-shop-*`/`.np-progress`/
`.np-wallet`/`.np-build-btn` 블록, `verify.cjs`의 관련 검사. 우상단 관리자 로그인(D-56~D-58,
아래 참고)은 이미 자기 전용 `js/core/cloud-auth.js`를 따로 갖고 있어 이번 삭제와 무관 — 그대로
동작한다. Supabase `numpath_village` 테이블은 남겨둠(비용 없음, 나중에 재사용 가능) — 관련
결정은 `docs/decisions/2027-h1.md` D-54/D-55에 그대로 두고 되돌림만 여기 기록(반복 제안 방지,
`docs/DECISIONS.md`의 "되돌린 접근" 관례).
병합 후(관리자 로그인 D-56~58 포함) `npm test` 134/134, `scripts/verify.cjs` 164/164(`serve.py`) 통과.

이전(2026-07-30(10)): **관리자 로그인 버튼 무반응 버그 수정**(D-58). 사용자가 "로그인 안 되네?" —
버튼을 눌러도 아무 반응 없음(구글 계정 선택 화면까지도 안 감)이라고 구체화. 코드 리뷰로
실제 원인 둘을 찾았다: ① `core/header.js`의 `initAuth()`가 로그인 상태를 비동기로 확인하며
`sync()`를 여러 번 부르는데, 그때마다 `renderPanel()`이 상태 변화 여부와 무관하게 로그인
버튼을 통째로 지우고 새로 그렸다 — 페이지 열자마자 햄버거를 누르는 흔한 패턴에서, 사용자가
누른 버튼이 그 사이 교체돼 클릭이 허공에 뜰 수 있었다(관찰된 증상과 정확히 일치). `lastRenderedEmail`
캐시로 상태가 실제로 바뀔 때만 다시 그리도록 고쳤다. ② `signInWithProvider()`가 실패해도
보통 reject 없이 `{ error }`로 resolve하는데, 기존 코드는 `.catch()`만 걸어둬서 실패가
콘솔에도 화면에도 전혀 안 남았다(`CLAUDE.md`의 "에러 삼킴 금지" 위반). `then(({error})=>...)`로
명시 확인해 버튼 라벨에 실패 사유를 노출하도록 고쳤다.
`npm test` 146/146(불변), `scripts/verify.cjs` 전체 재통과(`serve.py`) — 실제 OAuth 클릭
흐름 자체는 이 샌드박스가 Supabase CDN을 막아둬서 헤드리스로 재현 못함(D-55와 같은 한계).
**미해결**: 이번 수정으로도 안 되면 이제 버튼에 실패 사유가 그대로 뜨니 그걸로 원인을 좁힐
것 — Supabase 대시보드의 Google provider·리다이렉트 URL 허용 목록 쪽 문제일 가능성은 배제
안 했다(코드로 확인 가능한 범위 밖).

이전(2026-08-01): **세션 토큰 소모량 점검 — `ponytail` 플러그인 제거**. "요청마다 과정이 과한 것 같다"는
사용자 피드백으로 조사. 계정 전역에 설치돼 있던 `ponytail` 플러그인(`.claude/hooks/session-start.sh`가
원격 세션이 새로 뜰 때마다 재설치하던 것)이 세션 시작·서브에이전트 실행마다 ~90줄 지침을 컨텍스트에
재주입하고 있었다 — `claude plugin details`로 실측하니 세션당 always-on ~983 tok, 서브에이전트를
띄울 때마다 추가 재주입. `claude plugin uninstall`로 제거하고 `session-start.sh`의 재설치 블록도
삭제(graphify는 텍스트 주입 없는 순수 CLI라 별개로 유지). 재발 방지로 CLAUDE.md에 "새 플러그인
추가 전 `claude plugin details`로 always-on 토큰 비용 확인" 가드레일 한 줄 추가.
이어서 "자동화 대신 필요할 때 직접 정리하겠다"는 사용자 결정에 따라, 출력·추론 토큰 절감
설정만 우선 적용: `.claude/settings.json`에 `alwaysThinkingEnabled: false` +
`env.MAX_THINKING_TOKENS: "1024"` 추가.
**정정(같은 세션)**: 처음엔 `alwaysThinkingEnabled: false`로 확장 사고를 완전히 꺼뒀는데,
사용자가 "필요한 문제인지 파악해서 자동 적용"을 요청 — `false`는 쉬운/어려운 요청 구분 없이
사고를 막아버리는 것이라 요청과 반대였다. `alwaysThinkingEnabled` 키 자체를 지워 기본값(모델이
난이도 보고 자동 판단)으로 되돌리고, `MAX_THINKING_TOKENS`(1024)는 켜졌을 때의 상한선으로만
남겼다. `outputStyle`은 이미 기본값(Default)이 가장 간결한 옵션이라 변경 없음.
추가로 진행 과정 중계형 서술(무엇을 확인·읽었다)을 줄이고 끝에 요약만 남기라는 요청을
CLAUDE.md에 반영. 앱 코드 변경 없음 — `npm test`/`scripts/verify.cjs` 대상 아님.

이전(2026-07-30(9)): **햄버거 버튼 위치 조정 + 화면별 홈 버튼을 메뉴로 흡수**(D-57). 실기기
스크린샷 피드백 — 햄버거가 `#app` 예약 여백(D-56에서 확보한 56px) 안, 로고 행보다 위쪽
빈 공간에 붕 떠 보였다. `#app` 여백을 없애고 햄버거를 각 화면 `.header`/`.back-row`
우상단에 겹쳐 그리는 방식으로 바꿨다. 동시에 화면마다 있던 홈 버튼(`.exit-btn`, 🏠) 34곳을
전부 제거하고 햄버거 메뉴의 "홈으로 가기" 항목 하나로 합쳤다 — `core/router.js`에
`setExitGuard()`/`getExitGuard()`(onLeave와 같은 생명주기)를 추가하고, `core/dom.js`의
새 `goHome()`이 진행 중인 화면(문항 풀이 등)이면 기존과 같은 "그만둘까요?" 확인 모달을
거치고, 아니면 곧장 이동한다. 확인 모달은 사용자 확인 하에 그대로 유지했다(응답 유실
방지).
`npm test` 146/146(불변), `scripts/verify.cjs`에 새 검사 5개 추가(진행 무/유에 따른
동작·취소·확인·재시작 시 실제 초기화 확인) 후 `serve.py`로 통과 확인.
**미해결**: 햄버거 `top` 위치는 계산 근사치라 실기기(`.header`/`.back-row` 양쪽) 확인 필요.

**main 머지 완료(2026-07-30, 6차)** — 위 D-57 커밋이 충돌 없이 main에 머지됨
(`3a8bff2` → `654d78e`). main이 그 사이 더 앞서가지 않아 CURRENT_TASK.md 한 곳만
자동 병합, 코드는 순수 병합. 머지 전 `npm test` 146/146 재확인 후 push.

이전(2026-07-30(8)): **우상단 햄버거 메뉴 + 관리자 로그인을 main에 합치며 중복 정리**(D-56).
main을 먼저 merge해보니 main이 이미 NumPath 마을 동기화용 Supabase Auth(Google·카카오
OAuth, D-55)를 실제로 배포까지 끝낸 상태였다 — 내 쪽은 Google Identity Services(GIS)를
새로 붙였는데 `GOOGLE_CLIENT_ID`가 자리표시자라 동작하지 않는 상태였다. 사용자가 "로그인
관련 중복 정리해서 main에 남겨달라"고 요청해, GIS를 통째로 걷어내고 이미 동작하는 Supabase
Auth를 재사용하도록 다시 만들었다. `js/games/numpath/cloud.js`에 있던 Supabase 클라이언트
생성·자격증명을 `js/core/cloud-auth.js`(+`cloud-auth-loader.js`)로 빼서 NumPath 마을과
관리자 로그인이 같은 클라이언트 하나를 공유하게 했다(자격증명 중복 제거) — 마을 쪽의
로그인 시 병합(merge) 타이밍이 중요한 로직은 그대로 두고, 공용 클라이언트·`getCachedUser`·
`signInWithProvider`·`signOut`만 core로 옮겼다. `js/core/auth.js`는 이제 GIS 없이 이
공유 클라이언트의 세션을 확인해 이메일이 관리자(`xogns022@gmail.com`)와 같을 때만
`localStorage["gt_admin_email"]`에 남긴다(village.js와 같은 "로컬 캐시 우선 + 연결되면
교정" 패턴이라 CDN 차단·오프라인에서도 직전 로그인 상태가 유지된다). `index.html`의 GIS
`<script>` 태그도 제거.
상세는 `docs/decisions/2027-h1.md` D-56 참고.
`npm test` 146/146, `scripts/verify.cjs` 169/169(`serve.py`) 통과 확인.

**main 머지 완료(2026-07-30, 5차)** — 위 D-56 정리 커밋이 충돌 없이 main에 머지됨
(`a527010` → `684daf6`). main이 그 사이 더 앞서가지 않아 이 브랜치에서 이미 해결해 둔
문서·결정 번호 충돌만 반영됐고, 코드 쪽은 순수 병합. 머지 전 최종 확인으로 `npm test`
146/146 재확인 후 push.

이전(2026-07-30(7), 이 브랜치): 우상단 햄버거 메뉴 + 관리자 로그인 최초 구현(GIS 기반,
위 항목에서 Supabase Auth로 교체됨) — 부부 체크를 "출시 예정"으로 전환. 라우터의 기존
`guard()` 메커니즘을 그대로 써서(`comingSoonGuard()`), 관리자가 아니면 "곧 출시됩니다"
모달 후 `psych-list`로 돌려보낸다 — 목록 카드 클릭·주소 직접 접속·공유 링크 전부 이 한
지점을 거친다. `showModal()`은 `cancelLabel: null`이면 취소 버튼을 생략하도록 확장(새
모달 함수 안 만듦). 이 설계(게이트 위치·모달 확장·범위를 부부 체크 하나로 한정)는 이번
정리에서도 그대로 유지했다 — 바뀐 건 로그인 수단뿐이다.

이전(**main 머지 완료(2026-07-30, 4차)**) — NumPath 난이도·리플레이성·보상 체계(D-54) + 마을
클라우드 동기화(D-55) 브랜치를 main에 병합. 첫 push 시도가 "fetch first"로 거부돼(그 사이
병렬 세션이 부부 체크 D-51~53을 먼저 main에 합침) 다시 fetch 후 origin/main을 로컬 main에
merge — **코드 파일은 전부 자동 병합**, 문서 3개(CURRENT_TASK.md·docs/DECISIONS.md·
docs/decisions/2027-h1.md)만 "서로 다른 절 추가"로 충돌해 내용을 합쳤다.
**결정 번호가 두 번 충돌**: 이 브랜치의 원래 D-34·D-35는 첫 병합에서 D-51·D-52로 재번호했으나,
그 사이 main에 먼저 들어온 부부 체크 세션이 이미 D-51~D-53을 쓰고 있어 D-54·D-55로 한 번 더
재번호(관련 코드 주석·문서 전부 갱신). `docs/decisions/2027-h1.md`가 649줄로 파일 크기 자동
분리 규칙(300줄/15KB)을 넘겼지만, 대부분이 이번 세션이 만든 내용이 아니라(부부 체크
D-32~D-53) 되돌리기 위험이 있는 재구성이라 이번엔 분리를 보류 — 아래 "다음 작업 우선순위"에
남김.
병합 후 `npm test` 146/146, `scripts/verify.cjs` 164/164(`serve.py`) 통과.

이전(2026-07-30, 이 브랜치): NumPath 마을을 data-pantry.com 계정에 선택적으로 동기화(D-55,
사용자가 "로그인 체계 필요하지 않냐"고 물어 진행). data-pantry.com과 같은 Supabase
프로젝트(`duvpvwolgqurhgnhqezj`)에 `numpath_village` 테이블(RLS, 본인 행만) 신설·적용 완료.
로그인은 선택 사항 — 게스트는 기존 localStorage만 사용. 신규 `cloud.js`(Supabase JS, 구글·
카카오 OAuth)는 **static import 금지** — CDN 실패가 모듈 그래프 전체를 깨뜨리지 않도록
`cloud-loader.js`(동적 import + 실패 캐시)로만 연결했다. 로그인 감지 시 `mergeVillages()`
(coins=max, built=합집합, 멱등)로 로컬·클라우드를 합치고, 이후 코인 획득·건설마다 로그인
상태면 클라우드에도 반영. 마을 화면에 연동 패널 추가, XSS 방지용 `escapeHtml()` 신설
(`core/util.js`, OAuth 표시 이름을 처음으로 템플릿에 꽂는 사례).
`npm test` 80/80, `scripts/verify.cjs` 93/93 통과(CDN 차단 상태에서 패널이 "사용 불가"로 착지하고
나머지 화면은 정상 동작하는 것까지 확인 — 샌드박스가 실제로 이 조건이라 폴백 경로가 검증됨).

이전(2026-07-30, 이 브랜치): NumPath 게임 개선 3종(난이도 시스템·리플레이성 강화·보상 체계) —
상세는 `docs/decisions/2027-h1.md` D-54, `docs/numpath-architecture.md` 참고.

이전(2026-07-30, main — 이 브랜치와 별개로 먼저 병합됨): 부부 체크 결과 설명 상세화(D-51)·
AN3 자기참조형 재작성(D-52)·AN3·개인 성향 설명 문구 순화·확장(D-53) 3건이 순차로 main에
병합됨. 상세는 `docs/decisions/2027-h1.md` D-51~D-53, `PROGRESS.md` 참고.

이전(2026-07-30(6), main): **코드 정리 — 부부 체크 파츠화 + 사용 안 하는 코드 제거**(D-50).
"수정의 수정을 거치며 불필요한 코드·로직이 쌓였는지" 점검 요청.
- `js/tests/couple/screens.js`가 1274줄까지 커져 있었다(DISC 690·ADHD 534의 거의 두 배) —
  D-45(짧은 코드)·D-46(개인정보처리방침)까지 계속 얹기만 하고 안 쪼갠 결과. 배우자와 합치는
  흐름(초대·코드 입력·결합 결과)을 `screens-match.js`로 분리, 두 화면 파일이 같이 쓰는
  캔버스 배경·헤더·바닥 URL은 `card.js`로 뺐다. 결과 779줄/490줄로 정상 범위.
  의존 방향은 screens-match.js → screens.js 한쪽뿐(순환 없음).
- 그 과정에서 실제 코드 중복 발견: 공유 카드·코드 카드 두 캔버스 함수가 배경·헤더·바닥 URL을
  토씨 하나 안 틀리고 복사해 갖고 있었다(각각 다른 시점에 추가되며 생긴 중복) — `card.js`로
  통합.
- 다른 파일에서 아무도 안 쓰는데 `export`가 붙어 있던 것 2개 제거(`score.js`의
  `QC1_EXPECTED`·`factorRaw`, 자기 파일 안에서만 쓰임). 반대로 `resetCouple`은 분리 전엔
  안 쓰이는 것처럼 보였지만 `screens-match.js`가 실제로 가져다 쓰게 되며 export가 정당해짐.
- 아무 화면에도 안 쓰이는 죽은 CSS 규칙 1개(`share-note`) 제거.
- **손대지 않은 것**: `scripts/verify.cjs`(1166줄)는 회귀 검사가 계속 쌓이는 성격의 파일이라
  길어지는 게 정상이고(PROGRESS.md와 같은 부류), 검사 하나하나가 서로 독립적이라 "쌓인
  불필요한 코드"에 해당하지 않는다고 판단해 그대로 뒀다.
`npm test` 128/128(불변), `scripts/verify.cjs`를 `serve.py`·`wrangler pages dev` 양쪽에서
분리 전후 여러 차례 통과 확인.

**main 머지 완료(2026-07-30, 2차)** — 위 D-50 코드 정리(파츠화) 커밋이 main에 fast-forward
머지됐다(`aca2595` → `b125fe6`, 배포 트리거). 이번엔 main 쪽에 독립 커밋이 없어(직전 머지
이후 main이 그대로) 재번호 충돌·통합 버그 없이 순수 fast-forward. 머지 전 최종 확인으로
`npm test` 128/128, `scripts/verify.cjs`를 `serve.py`·새로 띄운 `wrangler pages dev` 양쪽에서
재실행 — 첫 `wrangler` 실행에서 반응속도 게임 경로 2건이 실패했으나, 원인은 4시간째 떠 있던
이전 세션의 스테일 `wrangler` 프로세스가 포트를 물고 있어 요청을 가로챈 것(코드와 무관한
환경 문제, `PROGRESS.md`의 기존 "스테일 wrangler" 사례와 동일 패턴) — 프로세스를 정리하고
재기동하니 재현 안 됨, 전체 재통과 확인 후 머지.

이전(2026-07-30(4)): **개인정보처리방침 페이지 신설**(D-46). "상용화해도 되나" 점검에서 언급한
② 항목 — D-45로 부부 체크 짧은 코드가 KV에 7일 저장되는데도 그 사실을 알리는 페이지가
없었다. `/privacy`(홈 하단 링크) 신설. 실제로 저장·처리하는 것만 적었다(부부 체크 코드
TTL 7일·식별자 없음, 로컬 저장소 미사용, AdFit·Cloudflare Web Analytics 사용 사실).
사업자등록번호 등 운영 주체 정보는 코드가 모르는 사실이라 채우지 않았고, 문의처는
사용자가 제공한 이메일(xogns022@gmail.com)을 썼다. 법률 자문을 대체하지 않는다는 점도
결정 기록에 명시.
`npm test` 123/123, `scripts/verify.cjs` 백엔드 포함·미포함 둘 다 통과.

**main 머지 완료(2026-07-30)** — 이 브랜치(부부 관계 성향 체크 + 짧은 코드 매칭)와 별개로
main에서 독립적으로 진행되던 작업들과 합쳐졌다. main 쪽 최근 작업:
- 목록 화면(심리테스트·미니게임)에 배너 추가 + 홈 하단 배너를 모바일 전용 고정(sticky)
  도크로 전환(`.home-ad-dock`). 관련 결정 D-49
- 배너 상단·하단이 같은 AdFit 광고 단위를 재사용해 한쪽만 채워지던 문제 수정
  (`docs/ERRORS.md` E-11) — `bannerTop`/`bannerBottom` 단위 분리
- DISC S축(안정형) 선택지가 사회적 바람직성 편향에 취약해 안정형 고유 행동으로 재작성
- DISC 인트로에 12유형 미리보기 화면(`disc-types`) 추가
- 테스트·게임 진입 화면 3곳에 페이지별 OG 미리보기(`og-shells/*.html`) 추가, `_redirects`의
  `.html` 확장자가 Cloudflare clean-URL 정규화로 308을 유발하던 버그 수정(D-47 갱신)
각 항목 상세는 `PROGRESS.md`에 온전히 남아 있다 — 이 파일은 크기 유지를 위해 요약만 둔다.

**머지 번호 충돌**: 두 브랜치가 각자 D-32~34를 다른 결정에 썼다 — main 쪽 3개(OG 셸·
DISC 미리보기·배너 도크)를 D-47~49로 재번호했다(부부 체크 D-32~46은 그대로 유지, 참조가
훨씬 많아 재번호 비용이 컸다).

**머지가 드러낸 실제 버그**: main이 광고 슬롯을 `"banner"` → `"bannerTop"`/`"bannerBottom"`로
나눴는데 부부 체크 화면들은 옛 이름을 그대로 쓰고 있어서, `AD_UNITS["banner"]`가
undefined가 되어 **부부 체크 인트로가 통째로 빈 화면이 되는** 회귀였다(각 브랜치 단독으론
안 보이고 머지해야 드러남). 12곳 호출을 전부 고쳐 수정. `npm test` 128/128,
`scripts/verify.cjs`를 `serve.py`·`wrangler pages dev` 양쪽에서 각 2회 연속 통과 확인.

이전(2026-07-30(3)): "상용화해도 되나" 질문에 답하며 발견한 보안 구멍 수정.
짧은 코드 발급 API(`POST /api/couple-code`)가 인증 없는 공개 엔드포인트인데, 페이로드
**길이만** 보고 KV에 저장하고 있었다 — 진짜 유효한 부부 코드인지는 안 봤다. 누구나 임의
문자열을 계속 보내면 하루 KV 쓰기 한도(1,000회)를 실제 사용자보다 먼저 채울 수 있는
구멍이었다. 저장 전에 `match.js`의 `decodePartner()`를 그대로 가져다 써서 체크섬까지
통과하는 진짜 코드만 저장하도록 막았다(형식을 새로 정의하지 않고 기존 검증 재사용).
`npm test` 123/123, `scripts/verify.cjs` 128/128(백엔드 포함) · 124/124(백엔드 없음).

이전(2026-07-30(2)): 실제 화면 스크린샷 피드백 3건 검토 — ① 분담 공정성(AN3) 앵커에 방향이
없다는 지적은 설계 판단이 필요해 보류(AN3 문항 자체가 대칭 질문이라 원천적으로 방향을
측정하지 않음). ② 가까움·거리 막대(ANX/AVO)에 항상 해설이 안 붙던 실제 결함은
`attachmentText()`로 수정. ③ 축 선택별 문항 텍스트가 실제로 달라지는지 확인 + "달라야 할
게 실수로 같아지는" 반대 방향 회귀 검사 추가.
`npm test` 123/123, `scripts/verify.cjs` 127/127(백엔드 포함) · 124/124(백엔드 없음).

이전(2026-07-30): **부부 결과 매칭 — 짧은 코드 + 백엔드 도입** (D-45). 이 사이트 최초로 백엔드가
생겼다 — Cloudflare KV(`COUPLE_CODES`) + Pages Functions(`functions/api/couple-code/`). 25자
배우자 코드는 그대로 두고, 8자 짧은 코드(Crockford Base32, TTL 7일)가 그걸 가리키는 포인터로만
KV에 들어간다. 인트로·결과 화면에 "부부 결과 매칭" 버튼 추가, `couple-pair`가 직접 입력 폼
(짧은 코드·25자 코드·링크 전체)도 보여준다. 백엔드 없이도(`serve.py`) 25자 링크 경로는
그대로 동작 — 폴백 우선 설계. 공유 카드(코드 없음)와 배우자 전용 코드 카드를 분리했다.
`CLAUDE.md`의 "백엔드·영속 데이터 없음" 불변식을 이 기능 하나로 한정된 예외로 갱신.
`npm test` 122/122, `scripts/verify.cjs` 123/123(백엔드 포함) · 120/120(백엔드 없음).

이전(2026-07-29(5)): 부부 체크 오류 검토 — 뒤로가기가 직전 답을 지우던 결함(D-44) 수정,
분포 특성 측정(무작위 응답 3만 건 — 갈등 스타일 CS5 69% 집중·애착 AT4/AT1 편중은 기획서
절단점 특성이라 임의로 손대지 않음). `npm test` 117/117, `scripts/verify.cjs` 116/116.

이전(2026-07-29(4)) ~ (2026-07-29 최초): 부부 관계 성향 체크 신설(기획서 v3.0) → v3.2 개정
(컴플라이언스 재작명·단일 궁합 점수 폐기, D-36~D-39) → v3.3 개정 + 이용 안내 화면(D-40~D-42)
→ 결과 화면 정보 계층 정리(D-43)까지 이어지는 초기 구축 이력. 상세는 `PROGRESS.md`와
`docs/couple-architecture.md`.

이전(2026-07-28, 메인 머지 완료): 독립 미니게임 **NumPath: Stack & Clear**(숫자 경로 퍼즐) 추가.
`js/games/numpath/`(engine·generate·solve·audio·play·screens·index) 신설, 라우터에
`registerGame`/`listGames` 신규 도입(`registerTest`/`listTests`와 대칭), `/game/numpath/*` 4화면.
`renderGameList()`가 이제 `listGames()`로 카드를 자동 생성해 "미니게임 목록이 비어 있음" 항목이
해소됨. 리뷰 중 `starsFor()`의 2성 등급이 모든 레벨에서 도달 불가능하던 버그를 찾아 같이 수정.
이어서 사용자 피드백("목표값이 안 보이고 설명 부족")으로 플레이 화면 HUD에 "현재값 → 목표값"
전용 배너를 추가하고 인트로에 칸 종류(시작/연산/배수/차단) 안내를 붙였다.
관련 결정: `docs/decisions/2027-h1.md` D-28(독립 게임 레지스트리)·D-29(공유 URL, D-7과 반대
방향)·D-30(타일 모델)·D-31(1차 범위 축소). 상세는 `docs/numpath-architecture.md`.
`docs/architecture.md`가 15KB를 넘겨 자동 분리 규칙에 따라 `docs/adhd-architecture.md` ·
`docs/disc-architecture.md` · `docs/numpath-architecture.md`로 나뉘었다 — 이제부터 ADHD/DISC
상세를 찾을 땐 architecture.md가 아니라 이 세 파일을 본다.

더 이전(2026-07-28 앞부분, 메인 머지 완료): 광고 수익화 3건·DISC 명칭 변경·딜레마 4택 재설계·
데이터팬트리 연동·홈 화면 기능 없는 헤더 ☰ 버튼 제거. 관련 결정은 `docs/DECISIONS.md` D-24~D-27.

